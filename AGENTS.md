# IXAEON 开发 Agent 阅读入口

## 只读这三份

1. **[委派队列](docs/委派/队列.md)** — 你能领的活，按顺序领（见下文「委派流水线」）。
2. **[三周任务单](docs/三周任务单.md)** — 当前唯一在用的计划，讲清楚为什么做这些。
3. 本文件 — 交付规则。

`docs/history/` 是历史规划与审核记录，**保留为事实，不决定当前顺序**。不要因为里面写着「继续推进 P0–P6」就去做 P0–P6。长期目标见 `docs/history/IXAEON_长期愿景与开发路线图.md`，但当前只做三周任务单里的事。

`REVIEW_PACKET.md` 是交付流水账，只追加。

## 当前唯一的验收信号

> 打开 IXAEON，连着聊三轮，它记得住，并且至少有一件事是它替你做完的。

**不看门禁绿不绿。** 2026-09-17 的方向纠正就是因为「全绿」取代了真实验收：9-16 一天「完成」了当天才写出的五个阶段，核查证实多为空壳（`selectModelResource` 全仓零调用、`ModelPool` 参数硬编码猜测值、`RoleCoordinator` 124 行内存 switch）。

## 三条硬规矩

1. **不许自己写自己的验收标准。** 验收条件由用户或整合方先写死，你只负责让条件成立。仓库里 27 套按审核编号命名的回归套件，就是「自己写代码自己判对错」的产物。
2. **一个任务一个小改动，超过 300 行必须拆。** 大块交付无法审，只能被「看到全绿就过」。
3. **需要发明新概念或新接口的任务，退回，不要做。** `ModelPool` 是规格填空的产物：字段齐全，值是编的。遇到这种任务说明它不该委派。

## 交付规则

- **分开报告：已实现 / 自动化通过 / 真机通过 / 用户接受。** 四者不能互相冒充。模拟工具不等于真实接入，生成提示词不等于已执行，CLI 退出码 0 不等于验证通过。
- **不写新的规划文档。** 需求变化直接改三周任务单。缺口如实写在交付说明里。
- **迁移只追加，不改写已发布迁移。** 迁移文件只由整合方编写。
- **保留他人未提交改动。** 历史文档、失败证据不覆盖，新结论追加。
- **暂停未上的能力**：Door、模型池、自我升级、多设备、角色治理停在设计稿，不要「顺手」实现。
- 权限分离不变：本地读取、云端外发、编码执行、正式升级分别授权；个人记忆不自动暴露给所有 MCP 客户端。

## 委派流水线（2026-09-18 起，用户已同意）

目的：整合方（Claude）不在的时候，执行方（Grok、Codex 等）也能按队列连续开发；验收交给机器，不靠谁口头说「全绿」。

### 领活

1. 打开 [委派队列](docs/委派/队列.md)，领第一个「待领」且依赖都已完成的任务，把状态改成「进行中（你的名字）」。
2. **一个任务一个分支、一个工作目录，不在共用目录里改**（多方同时在共用目录里改，会互相挡住）：

   ```bash
   git worktree add ../ixaeon-<任务号> -b <你的名字>/<任务号>
   ```

   Codex 云端任务本来就在自己的分支上，照常即可。

3. 读委派单：改哪些文件、不许碰什么、验收测试在哪，都写在里面。委派单里写死的接口不改名、不加字段。

### 做完的标准

- `node scripts/acceptance.mjs run <任务号>` 全过——这是整合方先写好、**锁定**的验收测试；
- `node scripts/verify.mjs` 全过（其中 `acceptance-lock` 一步核对锁定测试没被改）；
- 推送分支后，GitHub 上的 verify 检查是绿的。

**锁定的验收测试不许改。** 指纹记在 `docs/委派/锁定验收.json`，改了门禁就不过。觉得验收测试本身有错：停下，在交付说明里写清楚是哪条、为什么，由整合方改。你可以另加自己的测试，不能改锁定的。

### 合并

- **A 档**（界面、文案、小功能；不加迁移、不删数据、不碰权限与隐私、不改提示词）：CI 绿即可由用户合并。
- **B 档**（迁移、数据清理、权限与隐私、提示词、改变记忆含义的）：排队，由整合方复审后合并。
- 档位写在队列里，由整合方定。合并的人（用户或整合方）随后运行 `node scripts/acceptance.mjs done <任务号>` 并提交，队列里的状态改成「已完成」——这个任务的验收测试从此进门禁。

### 交付说明（每单必写）

改了哪些文件、各多少行；验收测试与 verify 的输出；**真机上点过没有**（没点就写没点）；已知缺口。四者分开写：已实现 / 自动化通过 / 真机通过 / 用户接受。

### 公开仓库

仓库是公开的。测试、文档、提交说明里不写用户的真实数据：真实记忆内容、网关地址、密钥、个人信息都不行。需要例子就用合成数据。真机验证用合成数据或用户数据的副本，不动用户的原始数据。

## 根目录只读资料

`JARVIS_CONSTITUTION_v0.1.md`、`Jarvis_Constitution_v0.1.docx`、`grok-20260903-IXAEON-思想碰撞.md` 为原始思想资料，不移动、不改名、不修改。
