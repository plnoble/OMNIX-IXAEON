# IXAEON 开发 Agent 阅读入口

## 只读这三份

1. **[委派队列](docs/委派/队列.md)** — 你能领的活，按顺序领（见下文「用户的原则」和「委派流水线 v2」）。
2. **[三周任务单](docs/三周任务单.md)** — 当前唯一在用的计划，讲清楚为什么做这些。
3. 本文件 — 交付规则。

`docs/history/` 是历史规划与审核记录，**保留为事实，不决定当前顺序**。不要因为里面写着「继续推进 P0–P6」就去做 P0–P6。长期目标见 `docs/history/IXAEON_长期愿景与开发路线图.md`，但当前只做三周任务单里的事。

`REVIEW_PACKET.md` 是交付流水账，只追加。

## 当前唯一的验收信号

> 打开 IXAEON，连着聊三轮，它记得住，并且至少有一件事是它替你做完的。

**不看门禁绿不绿。** 2026-09-17 的方向纠正就是因为「全绿」取代了真实验收：9-16 一天「完成」了当天才写出的五个阶段，核查证实多为空壳（`selectModelResource` 全仓零调用、`ModelPool` 参数硬编码猜测值、`RoleCoordinator` 124 行内存 switch）。

## 三条硬规矩

1. **不许自己写自己的验收标准。** 验收条件由用户或整合方先写死（委派单的锁定测试，或规格里的「验收条件」），你只负责让条件成立。v2 起你可以把规格里的条件写成测试，但测试要经审查（A 档 Codex、B 档整合方），条件一条不许删、不许放宽。仓库里 27 套按审核编号命名的回归套件，就是「自己写代码自己判对错」的产物。
2. **一个任务一个小改动，超过 300 行必须拆。** 大块交付无法审，只能被「看到全绿就过」。
3. **需要发明新概念或新接口的任务，退回，不要做。** `ModelPool` 是规格填空的产物：字段齐全，值是编的。遇到这种任务说明它不该委派。

## 交付规则

- **分开报告：已实现 / 自动化通过 / 真机通过 / 用户接受。** 四者不能互相冒充。模拟工具不等于真实接入，生成提示词不等于已执行，CLI 退出码 0 不等于验证通过。
- **不写新的规划文档。** 需求变化直接改三周任务单。缺口如实写在交付说明里。
- **迁移只追加，不改写已发布迁移。** 迁移文件只由整合方编写。
- **保留他人未提交改动。** 历史文档、失败证据不覆盖，新结论追加。
- **暂停未上的能力**：Door、模型池、自我升级、多设备、角色治理停在设计稿，不要「顺手」实现。
- 权限分离不变：本地读取、云端外发、编码执行、正式升级分别授权；个人记忆不自动暴露给所有 MCP 客户端。

## 用户的原则（设计上的小决定照这个来）

用户说过、定过的，规格没写到的地方按这些判断；拿不准就停下写进交付说明。

- **不想逐条审核。** 要用户拍板的东西必须控制数量：能自动判断的自动做，只把真正的冲突和决定留给用户。例：收件箱只放冲突、手动、工作结果、有争议的；知识性问题不附「建议待办」。
- **AI 建议不是定论。** AI 给的方案记成「AI 建议」，不当成用户的决定；时间久了方案可能过时，也可能当时没想全。
- **隐私优先。** 用户的真实数据（聊天、记忆、会话记录、数据库）只读、用副本；**不打印、不复制、不外发其中的内容**。需要看真实数据的形状，只用 `scripts/real/` 里只输出统计数字的脚本。新的外发（新的服务、新的网络访问、把新一类数据发给模型）一律 B 档。
- **如实报告。** 没做的写没做，没验证的写没验证；门禁绿不等于能用。

## 委派流水线 v2（2026-09-19 起，用户已同意）

目的：整合方（Claude）额度有限，大部分开发由执行方（Grok）连续完成，Codex 做第二道审查。整合方只定方向、写规格、复审 B 档、每周抽查一次已合并的 A 档并完整用一遍。**A 档从头到尾不需要用户转话，也不需要整合方。**

### 领活

1. 打开 [委派队列](docs/委派/队列.md)，领第一个「待领」且**依赖都已完成**的任务（依赖没完成的不能领，哪怕可以先写测试）。**直接在 main 上**把这一行的状态改成「进行中（你的名字）」，提交并推送（只改这一行）。任务分支里不改队列：几个分支改相邻的行会冲突。

   改 main 一律在你自己的**操作目录**里做，不在用户和整合方共用的检出目录（`OMNIX-IXAEON析衍`）里提交——那里可能有别人没提交的改动，会被一起扫进你的提交：

   ```bash
   git worktree add --detach ../ixaeon-ops origin/main      # 只需一次，之后照任务目录一样装好依赖
   cd ../ixaeon-ops && git fetch origin && git checkout --detach origin/main
   # 改 docs/委派/队列.md 这一行
   git commit -am "领 <任务号>：…" && git push origin HEAD:main
   ```

2. **一个任务一个分支、一个工作目录，不在共用目录里改**（多方同时在共用目录里改，会互相挡住）：

   ```bash
   git worktree add ../ixaeon-<任务号> -b <你的名字>/<任务号>
   ```

3. 读委派单或规格：改哪些文件、不许碰什么、验收条件、要跑哪个真机检查，都写在里面。写死的接口不改名、不加字段。

### 委派单与规格

- **委派单**（v1，整合方已写好锁定的验收测试）：照旧，只让锁定测试通过。
- **规格**（v2 起的大多数任务）：整合方只写一页——要什么、为什么、约束、验收条件、真机检查。你先把每条验收条件写成测试，放 `packages/*/test/acceptance/` 或 `apps/*/test/acceptance/`，文件名 `<任务号小写>-*.test.ts`，测试开头注释逐条对应规格的条件；再用 `node scripts/acceptance.mjs lock <任务号> <标题> <文件…>` 登记指纹。
  - A 档：测试和实现一起交，Codex 审查时核对测试有没有漏掉或放宽条件。
  - B 档：**先只推测试**，队列状态改「测试待审（整合方）」，接着去领别的；整合方看过、在 main 上锁定之后再写实现。
- 规格里的条件一条不许删、不许放宽。觉得条件本身有问题：停下，写进交付说明，由整合方改。

### 做完的标准

- `node scripts/acceptance.mjs run <任务号>` 全过；
- `node scripts/verify.mjs` 全过（其中 `acceptance-lock` 一步核对锁定测试没被改）；
- **真机检查**：委派单或规格要求的检查照做，输出原样贴进交付说明。检查工具：
  - `node_modules/.bin/jiti scripts/real/agent-sessions.ts`：在内存里试解析本机的 Claude Code / Codex 会话，只输出数量与开头类别（改了会话导入的必跑）；
  - `node_modules/.bin/jiti scripts/real/hermes-ask.ts "问题一" "问题二"`：真 Hermes、临时空库、合成的问题（改了发给模型的提示词或约定的必跑，该触发的、不该触发的都要问到）；
  - 界面：设环境变量 `IXAEON_DATA_DIR=<临时目录>` 启动应用，用合成数据点一遍，描述或截图。
  - 没跑就写没跑，**没跑的不能合并**。
- 交付说明写进 `docs/委派/交付/<任务号>.md`（模板见 [该目录的 README](docs/委派/交付/README.md)），随分支提交；
- 推送分支后，GitHub 上的 verify 是绿的。**推送前先 `git fetch origin && git rebase origin/main`**：分支要基于最新的 main，CI 才会用最新的门禁配置。

**锁定的验收测试不许改。** 指纹记在 `docs/委派/锁定验收.json`，改了门禁就不过。觉得验收测试本身有错：停下，在交付说明里写清楚是哪条、为什么，由整合方改。你可以另加自己的测试，不能改锁定的。

### 合并

**A 档：Codex 审过、CI 绿，你自己合并。**

1. 在任务的工作目录里跑 `node scripts/codex-review.mjs <任务号>`。结论写进 `docs/委派/交付/<任务号>-Codex审查.md`，一起提交。
   - 退出码 0（可以合并）→ 下一步；
   - 1（需要修改）→ 改完再审。Codex 提的「必须改」你不同意：不合并，理由写进交付说明，队列状态改「待复审（整合方）」；
   - 3（Codex 额度用完）→ 分支先等着，队列状态改「待审（Codex 额度）」，去领下一个，额度恢复后回来重跑。**不许跳过审查直接合并；**
   - 2（结论不明或出错）→ 同 3，并把输出贴进交付说明。
2. 分支的 CI 是绿的，然后在你的操作目录里（见「领活」）：

   ```bash
   cd ../ixaeon-ops && git fetch origin && git checkout --detach origin/main
   git merge --no-ff origin/<你的名字>/<任务号>
   node scripts/acceptance.mjs done <任务号>
   # 队列里这一行改成「已完成（日期）」
   node scripts/verify.mjs
   git add -A && git commit -m "并入 <任务号> …" && git push origin HEAD:main
   ```

3. 推送后看 main 上的 CI。红了：立刻修，修不了就 `git revert` 这次合并并在交付说明里写清楚。

**B 档：** 推送分支，队列状态改「待复审（整合方）」，接着领下一个。整合方复审后合并。

**只有整合方做的步骤——你就算自己审过、觉得没问题，也不做：**

- B 档测试的锁定（`acceptance.mjs lock`）和 B 档的合并、`acceptance.mjs done`；
- 迁移；改锁定的验收测试；改规格或委派单。

这些是给整合方的关卡，执行方自己过关等于没有关卡。2026-09-19 执行方自己复审并合并了 T2b、自己锁了 S3a / P3 / W1a 的测试：三份测试里有两处等于没测到规格的条件（W1a 用一条本来就不会发出去的记忆去测「不发没采纳的 AI 建议」；S3a 只拦 `readFileSync`，把整份逐块读完也能过），整合方事后补全重锁。等整合方的时候去领别的活，不要替整合方过关。

**什么算 B 档**：迁移；删除或清理数据；权限与隐私；发给模型的提示词或约定；改变记忆含义的（什么算用户说的、什么进记忆、什么给模型看）；新的外部服务或网络访问。其余（界面、文案、小功能、修 bug、性能、测试）是 A 档。档位由整合方写在队列里；你觉得标错了（A 档其实碰了上面这些），按 B 档走。

### 交付说明（每单必写）

写进 `docs/委派/交付/<任务号>.md`：改了哪些文件、各多少行；验收测试与 verify 的输出；**真机检查的输出**（没跑就写没跑）；Codex 审查结论；已知缺口。四者分开写：已实现 / 自动化通过 / 真机通过 / 用户接受。

### 公开仓库

仓库是公开的。测试、文档、提交说明里不写用户的真实数据：真实记忆内容、网关地址、密钥、个人信息都不行。需要例子就用合成数据。真机验证用合成数据或用户数据的副本，不动用户的原始数据。

## 根目录只读资料

`JARVIS_CONSTITUTION_v0.1.md`、`Jarvis_Constitution_v0.1.docx`、`grok-20260903-IXAEON-思想碰撞.md` 为原始思想资料，不移动、不改名、不修改。
