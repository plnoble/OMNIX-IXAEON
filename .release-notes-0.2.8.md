## IXAEON 析衍 v0.2.8

范围：B0–B5 收官批 + 用户实测反馈修复。**不是 0.3.0**（正式版另发）。

### 本版新能力
- **问答落 Core**：每次问答自动存为 ask_session 来源并入队提取，理解候选在「待讨论」等你确认；挂 ask.ixaeon.local 域授权，可随时撤销（撤销=停止提取与读取，与其他来源同一套边界）
- **B5 三平台导入器**：Claude conversations.json / Grok prod-grok-backend.json / Gemini MyActivity.json（Takeout），公开导出格式实现+合成验证，真实数据随用随填
- **研究循环接受控搜索**：带「出门说法」的主题手动检查时做受控搜索返回候选（不是发现，批准后才是来源）；Research 页候选展示+批准为来源
- **任务页验证命令可编辑**：独立验证命令开放配置（默认保持原行为），附权限沙箱说明

### 真机与修复
- 头部显示版本号；问答页显示真实模型名（session.info 捕获）
- 问话记忆路由约定：引导模型用 IXAEON 记忆而非引擎自带 memory（引擎可替换、Core 资料独立保存）
- MCP 双入口（HTTP 转发 + 直连）打包修复；Hermes 定位器兼容 HERMES_HOME
- Codex 执行器三修复：通道文件越界误判、Windows elevated 沙箱、turn.completed 后挂起（协议终点+10s 宽限+杀树）
- 数据库迁移 19/20（sources 重建扩 provider 枚举；真实日用库副本迁移探针通过、数据保全、源库零写入）

### 已知边界（如实）
- 记忆评测真实模型三轮：相关召回 1.0/1.0/1.0、无关不侵入 1.0/1.0/1.0 达标；临时不升格 0.9/0.9/1.0 未达 ≥0.95（问句歧义复合伪影，待改写重测）
- 真人首个编码任务完成（建草案→批准→真 Codex→独立验证→接受）；任务为小型工具文件
- 安装器/换引擎演练仍缺；对照评测部分为合成数据

安装包：IXAEON-Setup-0.2.8.exe（117.3 MB，Windows 10/11 x64）
SHA-256：`5C96488F1C3FE7CEF9632D5803BE411816F4E3513EFFBF93E78738440719C832`