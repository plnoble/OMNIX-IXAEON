# IXAEON 下一阶段开发计划：先稳定，再完成日用闭环

日期：2026-09-05。用途：直接交给另一个编码 agent 实施，并交回独立审核。

基线提交：`ea9da6266439cc60943a9ecc37c0e654ebd79331`。

当前状态：v0.1 尚未通过完整验收。依据见 [四次验收报告](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.1_四次验收报告_2026-09-05.md)。本文不是“上一版已通过”的证明。

## 1. 这一阶段到底做什么

一句话：**让 IXAEON 从“功能都摆在那里”，变成“你每天可以放心使用的项目记忆”。**

你和 ChatGPT 聊完，IXAEON 自动收好；同一对话归属哪个项目，你只需要确认一次；有了新内容，它知道还没分析完；编码 AI 开工时能拿到带出处、标明新旧的背景，完工后能把结果写回来。

不要求你每次搬运聊天记录，也不把每一句聊天都变成需要手动审批的表单。需要你处理的主要是：项目归属不明、重要决定待确认、真正的冲突。

浏览器扩展仍只负责你打开的网页对话。**本阶段不承诺自动同步 ChatGPT 手机 App、全部历史会话或关闭浏览器之后产生的内容。**

开发分两段：

- **v0.1.1：稳定性收口。** 修复本轮 F1–F4，统一对话身份和分析任务的规则。
- **v0.2：单项目日用闭环。** 做到归属稳定、进度可见、理解可确认、编码工作可回流。

## 2. 范围与不做的事

继续使用现有 Windows 桌面端、浏览器扩展、SQLite、本地原文库与 MCP，不换技术栈，不另起一套服务。

本阶段不做：iOS／Android Door、多设备同步、RAM 压力测试、分布式推理、无人值守执行任务、自动改用户项目代码、全盘扫描、云同步、知识图谱、大规模向量库、全新聊天产品。

Door 的设备探测构思保留，理由很简单：**主 IXAEON 先要可靠地记住一台设备上的事情，才适合接收更多设备的数据。** 不需要现在为了“将来扩展”增加空服务和空接口。

不改历史思想文档、原开发计划和既有验收报告。未获允许，不使用真实账户、密钥、收费模型，不进行系统安装或读取新的用户目录。

## 3. 先统一三个概念

| 概念 | 通俗解释 | 实现必须遵守的规则 |
| --- | --- | --- |
| 对话身份 | 这是哪一本笔记 | 正式对话依靠稳定对话 ID；草稿依靠独立草稿 ID，不依靠标题或正文 |
| 采集实例 | 这次从哪个窗口抄进来 | 可以因刷新、重开而改变，但不能因此新建同一正式对话 |
| 内容版本 | 这本笔记更新到哪一页 | 收到新内容和分析完新内容是两回事，需要分别记录 |

开发第一步：先写一份简短的身份与任务状态设计记录，列出下面的场景，再修改数据层和执行层。不要先增加界面按钮。

必须说明：正式对话刷新、多标签页、同路径草稿 A→B→A、草稿转正、暂停后转正、开关变化、任务运行时收到新内容、退出重启、恢复成功与失败时，各种 ID 和状态如何变化。

## 4. M0：v0.1.1 稳定性收口

### M0.1 对话身份与迁移

落实四次报告 F1 的全部要求，并满足：

- 同一正式 URL 即使来自新标签页或新 sessionId，也落在同一逻辑来源；不同正式 URL 即使正文相同，也不能合并。
- 草稿有能在约定生命周期中保持稳定的标识；草稿查找必须能找回较早的 A，不能只查询最新一条。
- 设计明确的草稿→正式身份绑定。优先保持 sourceId 不变；如必须重定向，旧引用必须可解析，并保留项目、暂停、依据与审计信息。
- 缺乏可靠身份的旧数据保守保留，不能仅凭标题、相同首句或正文包含关系自动合并。
- 来源与片段变更具有事务一致性；同步重试不多建记录。原文内容寻址文件可以保留多个版本，不能借去重覆盖历史。
- 升级迁移必须在旧库测试副本上验证。发现历史重复记录，不可直接批量删除；给出可预览的处理策略。不得操作用户真实库来“修数据”。

建议将身份解析提取为单独的可测试模块，避免继续在 HTTP 处理函数里堆例外判断；不要求引入额外框架。

### M0.2 可靠分析任务

落实 F2–F4。增加最小的持久化版本记录，名称可调整，但必须表达以下含义：

- `contentRevision`：当前可用内容版本。新增、编辑、当前分支切换等影响理解的变化都更新；完全重复提交不更新。
- `analyzedRevision`：已经成功生成理解的版本。提取失败、取消或引用不合法时不得推进。
- `targetRevision`：某次任务实际读取的版本。任务执行过程中又来新内容，要能识别还需补分析。

实现要求：

1. 同一来源最多一个正在运行的自动提取和一个合并后的后续需求；不同来源不能共用一个待分析标记。
2. 用数据库中的版本差或等价持久化状态表示“还欠一次分析”。内存计时器只负责延迟，不是唯一记忆。
3. 崩溃重启后能找出未完成工作。暂停的只保留待处理状态，不自动发送；恢复允许后处理最新版本。
4. 自动任务入队、真正执行、下一块模型调用和结果提交时检查开关／权限／暂停；用户取消会阻止后续块和结果提交。
5. 自动与手动任务分开判断。关闭自动分析不阻止用户对仍获授权的来源明确执行手动分析；撤销权限则一律禁止。
6. 运行任务使用一致的输入版本；不能把版本 3 的分析结果标成版本 4 已完成。旧版本完成可保留并明确标旧，再补最新版本，不允许较旧任务覆盖较新结果。
7. 模型失败保留旧理解和待分析版本，提供重试。仅对暂时性失败有限重试，建议最多 3 次并退避；认证失败、预算限制、权限拒绝不循环重试。
8. 退出／恢复前停止接收并取消、等待相关写入任务结束，不能关库后让旧回调继续写。失败恢复不丢原有待处理工作；回滚不完整继续保持故障状态，不创建空库掩盖问题。

模型请求已经发出后，不能声称“关闭开关就能收回资料”。界面与文档应如实说明边界。

### M0 验收门槛

- 本轮 U1–U7、前两轮 24 项独立回归及原有测试全部通过。
- 将本轮独立测试接入 `verify`，不得只手动跑一次。
- 新增“真浏览器扩展→真实本地接收服务→真实数据库”的受控串联测试；覆盖刷新、两个标签页、草稿转正、暂停。模型仍可用 FakeProvider。
- 新增新版本到达时任务正在运行、窗口内退出重启、取消后模型返回、恢复失败保留待处理状态等测试。
- 旧库迁移、原文与引用完整性、构建和打包复核通过。

通过这一自动化门槛后才能进入 M1。当前真实网站、真实模型、安装器人工验收可以在后续开发中穿插完成，但它们是发版门槛，不能用 M0 的自动化结果代替。

## 5. M1：知道资料属于哪里、处理到哪一步

优先扩展现有 Sources、Inbox、Overview／Settings 页面，不新增复杂工作台。

### M1.1 一次归属，后续继承

- 新网页来源先显示“未归属”。用户可把整场对话绑定到一个项目，后续新增消息继承，不要求逐条绑定。
- 未确认的模型项目猜测只能作为建议，不能悄悄加入某个项目的正式背景。用户明确绑定后，以该绑定为准。
- 项目归属操作与“确认结论正确”分开，不要把选项目同时当作认同 AI 结论。
- 重新绑定项目必须提示影响。在一个受控事务／流程中更新派生条目的有效归属，不能原文在 B 项目、理解还在 A 项目。
- 人工单独归属／纠正过的条目不能被静默搬走；遇到冲突列入待处理，受影响的旧项目简报不得继续无提示地使用这些条目。
- 刷新、重开、草稿转正与重新分析都不丢人工归属。
- 本阶段一场对话默认只绑定一个项目，不做按消息自动拆成多个项目。

### M1.2 展示真实状态

每个来源至少能看到：所属项目、最后收到内容的时间、内容版本、最后成功分析时间、已分析版本、当前任务状态和错误原因。

界面使用普通人能读懂的文案：

- 已收到，等待分析。
- 正在分析。
- 已分析最新内容。
- 有新内容尚未分析，当前显示旧理解。
- 自动分析已关闭／当前对话已暂停／授权已撤销。
- 分析失败，可以重试。

“收到资料”和“模型理解完成”不能共用一个成功标记。“最后同步时间”不能只取来源首次创建时间。

刷新状态不能只靠用户切换页面。采用已有 IPC 的事件或低频轮询即可；状态刷新不调用模型。

### M1 验收

1. 对话绑定 IXAEON 项目后，刷新、重开、转正和追加内容均保持绑定；后续无需手动搬运记录。
2. 两个项目的资料分别带唯一测试标记，带项目范围的检索／简报不能串入另一项目。
3. 模型缺配置、故障、取消、延迟时，界面明确区分“保存好了”和“理解好了”。
4. 用户在 Sources 发起归属与重试后，可在界面和数据库验证同一结果，不靠测试直接改库来替代功能。

## 6. M2：重要理解可确认，改口不会被冲掉

复用现有 items、corrections、needs_review、disputed／superseded 机制；仅补足必要的确认记录，不重建整个记忆系统。

必须区分三个维度：谁提取的、用户是否确认、目前是否有效。AI 提取的条目经用户确认后，不能篡改为“原本就是用户写的”。

本阶段交付：

1. **看依据**：待处理条目显示说话人、时间、项目和可展开的原文依据。
2. **确认／纠正／暂不处理／不采纳**：动作含义清楚并留审计；“不采纳”不会变成“确认正确”。旧记录与原始对话保留。
3. **控制审批负担**：普通有依据的 AI 理解可自动产生并明确标记；重要决定、冲突、项目不明的条目进入待处理。不是每条消息都需要审批。
4. **人工改口优先**：重新分析同一来源不能覆盖人工纠正，也不能反复把已否决建议当作当前决定；后续出现矛盾证据时提示新冲突，不自行抹掉纠正。
5. **冲突真的可见**：界面查询必须包含待展示的 disputed 条目，不能只取 current 后再试图从中筛 disputed。

验收用至少 6 组固定资料：AI 提议但用户没答应、用户明确否决、用户后来改口、不同来源矛盾、证据不足、编码 agent 声称完成但用户未验收。每组都检查界面、持久化状态和 MCP 输出，不只检查提取函数。

重要决定未经确认时，可以作为“待确认理解／风险”被读取，不能伪装成“用户已拍板”。具体确认策略写进产品说明，且旧数据迁移时不得全部默认已确认。

## 7. M3：编码 AI 的开工与收工闭环

继续使用已有四个 MCP 工具：`prepare_task`、`search_context`、`get_source_excerpt`、`record_work_result`。不为了本阶段另建一套聊天接口。

### 开工背景

- 项目简报包含：当前目标、已确认决定、待确认／冲突、否决事项、未完成工作、近期编码结果。
- 区分 AI 提取、用户确认／纠正与编码 agent 自报结果；用户未验收前不能把“agent 报成功”写成“用户验收通过”。
- 标明覆盖到哪个内容／分析版本。如果新内容未分析，简报明确“可能落后”，并提供按需查阅依据的入口，不伪装最新。
- 只有用户明确绑定或确认归属到该项目的资料进入该项目背景；未归属资料仍可在独立入口处理。
- 继续满足引用、撤权、字符预算与资料不足要求。新增说明文字也计入约定的预算统计，输出实际结构仍须通过契约校验。

### 收工回写

- 记录任务、结果、变更、测试、未完成事项、提交引用；未运行的测试必须写未运行。
- 增加可选的客户端提交 ID／幂等键，相同请求重试不产生多条相同 work_run；同键不同内容明确报冲突。旧客户端不传时维持原有兼容行为。
- 回写可以更新“近期工作”，但不直接覆盖用户长期决定与纠正。
- UI 能看见回写结果，并从下一次开工简报读到；不只证明 SQL 多了一行。

### M3 验收

1. 用全新的独立 MCP 客户端进程走完“获取背景→查询依据→写回工作→再次读取”，不把背景预先硬塞到客户端输入里。
2. 再补一轮真实编码 AI 客户端验证：让它仅根据项目标识和任务主动读取背景，完成一项安全、明确授权的小任务后回写；保留脱敏的工具调用记录。
3. 原有四工具旧输入仍有效；新增字段有契约测试，不能只改桌面端不改 MCP 端。
4. 工作结果重复提交、失败重试、撤权、错误项目、字符预算边界都有测试。

第 2 项需要用户可用的客户端与授权；没有条件时先完成脚本验证和操作清单，明确保留未验证项，不伪造调用记录，也不擅自创建新任务替用户试用。

## 8. 发布前真人验收

建议把“IXAEON 自己”作为第一个试用项目，先用非敏感资料。以下是验收工作，不是允许开发 agent 自动读取真实账户或付费调用的授权。

### A. 当前真实 ChatGPT 网页

用户完成配对和开启采集后，实测普通问答、流式回答、刷新、关闭重开、多标签页、草稿转正、暂停与继续。

页面内容稳定后，原文应在原计划要求的 10 秒内出现在来源页。**这 10 秒指本地接收，不是模型分析完成。** 若站点结构不支持可靠采集，显示不支持／失败，不能默默显示同步成功。仅验证可见内容，不宣称已抓到未打开的历史。

### B. 真实模型语义

完成原修复任务第 11.6 节的六组问题，并加入 M2 的六类语义情形。先写预期事实、允许的不确定回答和判据，再运行真实模型，避免事后照输出改答案。

记录模型标识、提示词版本、来源引用、实际回答、审核意见和提供者返回的用量；没有用量数据时标未知。不保存密钥，不把完整私密对话写进公开审核包。

必须满足：结论有可核查依据；不混淆 AI 提议与用户决定；不串项目；人工纠正优先；资料不足时不编造。失败样例保留并进入回归，不靠只挑成功截图交差。

### C. 安装与升级

在用户授权的干净测试账户／虚拟机执行安装器；用界面选自定义目录、退出重启、确认资料仍在；连接随包 MCP，不依赖全局 Node；验证从 v0.1 测试数据库升级、导出和恢复。

卸载数据保留／删除行为必须明确。测试只用临时资料，不能拿用户真实库做恢复和卸载试验。

### D. 短期日用观察

上述门槛通过后，建议由用户连续试用 3–7 天。这里只记录试用要求，不自动创建监控任务。

观察：是否还要手动搬运正文、是否重复建档、最新内容是否漏分析、关闭开关后是否仍产生自动请求、编码 AI 是否读到旧背景、纠正是否被覆盖。

记录必要的本地计数、耗时和错误即可，不做遥测。真实模型调用前有明确开关与调用次数上限；费用无法准确计算时不要显示虚构的金额。

## 9. 建议修改位置

以下是实施范围地图，不代表要同时重写所有文件。

| 工作 | 主要位置 |
| --- | --- |
| 稳定身份、草稿转正、采集暂停 | `apps/extension/src/content.ts`、`background.ts`；`apps/desktop/src/main/server/localServer.ts` |
| 来源事务、版本与迁移 | `packages/core/src/storage/sourceStore.ts`、`packages/core/src/db/migrations.ts`、`packages/contracts/src/entities.ts` |
| 分析排队、取消、恢复衔接 | `apps/desktop/src/main/appRuntime.ts`、`packages/core/src/jobs/jobQueue.ts`、`packages/core/src/extraction/extractor.ts`、`packages/core/src/storage/archiveStore.ts` |
| 项目归属、确认与状态展示 | `packages/core/src/storage/itemStore.ts`、`apps/desktop/src/main/ipc.ts`、`packages/contracts/src/ipc.ts`、现有 Sources／Inbox／Understanding 页面 |
| MCP 新旧状态、回写幂等 | `packages/core/src/storage/mcpStore.ts`、`packages/contracts/src/mcp.ts`、`apps/mcp/src/index.ts` |
| 防回归与交付 | `apps/desktop/test/review/`、现有 integration／e2e、`scripts/verify.mjs`、`REVIEW_PACKET.md`、`docs/dev-log-fixes.md` |

## 10. 交付方式与最终通过标准

按 M0、M1、M2、M3 分批交付可复核的提交，每批附：改变了什么、为什么、测试结果、尚未验证什么。不要把“稍后补测试”写成完成。

最终交付必须包含：

1. 简短设计记录：稳定身份、来源版本、自动任务执行规则、确认状态、升级迁移。
2. 全部源码与迁移，不改弱旧测试；新增回归接入默认验证，失败证据与修复后证据分别保存。
3. `verify`、端到端、打包复核结果；真实网站／模型／客户端／安装器分别标已验证或未验证。
4. 与源码对应的安装包、扩展包、版本号、哈希、截图和已知限制。
5. 更新的审核包与开发日志；一份普通用户能照做的“第一次配对→绑定项目→查看理解→编码工作回写”说明。
6. 旧版数据备份与回退说明，任何不可自动回退的迁移必须事先说明；不得以重建空数据库当作迁移。

通过标准不是新增了多少页面或测试，而是：

> 同一场对话持续进入同一个项目；新内容不丢、不重复、不越过开关；IXAEON 如实显示理解到了哪里；人工改口留得住；编码 AI 能读到可信背景并写回结果。

M0 未通过不进入 M1。M1–M3 开发完成但真人验收缺失时，只能称“候选试用版”，不能称 v0.2 已完整验收。涉及真实登录、付费或系统安装的步骤缺授权时，完成其余开发与测试，明确向用户列出待操作项。

本阶段通过后，再另行制定 Door 的第一步：先做有授权、只读、低负载的设备能力报告；RAM 压测与多设备调度另设安全边界，不纳入本计划。
