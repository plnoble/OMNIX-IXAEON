## IXAEON 析衍 v0.2.9

范围：M0 可信边界与契约加固 + M1 目标驱动桌面办事全闭环与 MCP 工具链回交。**不是 0.3.0**（正式版另发）。

### 本版新能力

- **MCP 正式办事工具链接入与回交（NP07 / Q08）**：
  - MCP 服务正式暴露并实现 `search_web`、`read_web`、`propose_task`、`get_task_status` 核心工具；
  - 工具调用执行结果经由 MCP 协议正式回交模型上下文（不再只是本地单向记录），Hermes/Agent 能依据搜索结果、网页正文和提案状态推进多轮决策；
  - 本地 HTTP 端点挂载，严格执行 Bearer 令牌鉴权与公开查询本地隐私脱敏；
  - 网关与 MCP 统一配置 `mcpBridgedTools` 防双写。
- **目标驱动桌面办事全流程闭环（NP08）**：
  - 用户在桌面对话下达业务目标 → Agent 查阅研究并调用 `propose_task` 提出代码草案卡片（包含修改范围与独立多条件验证脚本）；
  - 桌面用户审阅后一键批准（`approveAndQueue`）；
  - 执行器在独立受控工作区生成实际功能产物；
  - 独立验证脚本真实执行（覆盖正常、重复、异常多条件断言，退出码 0），验证通过后进入 `pending_accept`；
  - 用户在桌面最终验收（`accept`），任务状态转为 `completed`，实际变更文件与报告如实沉淀并在原会话闭环呈现；
  - 健全负例防护：越权未批准直接派发执行坚决被拒，已取消任务拒绝执行。

### 修复与加固（Q01–Q07 全部修复）

- **技能评测隔离（Q01）**：彻底剔除数据目录/当前工作区回退，未绑定有效独立工作区时坚决拒绝执行，杜绝数据目录污染；
- **同题前后对照（Q02）**：评测与批准阶段严格检查验证命令有效性，拒绝仅 `console.log` 的空命令；
- **TinyFish 官方契约对齐（Q03 / Q04）**：Search 对齐 `GET` + `X-API-Key`；Fetch 对齐 `POST` + `urls: [...]`，异常解析与流式响应超时贯穿，暂停即时中止云端渲染；
- **失败关联与聚类幂等（Q05）**：统一 `work_runs.id` 与 `client_ref` 基线查询，修复手动提炼后的重复聚类；
- **网关 Watchdog 真实杀进程树（Q06）**：会话挂死或探活超时触发时真实调用 `transport.kill()` 销毁底层进程；
- **数据库迁移 23（Q07）**：安全回填历史 `last_error = 'auto_discovered'` 来源为 `discovered_by = 'auto'`，历史来源身份准确恢复。

### 自动化验证与质量门禁

- 自动化测试：全量 51 个测试文件、290 项测试 100% 通过（0 失败，9 跳过）；
- 独立审核套件（`review-bbe651f-20260915.test.ts`）：14/14 全绿；
- 独立二次复审套件（`direction-round2-93ecaed.test.ts`）：12/12 全绿；
- M1 桌面闭环套件（`m1-desktop-action-loop.test.ts`）：2/2 全绿；
- 静态门禁：TypeScript `tsc --noEmit`、`eslint .`、`prettier --check` 均 0 错误、0 告警。

安装包：`IXAEON-Setup-0.2.9.exe`（122,985,478 字节，Windows 10/11 x64）  
SHA-256：`722CA60B1EF35E0F54D456CF6BAB9BAA781522D3F51231AA5579521CF131DD48`
