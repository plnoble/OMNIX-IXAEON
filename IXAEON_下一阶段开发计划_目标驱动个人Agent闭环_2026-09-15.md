# IXAEON 下一阶段开发计划：目标驱动的个人 Agent 闭环

日期：2026-09-15。制定基线：`bbe651f`。状态：**待实施的开发方案，不是已实现声明，也不是外发私人资料、消耗付费服务或正式升级的授权。**

配套依据：[本轮独立审核](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_最新代码审核_bbe651f_2026-09-15.md)。

## 1. 这次到底要往哪里走

IXAEON 不是一个让你整理卡片、给项目写交接说明的工具。它应当逐渐成为：了解你的不同想法和项目，能按当前情境取用记忆，主动研究，在授权内调动工具，做完后能说明结果、从真实失败中改进的个人 Agent。

**下一阶段不再以“增加一个提供方、一张管理页、一个开源项目名称”为主要交付。主要交付是：你在桌面对话中只说目标，它能真正办完一件事。**

最早的完整流程可以很小，但各环节必须是真的；之后马上验证多个话题、多个项目，避免把这个小例子做成产品的永久边界。

例如，你说：“帮我研究聊天导出文件怎样更好地去重、排序，看看能不能用在我的整理工具上，给我方案。”IXAEON 应自动读取获准背景，自己搜索公开资料，解释取舍；你批准后，它调用真实编码工具在受控副本修改，独立测试，再把差异、通过项和不足送回原对话。你不需要替它找所有网址、写验证命令、复制提示词或逐条确认普通记忆。

这只是验收例子，不是替用户新建项目的命令，也不规定所有项目必须为 IXAEON 服务。

## 2. 与原计划的关系：继续推进，不再重起炉灶

本计划是 [V0.3 重整总计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_重整开发计划_个人Agent内核与Hermes接入.md) 的下一段施工单，**不替代长期愿景，不降低 B0–B5 的真实行为门槛，不因增加工具提前改称 V0.4。**

继续保留以下决定：

- Core 自有资料、记忆、权限、任务、研究和经验；Hermes 是首选但可替换的发动机，锁定版本，不每日无条件追 latest。
- 不另写一套平行的通用 Agent Loop。已有降级机制如实标识，不能用降级结果冒充真实 Hermes 验收。
- 原始资料和来源证据可追溯，SQLite 管理结构化状态；Markdown 用于可读保存/导出，LanceDB 是可重建、可替换的检索索引，不是必须先迁移全部数据才能开工。
- 本地读取、向模型外发、公开网络研究、编码执行、正式升级分开授权。普通记忆自动使用不等于扩大外发范围。
- 主入口是对话、目标和办事进度。项目、来源、研究、任务页作为查看与管理入口，不要求用户替 Agent 编排整条流程。
- 既有纠正链、保留历史、不采纳、搁置、手工归属与撤权语义继续保留，不能为简化界面而丢掉数据含义。

本轮审核发现的是可信边界和实际接线问题，不构成推翻整个架构的证据。若确实需要更换引擎或数据库，先记录已复现阻碍、备选、迁移代价和对验收的影响，与用户沟通后决定。

## 3. 三批交付，不要等所有模块都完成才展示产品

| 批次 | 用户能够感受到的结果 | 对应原计划 | 本批出口 |
| --- | --- | --- | --- |
| M0：修可靠性与可信边界 | 不会因一次评测误碰应用数据；停止真能阻止新调用；错误不冒充成功 | B0、B1/B3/B4 的必要修复 | Q01–Q07 修复及回归，M1 真实验证条件明确 |
| M1：从真实桌面对话办成事 | 说目标 → 自动查背景/研究 → 提案 → 批准 → 真执行 → 真验收 → 回到对话 | 补齐 B1，贯通 B3/B4 | 一条真实生产入口全链，含拒绝/取消反例 |
| M2：成为可试用的个人 Agent | 多话题不串味、多个项目有判断地联系、持续研究、方法真的用于下一次任务 | B2/B3/B4 与 B5 交付 | 质量评测、受控日用体验、交付恢复证据 |

M0、M1、M2 分批提交，每批保留证据并可单独审核。M0 的安全缺口未关掉前，不扩大真实数据和无人值守调用。M1 完成后就给用户看，不等更多管理页面或大规模模型池。

## 4. M0：先修这一轮审核发现的问题

### M0.1 评测不能拥有应用资料目录的权限（Q01）

- 删除 `dataDir` / `process.cwd()` 作为评测目录的默认回退；缺有效绑定直接拒绝。
- 主进程建立评测任务，绑定候选版本、基线运行、项目、受控副本、获准文件范围和有效期。客户端只提交请求标识，不能换 taskId 就切换到其他工作区。
- 不允许应用数据库、密钥、Hermes home、用户原项目作为评测脚本的可写目录；验证进程同样不应读取它们。不把凭证或全部宿主环境交给验证命令。
- 检查规范化绝对路径、符号链接/junction、被替换的工作区和已失效的授权。不能只在 UI 上校验。
- 编码器不能修改独立判据、验收答案和权威评测结果。测试所需输入可复制到专用目录，结果由可信控制器收集。
- 明确当前隔离机制的能力与限制；不能把一个 Node permission flag 说成可安全运行任意恶意代码的完整系统沙箱。

验收：无任务绑定、跨项目、过期、路径替换都拒绝；数据目录读写失败；授权副本中的合法产物可以生成。保留现有“写出 cwd 被拒绝”的正向对照，不能靠全面禁用评测来过关。

### M0.2 评测必须证明同一件事真的改善了（Q02）

- 由 Agent 从失败提出候选方法和可检验目标，不让用户填 shell 命令。批准卡展示“原来哪里失败、准备怎样改、怎样判断有效”。
- 测试用例与客观判据先固定；旧方法和候选方法必须跑同一任务、输入与验收版本，输出可比。若环境无法复现，标记“基线不可复现”，不能拿历史非零码充当当前对照。
- 绑定 runId、来源运行主键、候选版本/方法哈希、用例与判据版本、输入/环境、产物和执行记录。不能由调用方填写 `passed`、退出码或“有收益”来批准。
- 确认候选方法实际参与本次执行；单纯把方法文字存进证据不够。只打印文字、换题、没检查产物、改验收答案、未运行候选、候选变更后复用旧成绩，全部不得通过。
- 建立适用范围、回退版本和撤销能力。批准一个方法不等于给它全局权限，也不等于批准应用自升级。

验收：一组受控缺陷必须在固定检查下失败；修好后在相同检查下通过；无关的成功命令不能提升状态。真实方法改进与下次使用放在 M2 验收，M0 先把可信证据机制做好。

### M0.3 修正已选网络入口，别继续堆提供方（Q03、Q04）

- TinyFish 按本轮审核所链接的官方 Search/Fetch 契约重做适配。开发时再次核对版本；如使用私有网关，必须有其明确契约，不能混称官方直连。
- 合成协议用例依据外部契约构造，不照抄实现的假设。覆盖请求方法、认证头、参数、`results[]`、逐 URL `errors[]`、HTTP 200 中的业务失败、空正文、401/429、网络失败。
- 搜索、静态读取、可选云渲染分清。动态正文没拿到可保留部分结果，但说明缺口，不把 SPA 骨架标为已研读，也不吞掉配额错误。
- 每一次外部调用前重新检查本轮身份、暂停/撤权、预算；静态请求晚到后不能继续新发渲染请求。取消信号贯穿请求与正文读取。
- 保留总计划默认：每请求 20 秒（包括正文）、2 MiB 流式限额、3 次重定向。默认每轮最多 3 次查询、10 页、8 次模型调用、5 分钟；重试同样扣预算。更改应说明实际依据。
- 渲染单独授权和计数，不因配置 Key 自动开启。建议首次授权时给出“每轮最多 2 次渲染”的保守选项；这是待用户选择的产品默认值，不是本计划授予额度。
- 统一记录搜索、渲染、模型的实际次数、可得用量和费用；无法计算金额时明确只限次数/用量，不虚构精确成本，不静默切到其他付费提供方。
- 研究批准的是方向、外发范围、公开网站规则、预算和频率；该范围内不逐 URL 请用户批准。私网、重定向、受保护页面、含私人令牌的 URL 等边界继续约束。

验收：官方形状响应可读；逐 URL 错误不变成功；正文超时/超限被中止；暂停后的晚响应不产生新云端请求；预算耗尽没有额外重试。先用合成数据验协议；一次真实公开检索/读取在 M1 使用获准账号完成。

### M0.4 把失败、候选和运行记录真正关联起来（Q05）

- work_runs 主键作为明确关联，client_ref 仅保留业务幂等语义；一对多用关系表或经过验证的结构，不能以逗号拼接混装两种标识。
- 迁移兼容旧单 id、client_ref 和聚类来源；无法解析的单独标记，不伪造基线。
- 自动聚类、从失败卡提案、受控评测、批准复用同一关系与幂等规则。真实失败卡必须传递其来源记录。
- 聚类只是发现可能重复的问题；记录分组依据、次数和原始失败。单次失败可作为单例建议，但不能展示为“反复出现”；模板文字不叫已完成的自动反思。

验收：双失败聚类可找到两条基线，已有候选不重复生成，跨项目不合并，重启幂等；两种 UI 入口都能走到有效评测。

### M0.5 超时收尾和旧库迁移（Q06、Q07）

- 引擎正常结束、异常、主动取消、watchdog 和应用退出走统一终态清理；先终止/释放本轮拥有的资源，再丢弃 resident 引用。不得杀其他用户进程。
- 用本地合成、可控的进程及子进程证明“停止了”，不只检查状态字符串或 kill spy。保留正常长任务的合理等待与活动判据，不能一律 60 秒无文本就认定失败。
- 截至本轮最新迁移为 22，修复使用 23 或接手时的下一版本，禁止修改已发布迁移。
- 对有可靠旧标记/审计依据的自动来源修正身份；无法还原的保守标记或不扩大权限，不能默认变成“用户亲自指定”。
- 测 21→最新、22→最新、重复启动；既有用户来源不被改坏，不删历史。真实旧库副本验证放在 M2，不操作唯一日用库。

### M0 交付要求

本轮独立反例 T01–T12 转绿，两个对照保留。先前独立审核 59 项及本轮基础回归无新增失败；类型、Lint、格式检查通过。将新确定性回归接入默认 verify，修复结果另存，保留 `checked` 原件。

允许因接口重构调整测试接线，但要说明等价的用户行为/安全断言在哪里保留。不能删反例、修改成功标准或用禁掉整个功能凑绿。

## 5. M1：从桌面对话完整办成一件事

### M1.1 接通真正给引擎返回结果的工具链（Q08）

按现有 ADR-11 继续 MCP 正式工具通道，不向 TUI gateway 虚构响应协议。补齐当前目标所需的最小集合：读获准 Core 背景、搜索、读网页、记录观察、拟定任务、查询任务状态与结果。能复用现有工具就复用，不为工具名另造一套存储和服务。

要求：

- 实际引擎收到工具结果并决定下一步；在 `tool.start` 通知中做本地动作、记日志，不等于结果已回交。
- MCP 在真正执行处验证 Core 颁发的本轮身份与能力范围，包括来源/受众、权限版本、预算、生命周期和幂等。不能相信模型传来的 runId 或 projectId 就获得权限。
- 内部 Hermes 工具会话与外部 Codex/其他 MCP 客户端区分授权；给内部 Agent 用的私人记忆不能自动变成所有编码客户端可读取的数据。
- 外部副作用的预算预留和幂等检查要能抵抗并发；拒绝旧会话、伪造运行、撤权后调用和重放。
- 不能在 MCP 与本地通知路径各执行一次；通知是进度，不重复发起搜索/记忆写入/任务。
- 账本在运行前建立，执行中持续保存；异常/重启如实恢复，未知状态先对账，不盲目重派。

如果锁定引擎无法完成，先提交最小协议阻碍证据与一个备选适配方案；不无限打补丁，也不未经讨论换整个平台。

### M1.2 一个主入口、少量关键决定

复用当前桌面对话入口。用户说目标后，显示当前在理解、搜索、研读、等待批准、执行还是验证，可展开查看来源与事件。

Agent 自己形成批准卡：目标、项目/非项目范围、拟改文件、理由、允许外发的背景摘要、选用工具/服务、预算、成功条件。用户可以说“只做第一项”“不要动原项目”“暂停”。修改范围后旧批准失效。

批准后程序直接调真实 CLI/适配器，不让用户复制提示词或手工编排命令。代码任务默认单并发、15 分钟上限可调整，依总计划控制修复重试；首次批准不等于无限重试。

结果返回原对话：做了什么、实际 diff、独立检查、未完成项、是否采用与是否部署。查询页同时可见，重进页面和重启可查。不能只有一张写着“完成”的任务卡。

### M1.3 真实场景及独立验收

采用用户选定/已有获准项目的受控副本；或先用明确标注的合成小项目演示，不冒称真实私人项目。第一条场景应有实际功能变化，如导出记录去重排序，不只写一句 note.txt。

该场景至少包含：

1. 从桌面对话发起，不预填网址和编码脚本。
2. 真 Hermes 经正式工具通道取得 Core 背景、真实公开搜索和正文，基于证据提出“做/不做”的理由；不是测试程序替模型串好调用。
3. 用户批准具体修改后，真实 Codex 在受控副本工作，留下实际运行标识、版本、耗时及脱敏日志；仅 CLI 退出码 0 不等于完成。
4. 固定独立判据至少覆盖正常输入、边界输入、错误输入三类；至少一个已知缺陷能被判失败。不能靠文件中出现 `valid`、`passed` 或“完成”几个字验收。
5. 成功、失败都回到原对话。用户接受产物与部署批准分开；本批不替换日用应用。
6. 另跑拒绝批准、批准后改范围、执行中取消、暂停后的迟到回报、重启对账反例，证明没有越权/重复执行。

合成自动化测试继续保留，真实验收另存。没有账号/预算时标记“待真实验证”，继续非依赖工作；不能把 Fake 结果改名“真实闭环”。

## 6. M2：多话题日用与真正可复用的经验

### M2.1 自动记忆不是让用户当资料管理员

- 把聊天理解分为：可追溯原文、场景内的临时事项、可跨会话使用的稳定背景。重复提及是线索，不自动等于永久人生目标。
- 普通背景可自动保存和按情境使用，保留 AI 推断属性、来源、时间和适用范围，允许一句话纠正。不把未确认普通观察全塞进“需要你拍板”。
- 拍板区只放会影响行动的真实冲突、难以确定的高影响决定、权限与升级等重要事项。一次 PPT 排版、某次会议安排应留在其场景，不能成为全局约束。
- 原有 `needs_reasons` 与确认语义不能无脑清空。将“资料状态”与“需要用户马上处理”分清，必要时新增派生规则/视图及迁移，不抹去人工待处理标记。
- 对话入口、提取器、ContextSelector、Inbox 和重启后的状态一并验证，不能只修改最后一句提示文案。
- 无关问题不强加个人记忆；否定、纠正、撤权、时间过期和临时要求边界在连续会话及新会话均有效。撤权后的引擎缓存也必须失效。

验收示例：“某次 PPT 马来西亚放中间”在该 PPT 内能回忆，问买手机/讨论另一个项目时不侵入；明确的长期偏好在相关新会话能使用，不要求用户逐条确认。对删除/不再记忆/取消外发分别说明效果，不能将它们混成一件事。

### M2.2 三个项目，既会联系也会说不相关

选择三个获准项目和混合话题，分别验证：

- 两个项目确实可复用一项能力，Agent 提出具体关系、来源和潜在代价，而不是泛泛说“可以联动”。
- 第三个项目没有这层关系，Agent 不强行拉通；个人兴趣也不必都围绕 IXAEON。
- 用户否决某个关联或修改目标后，相关建议和上下文随之更新，重启不复活。

本地项目索引与云端代码/聊天外发是两件事；只读本地也可先形成目录清单。模型使用的内容必须属于已授权范围，导入一个目录不等于给所有服务开放整个目录。

### M2.3 真正持续研究，而不是网页收藏页

- 用户启用一个方向时一次说明公开主题、服务、预算、频率、通知与是否允许实验；默认每天最多一轮。后续在范围内自己发现查询与公开来源，不要求用户每轮给网址。
- 从关注方向或重复失败发现值得研究的问题，研读变化、判断与用户/项目的关系，给出“值得行动/继续观察/不适用”的证据和不确定性。
- 没有实质变化时安静记录，不刷屏。新领域、新私人数据外发、新费用或扩大代码范围再请示。
- 新发现可形成研究/改进提案，但不得自动获准编码或升级。当前“自主性”先做到自己找问题和方案，不是自己给自己授权。
- 验证定时触发、关闭/恢复、重启、离线/超额、重复结果去重、无变化不通知。应用没运行时若不能研究，直说“需应用保持运行”，不要冒称已有全天后台服务。

### M2.4 一次真方法改进，再做一次验证复用

选一项已有可重复失败，使用 M0 的可信评测机制：固定案例 → 候选方法实际运行 → 对照改善且旧能力不退步 → 用户批准适用范围 → 后续同类新任务确实使用该版本 → 记录结果。

后续任务必须能查出选择的 Skill 版本/方法哈希与执行证据；仅 `approvedForProject()` 查到一条记录不算使用。加一个不适用任务验证不会强行注入；撤销后新任务不再使用，正在运行的行为按权限/策略如实处理。

这是方法层面的受控成长，不称为模型权重自训练，也不称为整个软件已经可以自我发布。正式代码升级仍在下一接力阶段。

### M2.5 数据覆盖、质量与交付

- **四平台导入继续保留。** 按 2026-09-13 用户选择，Gemini/Grok/Claude 真实样本暂缓，不反复催要；保留公开格式适配、合成契约测试和“真实样本待验证”的标识。不可宣布已同步所有账号全部历史。
- ChatGPT 已有导入/可见增量路径，补新增、编辑、重生成、暂停/恢复、重复导入与撤权流程的证据。采集范围和浏览器条件说清楚；不要求每次手工转发，也不声称后台读到了从未授权的聊天。
- 沿用总计划 §9.1：至少 60 个平衡场景，开发集与独立验收集区分，生产 ContextSelector/提取链实际参与；真实模型独立三轮，保留每轮原始回答和失败。相关事实正确率 ≥90%、无关不侵入 ≥95%、临时不升长期约束 ≥95%，分别报告每类和每轮，不挑最好成绩。权限拒绝、纠正链、证据有效性全部通过。
- 评分争议可修订规则，但须保留原判定、修订理由与人工抽审；不能在失败后改问句、改阈值或丢弃难例而宣称进步。确有坏题时给出审计记录，重新跑完整适用集合。
- ≥10,000 条合成混合消息报告索引/查询/重建、P50/P95、内存/磁盘和 UI 影响；模型耗时与检索耗时分开。LanceDB 是否投入由召回、成本和重建实测决定，不因技术名听起来合适就迁移。
- 使用获准非空真实旧库的**副本**升级到最新、幂等、数据与引用保全；记录源库升级前后不变。备份/恢复、引擎停用后 Core 仍可读/导出、安装器实际安装/升级与卸载保留数据等按 B5 做隔离验证。
- 打包绑定 commit、配置、迁移版本、哈希、实测记录。安装包生成不等于真实安装通过，更不等于用户批准覆盖日用版本。

## 7. 验收索引：交付时必须逐项填证据

下列 NP 编号属于本计划，避免与历史 T/R/S 编号混淆。默认自动化不能消耗真实账号额度；真实部分单独开关、最小授权与额度。

| 编号 | 必须证明的行为 | 证据要求 |
| --- | --- | --- |
| NP01 | 评测目录、身份和批准正确绑定 | 真实 IPC + 受控本地进程；错误绑定拒绝、合法副本通过 |
| NP02 | 无关命令不能证明改善 | 固定对照任务、方法实际执行、无关命令/改题/改答案反例 |
| NP03 | 已选网络 API 契约与错误处理正确 | 官方形状协议测试；获准后一次真实公开调用 |
| NP04 | 暂停/撤权/限额在新外部副作用前生效 | 请求在途、晚响应、重试、正文超时/超限测试与调用计数 |
| NP05 | 失败关联、去重与来源迁移正确 | 双失败/旧候选/跨项目；21 与 22 起点迁移到最新 |
| NP06 | 引擎停止真正释放本轮进程资源 | 本地合成进程树，正常/超时/取消/异常/退出；不误杀其他进程 |
| NP07 | 正式 MCP 回交与 Core 权限边界有效 | 实际路由身份/预算/重放/双执行反例 + 真实引擎调用证据 |
| NP08 | 桌面一句目标办成实际功能修改 | 真实 UI、引擎、搜索、编码、独立判据、diff、结果回对话 |
| NP09 | 自动记忆、多话题与三项目正反判断 | 真实 UI/生产检索、三轮语义评测、纠正撤权与重启 |
| NP10 | 主动研究持续运行但不烦人/越权 | 定时触发、独立选来源、无变化安静、暂停与预算耗尽 |
| NP11 | 批准的方法在下一任务真实使用 | 同案例对照、批准版本、下一任务运行记录、不适用/撤销反例 |
| NP12 | 数据与交付可恢复 | 导入增量/幂等、真实旧库副本、隔离安装升级、导出恢复、包哈希 |

NP01–NP06 是 M0 的主要出口；NP07–NP08 是 M1 的主要出口；NP09–NP12 是 M2 的主要出口。前批行为必须在后批回归，不在阶段切换时删除。

## 8. 工程落点与防止无效扩张

以下是职责范围，不要求创建更多同名服务：

| 范围 | 主要位置 | 责任 |
| --- | --- | --- |
| 真实入口与进度 | `apps/desktop/src/main/appRuntime.ts`、renderer 对话/Inbox/Tasks/Research | 复用主流程、批准与停止、错误和结果回交 |
| 工具契约与身份 | `apps/mcp/src/shared.ts`、contracts、Core 工具路由 | 真实 MCP 回交、受众、身份、授权、预算与幂等 |
| 运行生命周期 | `packages/core/src/runtime/adapter.ts`、`tuiGateway.ts` | 正式引擎接入、取消、资源清理、运行账本 |
| 编码与经验 | `packages/core/src/execution/`、`runtime/skills.ts` | 副本绑定、独立评测、失败关系、方法选择与撤销 |
| 研究 | `packages/core/src/research/`、桌面网络适配 | 契约、限额、取消、真正研读和主动发现 |
| 记忆与数据 | 生产提取/ContextSelector、storage、db/migrations | 自动记忆、情境隔离、可追溯关系、兼容迁移 |

每批开发者检查当时 Git 状态，保留别人未提交改动，不修改历史失败证据。不要在本批先开发：全功能 Platform Manager、第四/第五个同类搜索入口、大规模分布式推理、全套自主权重训练或另一套管理后台。

这不是放弃扩展：如某项工具直接解除 NP08 的真实阻碍，可以最小接入并说明证据；没有阻碍证据，就先把现有通路用起来。

## 9. 长期能力的接力位置，不能悄悄删除

| 能力 | 当前处理 | 下一接力条件 |
| --- | --- | --- |
| 正式受控自我升级 | 本阶段做到候选、真实改动、独立评测、批准方法；不自改生产 | M1/M2 通过后，单独实现候选包→回归→用户批准→切换→健康检查→回滚 |
| 更广聊天/文件持续接入 | 保留四平台入口、先把现有 ChatGPT 增量跑实 | 按用户实际使用逐个补真实样本与合规采集路径，不承诺无授权读全账号 |
| 长期在线与研究 | 应用存活时的调度先真实可靠 | 独立后台服务/消息入口再处理断线、设备休眠、升级与通知；不能假装当前已 24 小时在线 |
| Door 设备能力感知 | 保留原模块设计，不抢当前主线 | 主端任务/授权/结果协议稳定后，先接一台真实子设备，报告资源与可承担任务 |
| 云/本地/手机/NAS 模型池 | 当前先保存可替换模型政策、预算、能力边界 | Door 注册与评测基础可用后，按质量/速度/隐私路由；不预设所有设备适合分布式推理 |
| 自主发现不足与改进 | M2 先做可追溯失败→研究→方法候选 | 有稳定评测和升级回退后再提高自治额度，由用户控制范围与费用 |

## 10. 每批交付格式与开工指令

交付必须包含：

1. commit、工作区差异、迁移版本、改变的实际行为。
2. 本计划 NP 编号与审核 Q 编号对应的源码、测试和结果路径。
3. 分开填写：已实现 / 自动化通过 / 真实环境通过 / 用户接受 / 未完成。用户没有亲自验收就留空或写未验收。
4. 真实外部运行附获准范围、实际服务/模型/版本、运行 ID、来源与工具事件、费用/次数、产物和失败；凭证、完整私人聊天不得写进报告。
5. 修复前 `checked` 原件不动；修复后结果另存。截图和失败上下文独立目录保存，不被复跑清理。
6. 更新 REVIEW_PACKET、开发日志与验收映射；有重大取舍才追加 ADR，并标“提案/已批准/已验证”，不能混为一谈。

开工顺序：先读 AGENTS 和本轮审核，检查当前提交/未提交变化，复现 Q01–Q07，再完成 M0；随后立即做 M1 的真实桌面路径，最后做 M2 的广度与可靠性。不要再写一轮只有类和页面的“重整完成”。

需要真实权限时一次列出最小事项：可使用的模型/搜索或渲染服务及上限、可用的合成或脱敏语料、目标项目受控副本范围、编码执行预算、是否允许隔离安装试验。无需让用户在聊天中粘贴 Key；优先使用既有安全设置。未获准的真实动作不做，其余隔离开发继续。

**本阶段真正的结束语应当是：IXAEON 能在真实入口理解一个目标并办成事，能在不同话题间用对记忆，能证明一个方法的改进；而不是“又集成了几个项目，所有单元测试都绿了”。**
