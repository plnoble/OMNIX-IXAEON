# IXAEON 三周任务单

日期：2026-09-17。**这是当前唯一在用的计划文档。**

其余规划与审核文档已移入 `docs/history/`，作为历史事实保留，不再决定开发顺序。

## 为什么改方向

对照 2026-09-16 总计划自查（REVIEW_PACKET §69）与本轮独立核查，结论是：

- 开发循环被审核循环取代。9-13 之后几乎所有提交都是「修审核发现 + 加一个回归套件」，产品面不动。
- 9-16 一天之内「完成」了当天才写出的 P2–P6 五个阶段。核查证实其中多为空壳：
  - `selectModelResource()` 全仓只出现 1 次——它自己的定义，无调用方；
  - `ModelPool` 的上下文/价格/延迟为硬编码猜测值，而计划明文要求实测；
  - `RoleCoordinator` 为 124 行内存 switch，重启归零，仅一处使用。
- 根因是验收信号错了：验收「门禁全绿」，就只会生产测试。

**新的验收信号（唯一）：**

> 打开 IXAEON，连着聊三轮，它记得住，并且至少有一件事是它替你做完的。

## 四个已定的设计决定

1. **对话是权威记录。** 消息表是真相，`ask_session` 来源从对话派生，不再每问一次建一条来源。
2. **重启后对话在库里，引擎会话是新的。** 重开旧对话时把最近几轮消息重新喂给引擎；它「记得」的就是你看得见的那些消息，不假装没断过。
3. **待办是薄的一层。** 只存「叫什么、什么状态、从哪次对话来」，底下指向已有的 coding_tasks / research 任务。不新造任务系统。
4. **语义检索不引入 LanceDB。** 向量存 SQLite BLOB，检索暴力扫余弦。1 万条约 50ms，10 万条内够用。向量库是百万级以上的工具，当前规模引入只增加对账负担。**LanceDB 欠项就此结清：规模不到，不需要。**

## 任务单

大小口径：小 ≤100 行改动，中 ≤300 行。**超过 300 行必须拆。**

### 第 0 周 · 清理（可与第 1 周并行）

| # | 干什么 | 谁 | 验收条件 |
| --- | --- | --- | --- |
| C1 ✅ | 删掉 ModelPool 和 RoleCoordinator | 委派（Grok，已并 2026-09-17） | 两文件不存在；`index.ts` 不再导出；`selectModelResource` 从 appRuntime 删除；skill 批准处改为直接权限判断，不为留类而留 124 行；typecheck 通过 |
| C2 ✅ | Door 降级为设计稿 | 委派（Grok，已并 2026-09-17） | 代码移出主干；**迁移 24 三张表原样保留**（迁移只追加不改写），旁加注释说明当前无代码使用；设置页 Door 面板与 `/api/door/*` 路由移除 |
| C3 | 测试套件合并 | 我分类 + 委派执行 | 门禁 32 道降到 ≤12 道；每个删除的套件在清单中写明理由；保留套件全绿；**e2e 欠账一并处理**：`smoke.spec.ts` 4 项与 `screenshots.spec.ts` 1 项在本轮开始前的 d2ef87f 上就已失败（如 `state-card` 于 9-09 已从源码删除，测试未跟进），因 e2e 不在默认门禁而长期未被发现——逐项修复或删除，并决定 e2e 是否进门禁 |
| C4 ✅ | 文档归档 | 我 | 历史规划/审核文档移入 `docs/history/`（不删不改）；README 与 AGENTS.md 指向本文件 |

### 第 1 周 · 对话骨架

| # | 干什么 | 谁 | 验收条件 |
| --- | --- | --- | --- |
| D1 ✅ | 迁移 26：conversations + messages | **我** | 只追加；非空旧库升级后原数据完好 |
| D2 ✅ | ConversationStore 增删改查 | **我** | 重启后对话与消息都在；多对话并存互不串；删对话时消息级联 |
| D3 ✅ | 提问带最近 N 轮上下文 | **我** | 第二问能接住第一问；新对话拿不到别的对话内容 |
| D4 ✅ | 引擎会话按对话隔离 | **我** | 两对话交替提问不串台；重开旧对话能续 |
| D5 ✅ | ask_session 来源改为从对话派生 | 委派（Grok，整合方修并发缺陷后并入） | 对话内容仍进提取管线；不再每问一次建一条来源 |
| D6 ✅ | 聊天界面重写 | 委派（Grok，整合方修订后并入） | 左侧对话列表可切换；消息按时间流排列，上一轮不消失；引用仍可展开；重启后打开还在 |

### 第 2 周 · 流式 + 待办

| # | 干什么 | 谁 | 验收条件 |
| --- | --- | --- | --- |
| S1 ✅ | Hermes `message.delta` → 主进程事件通道 | 委派（Grok，整合方修订后并入 2026-09-18；[S1S2](委派/S1S2-回答逐字显示.md)） | 主进程按 conversationId + messageId 收发逐段文本；取消真的中断 |
| S2 ✅ | IPC 流式订阅与渲染 | 委派（同上） | 字逐段出现；中途取消能停；失败显示 failed 而非空白 |
| T1 | 迁移 29：todos 表（27 = 向量表，28 = jobs.note） | **我** | 只追加 |
| T2 | 从对话生成待办 | 我设计 + 委派实现 | Agent 提议进待办；可接受/拒绝/手动加；拒绝过的不重提；现有编码任务批准卡接入；**批准状态以任务表为准，不读消息里的快照**（D6 复核发现：卡片状态是提问时的快照，批准后重开对话仍显示「批准并排队」。后端已加状态守卫，再点只会报错、不再覆盖工作区，但界面仍需改为读实时状态） |
| T3 | 待办列表界面 | 委派 | 看得到「正在做 / 等你拍板 / 已完成」；点一条跳回来源对话 |

### 第 3 周 · 语义检索

| # | 干什么 | 谁 | 验收条件 |
| --- | --- | --- | --- |
| R1 ✅ | 本机向量模型接入 | **我**（因首次真机反馈提前，2026-09-17） | 完全离线；不静默下载模型；中文可出向量 |
| R2 ✅ | 索引构建与增量 | 委派（余项由 Grok 完成，整合方修订后并入 2026-09-18；[R2](委派/R2-语义索引状态与重建.md)） | 新资料自动补向量；可重建；重建期间检索降级关键词仍可用 |
| R3 ✅ | 关键词 + 语义融合 | **我**（同上） | `evalScenarios.ts` 中「健身 vs 马拉松」无词法重叠场景能召回；原关键词可召回项不许变差 |

完成情况（2026-09-18）：

- **R1**：本机 Ollama + `qwen3-embedding:0.6b`（用户选定并批准下载，1024 维）。向量服务地址只允许本机，记忆文本不经此离开电脑。模型不在或服务没开时不下载、不报错打断，整轮退回关键词选材，聊天里如实说明。迁移 27 `item_embeddings`，向量存 SQLite BLOB，原文变了自动作废重算。环境变量 `IXAEON_EMBED_MODEL` 可换模型，设为 `none` 关闭。
- **R3**：60 个记忆评测场景用真实模型全过，10 道无关题一条记忆都不选（`hybrid-memory-eval.test.ts`，阈值校准过程写在 `SEMANTIC_THRESHOLDS` 注释里）。在用户真实资料的**副本**上对比：「正式系统名是什么？」从误注入 6 条开业资料变为 0 条；「我最近在忙什么？」「我现在手上有哪些事？」从 0 条变为 8 条目标与待办（新增「概览问题」兜底，判定从严，「什么是目标检测算法」不触发）。
- **R2 已做**：应用启动时与每次提问前在后台补向量；没补到的条目按关键词规则判断，召回不变差。**R2 未做**：提取完成后立即补（现在要等下一次提问）；设置页显示覆盖情况与「重建」入口。
- **日常使用前提**：Ollama 要在后台运行（建议开机自启）。没开时聊天照常，只是退回关键词选材。

### 贯穿 · 用户

| # | 干什么 |
| --- | --- |
| U1 | 导入真实 ChatGPT 导出 |
| U2 | 每天用，记三样：**它记错了什么 / 哪儿啰嗦 / 哪一步你还在自己动手** |

U2 的三行字就是下一轮开发计划。不再另写规划文档。

## 第一次真实使用反馈（U2，2026-09-17）

| 反馈 | 查实的根因 | 状态 |
| --- | --- | --- |
| 第一问答非所问 | IXAEON 记忆工具从未接到 Hermes（`mcp_servers` 为空，模型 4 次找工具都落空）；预注入记忆靠关键词，「正式」误配「正式开业」 | 语义检索已上线 ✅（R1/R3，副本上误注入 6 条 → 0 条）；记忆桥见 **F1**；示例问题已换 |
| **（排查中发现）每轮都让模型调一个不存在的工具** | 派发内容无条件附带「请调用 record_observation」，而记忆桥没接时 Hermes 没有这个工具 | 已改为只在记忆桥接上时附带 ✅（F1 接上后置 `memoryBridge: true`） |
| 第二问等了约 2 分钟 | 上游网关当时卡顿（60 秒无输出、同时段 502）+ 3 轮空转工具调用 + 每轮 2 万多 token | 空转随工具钉定消失；网关稳定性属用户侧 |
| 换了模型没生效 | 聊天走 Hermes，用 Hermes 自己 config.yaml 的模型；IXAEON 设置只管后台分析与兜底 | 已修 ✅：设置页新增「聊天模型」，经 `HERMES_MODEL` 在启动网关时传入（优先于 config.yaml 且不写回），换模型即重开网关进程 |
| 换模型要重填 Key | 「获取可用模型」强制当场输入 Key | 已修：同一地址复用已保存 Key，地址变了拒绝 ✅ |
| 点归档后黑屏 | 未复现；归档请求没到后端。应用原本没有任何崩溃兜底 | 已加错误边界与崩溃日志 ✅；「改名」用了 Electron 不支持的 prompt，已修并加 lint 规则 ✅ |
| **（排查中发现）账本写「已拦截」的工具实际被执行** | TUI 协议无 tool.respond，Hermes 自跑原生工具；不钉工具集时聊天拿到 19 套工具（含终端、本机文件） | 已钉定 `HERMES_TUI_TOOLSETS=web,ixaeon` 并用真实解析验证；账本改为如实记录 ✅ |
| **（排查中发现）模型请求走明文 HTTP** | Hermes 网关地址为 `http://` 公网 IP，问题、注入的记忆、Key 均未加密 | 已解决 ✅：用户 9-18 启用 HTTPS 域名，Hermes 与 IXAEON 两边地址已切换（各留备份）。**遗留：原 Key 在新网关上返回 401，需用户换新 Key** |

## 第二次真实使用反馈（U2，2026-09-18）

| 反馈 | 查实的根因 | 状态 |
| --- | --- | --- |
| 回答里的「当前业务项目」不相关 | 应用启动 2 秒后就提问，向量还没补上（启动那次补向量还撞上 Ollama 未就绪，日志 06:00:37「语义索引暂不可用」→ 06:02:41 才补完 80 条），那一问整轮按关键词选材 | 已修 ✅：提问前先补齐向量（上限 30 秒），等不到才照常问并说明 |
| 7 月已结束的事被当成「核心主线」 | 80 条记忆的时间戳全是 9-10～9-13（导入分析那几天），内容却是 7 月的事；注入的记忆不带任何时间，类型写着 goal。Hermes 系统提示里有今天的日期，但两个时间对不上 | 已修 ✅：每条注入的记忆标「记于 <日期>」，段首写明今天，并说明记录日期不是事情发生的日期。**仍缺：已结束的事自动过期/归档**（见下） |
| 换聊天模型要改 Hermes 配置，太麻烦 | 同上表「换了模型没生效」 | 已修 ✅ |
| 导入资料的分析能不能不用我介入 | 本来就是自动的（`autoAnalyze` 开着，导入即排队）。真正卡住的是任务大面积失败：成功 29、失败 199、排队 20，44 条来源只有 15 条产出条目。两类原因：网关 502；模型引用的原话与资料对不上 → **一条对不上就整份作废** | 已修 ✅：对不上的丢掉、其余照常入库，丢弃如实显示；「资料」页可一键重跑所有失败的。网关 502 属用户侧 |
| 网关已启用 HTTPS | 新地址是域名；Hermes `config.yaml` 与 IXAEON 设置里原本仍是 `http://` 的 IP | 已切换 ✅（先备份）；F1 已完成 ✅。原 Key 在新网关 401，待用户换 Key |

### 下一步待办（由第二轮反馈产生）

| # | 干什么 | 验收条件 |
| --- | --- | --- |
| E1 | 已结束的事自动退场 | 记忆带「什么时候不再成立」的判断：过了日期的筹备/行程不再作为当前目标注入；概览问题优先给仍然成立的；用户可一键确认「这件事已结束」。**注意**：条目时间戳是导入分析的日期，不是事情发生的日期，判断要看内容里的日期 |

### 等待时长实测（2026-09-18，真 Hermes，同一句「用三句话说说春天」）

| 阶段 | gemini-3.7-flash-tiered | grok-4.6 |
| --- | --- | --- |
| 起进程、建会话 | 1.4 秒 | 1.3 秒 |
| **新会话组装（发现工具、查模型信息），期间无任何输出** | **9 秒** | **5 秒** |
| 模型思考（首字之前） | 6 秒 | 22 秒 |
| 正文输出 | 一次性到达 | 约 1 秒逐字 |

结论：逐字显示（S1/S2）对等待时长帮助有限，大头在「新会话组装」和「模型思考」。

| # | 干什么 | 谁 | 验收条件 |
| --- | --- | --- | --- |
| P1 | 会话预热：打开聊天页 / 新建对话时就在后台起好 Hermes 会话并完成组装 | **我**（要摸清 Hermes 的预热接口） | 新对话第一问不再有 5–9 秒的空等（实测对比）；预热失败不影响正常提问；不预热无人使用的会话（有上限） |
| P2 | 等待时显示进度：「正在准备…」「正在思考 12 秒」「正在回答」 | 我定契约后委派 | 思考事件到达即切换状态并计时；思考内容本身不显示 |

### F1：记忆桥开关（新增，用户 2026-09-17 决定）

| # | 干什么 | 谁 | 验收条件 |
| --- | --- | --- | --- |
| F1 ✅ | 把日用记忆库经 MCP 接到 Hermes，**默认关闭** | **我** | 设置页开关；关闭时 Hermes 配置里 `mcp_servers.ixaeon` 为 `enabled: false`；网关为明文 HTTP 时不允许开启并说明原因；开启后走桌面本地接口（受 IXAEON 授权约束），不直连数据库；开关同时决定 `AgentSession` 的 `memoryBridge`（是否让模型调 record_observation），`mcpBridgedTools` 名单与 Hermes 里实际注册的工具一致；改 Hermes `config.yaml` 前先备份 |

**完成情况（2026-09-18）**：设置页「记忆桥」开关，默认关闭；开之前检查 Hermes 网关是否 HTTPS。Hermes 用单独令牌进桌面端 `/api/hermes/tool`，受众 model，只有 search_memory / get_evidence / record_observation 三个工具；检索复用聊天注入的混合选材。令牌不写进 config.yaml（配置里是 `${IXAEON_HERMES_BRIDGE_TOKEN}` 占位符，由 IXAEON 启动网关时传入；关着不传，MCP 服务拒绝启动）。写 Hermes 配置用它自己的保格式写入器，先备份、写后核对。已用本机真实 Hermes 验证写配置、令牌展开、MCP 客户端登记三个工具；**真实模型调用一轮未验证**（网关 Key 401）。

**2026-09-17 设计修正（用户已定）**：MCP 层把所有客户端都当编码客户端，照原样接入 Hermes 只能看到项目资料，解决不了个人问题。改为给 Hermes 单独的服务端凭证，受众等同现有聊天注入（含个人结论、不含个人聊天原文），Codex 等拿不到该凭证。**等用户网关换 HTTPS 后再做**；在此之前先把 R1–R3 语义检索提前——首次真机「答非所问」的直接原因是关键词误匹配（「正式」撞「正式开业」）。

### 已知未通的入口

- 「任务」页技能候选的「运行受控对照验证」：原实现连续调用 `window.prompt`，在桌面应用里从未生效；现改为如实提示。需要页面内表单（任务 ID、验证命令、收益说明）——已由 Grok 按 [K1](委派/K1-技能受控对照验证表单.md) 补上表单并入 ✅（2026-09-18）。

## 委派规则（防止老毛病复发）

1. **委派出去的任务，不许它自己写验收标准。** 验收条件由用户或整合方先写死，执行方只负责让条件成立。27 套疤痕测试就是「自己写代码自己判对错」的产物。
2. **一个任务一个小改动，超过 300 行拆开。** 大块交付无法审，只能「看到全绿就过」。
3. **需要发明新概念/新接口的任务不委派。** `ModelPool` 就是规格填空的产物：字段齐全，值是编的。

## 明确不做（排队，非取消）

Door、模型池、自我升级、多设备协作、角色治理——全部停在设计稿。等日用中真的撞上「这台机器跑不动」或「这次不该发给云端」，那时才知道该做成什么样。

长期目标不变，见 `docs/history/IXAEON_长期愿景与开发路线图.md`。
