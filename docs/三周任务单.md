# IXAEON 三周任务单

日期：2026-09-17。**这是当前唯一在用的计划文档。**

其余规划与审核文档已移入 `docs/history/`，作为历史事实保留，不再决定开发顺序。

## 为什么改方向

对照 2026-09-16 总计划自查（REVIEW_PACKET §69）与本轮独立核查，结论是：

- 开发循环被审核循环取代。9-13 之后几乎所有提交都是「修审核发现 + 加一个回归套件」，产品面不动。
- 9-16 一天之内「完成」了当天才写出的 P2–P6 五个阶段。核查证实其中多为空壳：
  - `selectModelResource()` 全仓只出现 1 次——它自己的定义，无调用方；
  - `ModelPool` 的上下文/价格/延迟为硬编码猜测值，而计划明文要求实测；
  - `RoleCoordinator` 为 124 行内存 switch，重启归零，仅一处使用。
- 根因是验收信号错了：验收「门禁全绿」，就只会生产测试。

**新的验收信号（唯一）：**

> 打开 IXAEON，连着聊三轮，它记得住，并且至少有一件事是它替你做完的。

## 四个已定的设计决定

1. **对话是权威记录。** 消息表是真相，`ask_session` 来源从对话派生，不再每问一次建一条来源。
2. **重启后对话在库里，引擎会话是新的。** 重开旧对话时把最近几轮消息重新喂给引擎；它「记得」的就是你看得见的那些消息，不假装没断过。
3. **待办是薄的一层。** 只存「叫什么、什么状态、从哪次对话来」，底下指向已有的 coding_tasks / research 任务。不新造任务系统。
4. **语义检索不引入 LanceDB。** 向量存 SQLite BLOB，检索暴力扫余弦。1 万条约 50ms，10 万条内够用。向量库是百万级以上的工具，当前规模引入只增加对账负担。**LanceDB 欠项就此结清：规模不到，不需要。**

## 任务单

大小口径：小 ≤100 行改动，中 ≤300 行。**超过 300 行必须拆。**

### 第 0 周 · 清理（可与第 1 周并行）

| #     | 干什么                            | 谁                            | 验收条件                                                                                                                                                                                                                                                                                                    |
| ----- | --------------------------------- | ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1 ✅ | 删掉 ModelPool 和 RoleCoordinator | 委派（Grok，已并 2026-09-17） | 两文件不存在；`index.ts` 不再导出；`selectModelResource` 从 appRuntime 删除；skill 批准处改为直接权限判断，不为留类而留 124 行；typecheck 通过                                                                                                                                                              |
| C2 ✅ | Door 降级为设计稿                 | 委派（Grok，已并 2026-09-17） | 代码移出主干；**迁移 24 三张表原样保留**（迁移只追加不改写），旁加注释说明当前无代码使用；设置页 Door 面板与 `/api/door/*` 路由移除                                                                                                                                                                         |
| C3    | 测试套件合并                      | 我分类 + 委派执行             | 门禁 32 道降到 ≤12 道；每个删除的套件在清单中写明理由；保留套件全绿；**e2e 欠账一并处理**：`smoke.spec.ts` 4 项与 `screenshots.spec.ts` 1 项在本轮开始前的 d2ef87f 上就已失败（如 `state-card` 于 9-09 已从源码删除，测试未跟进），因 e2e 不在默认门禁而长期未被发现——逐项修复或删除，并决定 e2e 是否进门禁 |
| C4 ✅ | 文档归档                          | 我                            | 历史规划/审核文档移入 `docs/history/`（不删不改）；README 与 AGENTS.md 指向本文件                                                                                                                                                                                                                           |

### 第 1 周 · 对话骨架

| #     | 干什么                            | 谁                                   | 验收条件                                                                         |
| ----- | --------------------------------- | ------------------------------------ | -------------------------------------------------------------------------------- |
| D1 ✅ | 迁移 26：conversations + messages | **我**                               | 只追加；非空旧库升级后原数据完好                                                 |
| D2 ✅ | ConversationStore 增删改查        | **我**                               | 重启后对话与消息都在；多对话并存互不串；删对话时消息级联                         |
| D3 ✅ | 提问带最近 N 轮上下文             | **我**                               | 第二问能接住第一问；新对话拿不到别的对话内容                                     |
| D4 ✅ | 引擎会话按对话隔离                | **我**                               | 两对话交替提问不串台；重开旧对话能续                                             |
| D5 ✅ | ask_session 来源改为从对话派生    | 委派（Grok，整合方修并发缺陷后并入） | 对话内容仍进提取管线；不再每问一次建一条来源                                     |
| D6 ✅ | 聊天界面重写                      | 委派（Grok，整合方修订后并入）       | 左侧对话列表可切换；消息按时间流排列，上一轮不消失；引用仍可展开；重启后打开还在 |

### 第 2 周 · 流式 + 待办

| #     | 干什么                                                                                                                                             | 谁                                                                                                                                                              | 验收条件                                                                                                  |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| S1 ✅ | Hermes `message.delta` → 主进程事件通道                                                                                                            | 委派（Grok，整合方修订后并入 2026-09-18；[S1S2](委派/S1S2-回答逐字显示.md)）                                                                                    | 主进程按 conversationId + messageId 收发逐段文本；取消真的中断                                            |
| S2 ✅ | IPC 流式订阅与渲染                                                                                                                                 | 委派（同上）                                                                                                                                                    | 字逐段出现；中途取消能停；失败显示 failed 而非空白                                                        |
| T1 ✅ | 迁移 33：todos 表 + `TodoStore`（27 = 向量表，28 = jobs.note，29 = items.time_status，30 = 清聊天存档回声，31 = items.said_by，32 = app_settings） | **我**（2026-09-19）                                                                                                                                            | 只追加；状态按顺序走；拒绝过的不重提；列表带编码任务实时状态                                              |
| T2    | 从对话生成待办                                                                                                                                     | 我设计 + 委派实现：[T2a 聊天里的待办](委派/T2a-聊天里的待办.md) ✅（2026-09-19 复审并入，真机后收紧约定措辞）、[T2b 编码任务进待办](委派/T2b-编码任务进待办.md) | 锁定的验收测试（AI 在回答末尾列「建议待办」、你写「待办：」直接加；编码任务拍板即批准、状态以任务表为准） |
| T3 ✅ | 待办列表界面                                                                                                                                       | 委派（Grok，2026-09-19 并入）：[T3 待办页](委派/T3-待办页.md)                                                                                                   | 锁定的验收测试（等你拍板 / 要做 / 已完成；点一条跳回来源对话）                                            |

### 第 3 周 · 语义检索

| #     | 干什么            | 谁                                                                                         | 验收条件                                                                              |
| ----- | ----------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| R1 ✅ | 本机向量模型接入  | **我**（因首次真机反馈提前，2026-09-17）                                                   | 完全离线；不静默下载模型；中文可出向量                                                |
| R2 ✅ | 索引构建与增量    | 委派（余项由 Grok 完成，整合方修订后并入 2026-09-18；[R2](委派/R2-语义索引状态与重建.md)） | 新资料自动补向量；可重建；重建期间检索降级关键词仍可用                                |
| R3 ✅ | 关键词 + 语义融合 | **我**（同上）                                                                             | `evalScenarios.ts` 中「健身 vs 马拉松」无词法重叠场景能召回；原关键词可召回项不许变差 |

完成情况（2026-09-18）：

- **R1**：本机 Ollama + `qwen3-embedding:0.6b`（用户选定并批准下载，1024 维）。向量服务地址只允许本机，记忆文本不经此离开电脑。模型不在或服务没开时不下载、不报错打断，整轮退回关键词选材，聊天里如实说明。迁移 27 `item_embeddings`，向量存 SQLite BLOB，原文变了自动作废重算。环境变量 `IXAEON_EMBED_MODEL` 可换模型，设为 `none` 关闭。
- **R3**：60 个记忆评测场景用真实模型全过，10 道无关题一条记忆都不选（`hybrid-memory-eval.test.ts`，阈值校准过程写在 `SEMANTIC_THRESHOLDS` 注释里）。在用户真实资料的**副本**上对比：「正式系统名是什么？」从误注入 6 条开业资料变为 0 条；「我最近在忙什么？」「我现在手上有哪些事？」从 0 条变为 8 条目标与待办（新增「概览问题」兜底，判定从严，「什么是目标检测算法」不触发）。
- **R2**：应用启动时、每次提问前、每次提取成功后在后台补向量；没补到的条目按关键词规则判断，召回不变差。设置页「语义检索（本机）」显示覆盖情况与失败原因，可重建（先确认向量服务可用再删旧向量）。
- **日常使用前提**：Ollama 要在后台运行（建议开机自启）。没开时聊天照常，只是退回关键词选材。

### 贯穿 · 用户

| #   | 干什么                                                             |
| --- | ------------------------------------------------------------------ |
| U1  | 导入真实 ChatGPT 导出                                              |
| U2  | 每天用，记三样：**它记错了什么 / 哪儿啰嗦 / 哪一步你还在自己动手** |

U2 的三行字就是下一轮开发计划。不再另写规划文档。

## 第一次真实使用反馈（U2，2026-09-17）

| 反馈                                               | 查实的根因                                                                                                                | 状态                                                                                                                                                                                                                                           |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 第一问答非所问                                     | IXAEON 记忆工具从未接到 Hermes（`mcp_servers` 为空，模型 4 次找工具都落空）；预注入记忆靠关键词，「正式」误配「正式开业」 | 语义检索已上线 ✅（R1/R3，副本上误注入 6 条 → 0 条）；记忆桥见 **F1**；示例问题已换                                                                                                                                                            |
| **（排查中发现）每轮都让模型调一个不存在的工具**   | 派发内容无条件附带「请调用 record_observation」，而记忆桥没接时 Hermes 没有这个工具                                       | 已改为只在记忆桥接上时附带 ✅（F1 接上后置 `memoryBridge: true`）                                                                                                                                                                              |
| 第二问等了约 2 分钟                                | 上游网关当时卡顿（60 秒无输出、同时段 502）+ 3 轮空转工具调用 + 每轮 2 万多 token                                         | 空转随工具钉定消失；网关稳定性属用户侧                                                                                                                                                                                                         |
| 换了模型没生效                                     | 聊天走 Hermes，用 Hermes 自己 config.yaml 的模型；IXAEON 设置只管后台分析与兜底                                           | 已修 ✅：设置页新增「聊天模型」，经 `HERMES_MODEL` 在启动网关时传入（优先于 config.yaml 且不写回），换模型即重开网关进程                                                                                                                       |
| 换模型要重填 Key                                   | 「获取可用模型」强制当场输入 Key                                                                                          | 已修：同一地址复用已保存 Key，地址变了拒绝 ✅                                                                                                                                                                                                  |
| 点归档后黑屏                                       | 未复现；归档请求没到后端。应用原本没有任何崩溃兜底                                                                        | 已加错误边界与崩溃日志 ✅；「改名」用了 Electron 不支持的 prompt，已修并加 lint 规则 ✅                                                                                                                                                        |
| **（排查中发现）账本写「已拦截」的工具实际被执行** | TUI 协议无 tool.respond，Hermes 自跑原生工具；不钉工具集时聊天拿到 19 套工具（含终端、本机文件）                          | 已钉定 `HERMES_TUI_TOOLSETS=web,ixaeon` 并用真实解析验证；账本改为如实记录 ✅                                                                                                                                                                  |
| **（排查中发现）模型请求走明文 HTTP**              | Hermes 网关地址为 `http://` 公网 IP，问题、注入的记忆、Key 均未加密                                                       | 已解决 ✅：用户 9-18 启用 HTTPS 域名，Hermes 与 IXAEON 两边地址已切换（各留备份）。（更正：此前据 config.yaml 里 `model.api_key` 查模型列表返回 401，误判「Key 在新网关不可用」；实测 Hermes 聊天正常，它实际用的 Key 不在该字段。无需换 Key） |

## 第三次真实使用反馈（2026-09-20，用户第一次用上 T2a/T2b/T3/P3/W1a 之后）

用户重启应用（迁移 28→33，自动备份 `pre-migration-28-33-20260920-084941`）、聊了几轮、点了待办和研究。查他真实库得到的事实：

1. **记忆其实没建起来**：47 份资料里 **31 份从没产出过任何记忆**，现有 94 条只来自 16 份。分析任务历史失败 223 次，最大一类是「模型给的依据对不上原文」（约 100 次）——模型把原文里不连续的几处拼成一条摘录，校验判无效；一条都对不上时整份作废。→ [X1](委派/X1-提炼打捞依据.md)（打捞真依据 + 重新分析）。
2. **研究页按钮莫名其妙变暗**：一次「立即检查」跑了 3 分钟，页面级 `busy` 把所有按钮（含新建关注的创建）全锁住，且没有任何「正在检查」提示。→ [U1](委派/U1-研究页忙状态.md)。
3. **发现堆着没人告诉他**：用户原话「发现了新的更新，然后呢？等着我去看？」。他在三种通知方式里只选「符合要求清单才提醒」——写下硬要求，对上了才出现。→ 迁移 34（整合方已加）+ [N1](委派/N1-要求清单只在对上时提醒.md)。
4. 顺带确认：T2a 的建议待办真机有效（一轮回答提了 3 条，用户点了「要做」）；A1 的耐心重试在工作（502 失败已排退避重试）；`memory.personal_to_chat` 已由用户打开；记忆桥（F1）也开着。
5. 还没验证的：W1a 提关注方向（模型 Key 在 Electron 加密存储里，脚本进程解不开，只能用户在应用里点）。

## 第二次真实使用反馈（U2，2026-09-18）

| 反馈                               | 查实的根因                                                                                                                                                                                                       | 状态                                                                                                                                |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 回答里的「当前业务项目」不相关     | 应用启动 2 秒后就提问，向量还没补上（启动那次补向量还撞上 Ollama 未就绪，日志 06:00:37「语义索引暂不可用」→ 06:02:41 才补完 80 条），那一问整轮按关键词选材                                                      | 已修 ✅：提问前先补齐向量（上限 30 秒），等不到才照常问并说明                                                                       |
| 7 月已结束的事被当成「核心主线」   | 80 条记忆的时间戳全是 9-10～9-13（导入分析那几天），内容却是 7 月的事；注入的记忆不带任何时间，类型写着 goal。Hermes 系统提示里有今天的日期，但两个时间对不上                                                    | 已修 ✅：每条注入的记忆标「记于 <日期>」，段首写明今天，并说明记录日期不是事情发生的日期。**仍缺：已结束的事自动过期/归档**（见下） |
| 换聊天模型要改 Hermes 配置，太麻烦 | 同上表「换了模型没生效」                                                                                                                                                                                         | 已修 ✅                                                                                                                             |
| 导入资料的分析能不能不用我介入     | 本来就是自动的（`autoAnalyze` 开着，导入即排队）。真正卡住的是任务大面积失败：成功 29、失败 199、排队 20，44 条来源只有 15 条产出条目。两类原因：网关 502；模型引用的原话与资料对不上 → **一条对不上就整份作废** | 已修 ✅：对不上的丢掉、其余照常入库，丢弃如实显示；「资料」页可一键重跑所有失败的。网关 502 属用户侧                                |
| 网关已启用 HTTPS                   | 新地址是域名；Hermes `config.yaml` 与 IXAEON 设置里原本仍是 `http://` 的 IP                                                                                                                                      | 已切换 ✅（先备份）；F1 已完成 ✅；两边在新网关上都实测可用                                                                         |

### 下一步待办（由第二轮反馈产生）

| #     | 干什么             | 验收条件                                                                                                                                                                                                            |
| ----- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1 ✅ | 已结束的事自动退场 | 记忆带「什么时候不再成立」的判断：过了日期的筹备/行程不再作为当前目标注入；概览问题优先给仍然成立的；用户可一键确认「这件事已结束」。**注意**：条目时间戳是导入分析的日期，不是事情发生的日期，判断要看内容里的日期 |

### 等待时长实测（2026-09-18，真 Hermes，同一句「用三句话说说春天」）

| 阶段                                                   | gemini-3.7-flash-tiered | grok-4.6    |
| ------------------------------------------------------ | ----------------------- | ----------- |
| 起进程、建会话                                         | 1.4 秒                  | 1.3 秒      |
| **新会话组装（发现工具、查模型信息），期间无任何输出** | **9 秒**                | **5 秒**    |
| 模型思考（首字之前）                                   | 6 秒                    | 22 秒       |
| 正文输出                                               | 一次性到达              | 约 1 秒逐字 |

结论：逐字显示（S1/S2）对等待时长帮助有限，大头在「新会话组装」和「模型思考」。

| #     | 干什么                                                              | 谁                                                                        | 验收条件                                                                                            |
| ----- | ------------------------------------------------------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| P1 ✅ | 会话预热：打开聊天页 / 新建对话时就在后台起好 Hermes 会话并完成组装 | **我**（2026-09-18 完成：预热后第一问开始调模型 0–0.9 秒，冷启动约 9 秒） | 新对话第一问不再有 5–9 秒的空等（实测对比）；预热失败不影响正常提问；不预热无人使用的会话（有上限） |
| P2    | 等待时显示进度：「正在准备…」「正在思考 12 秒」「正在回答」         | 委派（单已写：[P2](委派/P2-等待进度显示.md)）                             | 思考事件到达即切换状态并计时；思考内容本身不显示                                                    |

**E1 完成情况（2026-09-18）**：按内容里写的日期判断（时间戳是导入分析的日期，不能用）；自动做的只是「不当眼下的事」——首页目标不列、概览问题排后、注入时标「所述日期 X 已过」，不删不搁置，问到那件事时仍能查到。首页「看起来已经结束的事」逐条一键确认；整份资料里写了日期的都已过去时建议整份归档（复用现有归档）。用户资料副本上认出 14 条、无误判，整份建议 2 份。

### E2 ✅：聊天存档的「回声」

E1 核对真实数据时发现：IXAEON 自己的聊天存档（`ask_session` 来源）连同**模型的回答**一起被拿去提炼。9-17 那次「正式系统名是什么？」被错误注入了开业资料，模型在回答里复述了它们，提炼又把复述当成新结论存了回来——同一批旧内容多了 8 条副本。错的内容会越存越多，模型编的内容也会被当成用户的事。

| #     | 干什么                                                                 | 谁     | 验收条件                                                                                                                                               |
| ----- | ---------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| E2 ✅ | 聊天存档只从用户说的话里提炼，不从模型的回答里提炼；清理已经回灌的副本 | **我** | 提炼聊天存档时模型的回答不交给模型；模型硬引用回答的结论进不来；导入的聊天不受影响；已回灌的副本清掉，用户处理过的（确认、否决、改过项目、纠正过）不动 |

**完成情况（2026-09-18，用户定「聊天存档只从我说的话里提炼」）**：提炼 `ask_session` 来源时只把用户的原话交给模型。「就按你刚才那个方案」这类采纳**记不下方案内容**——要记就把内容说出来，或在记忆页手动加。迁移 30 清掉依据全部来自模型回答、没经过用户处理的 AI 条目，删了哪几条记进审计。用户数据库副本上实跑：116 条 → 92 条，删掉的正好是聊天存档里那 24 条（全部依据都是模型回答），其他来源一条没动，无残留依据或向量。

### E3 ✅ E4 ✅：导入的聊天，谁说的话才算「我的事」

用户问「导入聊天后的提炼能力是否完善」，按真实数据核对（2026-09-18，只读）：

- **管线是通的**：导入保留说话人和分支；每条结论都能点回原文，摘录必须逐字出自所引片段；部分对不上时只丢那几条；失败可一键全部重试；改过的结论重提时不被覆盖；写了日期的已过去的事自动退场（E1）。
- **覆盖还不全**：导入的 35 段聊天已分析 17 段（16 段出了结论），其余 18 段里 16 段正在排队或分析中；9-17 以来 25 次失败里 24 次是网关/网络错误（fetch failed、502、429），1 次是模型给的唯一一条依据对不上原文。
- **主要问题是说话人**：从导入聊天提炼出的 91 条结论里，**58 条（64%）依据全部来自 ChatGPT 的回答**，不是用户的话，却被记成用户的决定（20）、偏好（11）、约束（10）、目标（3）等。典型：ChatGPT 推荐的方案被记成「我的偏好」，一次排障对话里的通用建议被记成「我的约束」。这和 E2 是同一个问题，只是量更大。
- **几乎没人过目**：91 条里只有 1 条经用户确认。归到项目下的 65 条不经确认就能被选进聊天上下文，其中 33 条依据来自 ChatGPT 的回答；没归项目的 26 条要用户分享后才进。
- **附带文件被当成资料**：ChatGPT 导出包里的 8 个附带文件（账号设置、导出清单等 JSON）被当成文档导入，分析结果为空是对的，但它们不该进资料库。

| #     | 干什么                                                                                              | 谁     | 验收条件                                                                                                                                                                |
| ----- | --------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E3 ✅ | 导入的聊天按说话人区分：用户的话才能成为「我的目标 / 决定 / 偏好 / 约束」，AI 的回答记成「AI 建议」 | **我** | 记在谁名下按依据片段的说话人定，不由模型判断；AI 建议不进「待确认」、可「采纳」；聊天注入标明出处；首页不把没采纳的 AI 建议当成用户的约束或待处理的事；旧条目按依据回填 |
| E4 ✅ | 导入文件夹时跳过 ChatGPT 导出包的附带文件                                                           | **我** | 目录里有 conversations.json 或 export_manifest.json 时，账号资料、设置、导出清单等不导入；陌生 JSON 照常导入；不是导出包的目录不受影响                                  |

**完成情况（2026-09-18，用户定做法 B：「ChatGPT 或其他 AI 给的建议是有必要的，是有用的」）**：条目新增「谁说的」（迁移 31）。用户数据库副本上实跑：导入聊天的 91 条里 58 条记为 AI 建议、33 条是用户的话；待处理条目 75 → 36（含迁移 30 清掉的 24 条回声）。提炼提示词升到 v4（用户的目标/决定/偏好/约束只能来自用户的话；AI 的建议写明是 AI 的说法；用户明确采纳时另记一条用户的决定）。已导入的 8 个附带文件**没有自动处理**：归档会让它们以「过往工作档案」的名义出现在关键词检索里，不合适；在「资料」页勾选后删除即可（删的是资料记录，Vault 原文副本保留）。用户消息里粘贴的资料仍记在用户名下——分不出来，先不处理。

**遗留**：记忆桥的 `search_memory` 目前自己拼出处标签（`appRuntime.ts`），还不认「AI 建议」——已写成委派单 [A3](委派/A3-记忆桥出处标签.md)。

### E5 ✅ E6 ✅：聊天里 AI 的建议、少审核

用户 2026-09-18：

- 「让 AI 帮我想方案时，哪怕当下合适，随着时间可能有更好的方法、当时也可能想得不全，所以不想把它当成『完全确定』的概念让后面的 AI 完全遵守。」
- 「我不想每句话都去审核……每导入一份资料就要逐句审核一遍，会让我生厌。」

| #     | 干什么                                                       | 谁     | 验收条件                                                                                                                  |
| ----- | ------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------- |
| E5 ✅ | 聊天存档也提炼 AI 的建议（记在 AI 名下），复述已有记忆的不收 | **我** | 每条回答记下本轮注入的记忆；AI 的新建议进「AI 建议」；复述注入记忆的不进；没有记录的旧回答、Core 兜底回答仍只提炼用户的话 |
| E6 ✅ | 少审核：默认可用，用的时候纠正                               | **我** | 回答下面列出「用到的记忆」，不对或过时当场点掉；待讨论只放要你拍板的；个人记忆给聊天用改成一次决定                        |

**完成情况（2026-09-18）**：

- **E5**：回声主要靠提炼模型自己认——块前列出这一轮 AI 看过的记忆，写明复述、自我介绍、缺信息说明都不收（提示词 v5）。代码再兜底：与注入记忆语义相似度 ≥ 0.8 或字面重合 ≥ 0.6 的丢掉。阈值按真机数据定：24 条回声一半在 0.75 以上，字面比对只认得出 2 条（多是改写、合并过的复述）；导入里 58 条真正的 AI 建议到 0.8 的只有 2 条。聊天注入时说明：AI 的建议（含采纳过的）是当时的看法、不是定论，有更好的方案直接提。
- **E6**：待讨论页、首页只放冲突、你要求继续待处理的、编码代理报回的结果，其余只显示条数。设置页新增「个人记忆给聊天用」（迁移 32，默认关）：没归项目的记忆原本要逐条分享给模型才进聊天，而界面上根本没有逐条分享的入口——导入的个人聊天提炼得再好，聊天也用不上。打开只放宽 IXAEON 自己的聊天，编码客户端照旧拿不到。
- 用户数据库副本上实跑（迁移 29–32）：待讨论 36 → 0（没有冲突和编码结果）；打开开关后聊天可用的记忆 65 → 92。

### P2 ✅（2026-09-18，Grok 实现，整合方复核并入）

整合方修订：界面只认这一轮（按第一条进度事件的消息 id）；重开时库里还在写的一轮不显示停住的「正在准备…（0 秒）」；两条测试收紧。委派单验收 1–4 全过，verify 全过，d6 聊天界面 e2e 通过。**真机（真 Hermes）尚未点过。**

### F1：记忆桥开关（新增，用户 2026-09-17 决定）

| #     | 干什么                                       | 谁     | 验收条件                                                                                                                                                                                                                                                                                                                                              |
| ----- | -------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 ✅ | 把日用记忆库经 MCP 接到 Hermes，**默认关闭** | **我** | 设置页开关；关闭时 Hermes 配置里 `mcp_servers.ixaeon` 为 `enabled: false`；网关为明文 HTTP 时不允许开启并说明原因；开启后走桌面本地接口（受 IXAEON 授权约束），不直连数据库；开关同时决定 `AgentSession` 的 `memoryBridge`（是否让模型调 record_observation），`mcpBridgedTools` 名单与 Hermes 里实际注册的工具一致；改 Hermes `config.yaml` 前先备份 |

**完成情况（2026-09-18）**：设置页「记忆桥」开关，默认关闭；开之前检查 Hermes 网关是否 HTTPS。Hermes 用单独令牌进桌面端 `/api/hermes/tool`，受众 model，只有 search_memory / get_evidence / record_observation 三个工具；检索复用聊天注入的混合选材。令牌不写进 config.yaml（配置里是 `${IXAEON_HERMES_BRIDGE_TOKEN}` 占位符，由 IXAEON 启动网关时传入；关着不传，MCP 服务拒绝启动）。写 Hermes 配置用它自己的保格式写入器，先备份、写后核对。已用本机真实 Hermes 验证写配置、令牌展开、MCP 客户端登记三个工具；**开着记忆桥的真实聊天一轮尚未验证**（需用户在设置页开启）。

**2026-09-17 设计修正（用户已定）**：MCP 层把所有客户端都当编码客户端，照原样接入 Hermes 只能看到项目资料，解决不了个人问题。改为给 Hermes 单独的服务端凭证，受众等同现有聊天注入（含个人结论、不含个人聊天原文），Codex 等拿不到该凭证。**等用户网关换 HTTPS 后再做**；在此之前先把 R1–R3 语义检索提前——首次真机「答非所问」的直接原因是关键词误匹配（「正式」撞「正式开业」）。

### 已知未通的入口

- 「任务」页技能候选的「运行受控对照验证」：原实现连续调用 `window.prompt`，在桌面应用里从未生效；现改为如实提示。需要页面内表单（任务 ID、验证命令、收益说明）——已由 Grok 按 [K1](委派/K1-技能受控对照验证表单.md) 补上表单并入 ✅（2026-09-18）。

## 下一阶段（2026-09-18 起）：到「初步体验版」

用户 2026-09-18：「现在的半成品每次使用都让我觉得和我想要的差距很大。」

**初步体验版的标准**：上面的验收信号（连着聊三轮它记得住，并且至少替你做完一件事），加上用户自己的场景逐条跑通。场景由用户给，不由开发方猜。

### 用户的目标场景（2026-09-18）

用户：「不一定非得都实现，但我最终的目标是一个全能的贾维斯。」

| 场景               | 用户原意（概括）                                                                                                                                | 现在有什么                                                                                                                                                        | 缺什么                                                                                                                                                     |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 一：开发整合方     | 把和编码 agent（Codex、Claude、Grok 等）的聊天记录、项目文件、GitHub 给它，它理解你想开发什么、做到哪了，然后指挥 Codex / Claude 去开发，再验收 | ChatGPT / Claude / Grok / Gemini 导出导入；项目目录快照；编码代理报工作结果（MCP）；Codex 执行器 + 批准卡 + 受控验证命令；本仓库的委派流水线（锁定验收测试 + CI） | 导入 Claude Code / Codex 的会话记录；读 git 历史与 GitHub 议题；「这个项目做到哪了」的进度简报；按委派流水线自动写单、派单、验收的闭环；Claude Code 执行器 |
| 二：主动研究与提醒 | 从你的信息里知道你关心的方向，自己去找有用的项目和资料、研究、给方案；你要的东西一出现就提醒你（例如某类设备满足你列的要求时）                  | 研究主题 / 来源 / 发现 / 值得行动的线索（首页）；Hermes 有联网搜索                                                                                                | 从记忆里提出「你关心的方向」给你勾选；定时自动研究；「要求清单」匹配与提醒；研究结论记成 AI 建议（E3 规则）                                                |
| 三：自我提升       | 有更好的 agent / 记忆技术出现时，自己开发、评估、替换现有版本                                                                                   | 记忆评测场景（60 个）；Hermes 可替换的设计                                                                                                                        | 各部件的评测基准；在副本上试换、对比、再由你批准替换。**先有评测，再谈替换**                                                                               |
| 四：多设备子 agent | 手机、平板、电脑上各装一个子 agent，能看屏幕、能操作，关键时候调用                                                                              | 无（设计稿阶段暂停）                                                                                                                                              | 先做电脑上的屏幕与操作能力，再考虑手机                                                                                                                     |

| 阶段 | 目标           | 内容                                                                                                                                                                                                                                                                                                                   | 预计                    |
| ---- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 1    | 记得对、答得快 | E1–E6 ✅、P1–P2 ✅；A1–A3 ✅（Grok 按委派流水线完成）                                                                                                                                                                                                                                                                  | 9-22 前                 |
| 2    | 替你做事       | T1 ✅、T2a ✅、T3 ✅；场景一第一步 [S1](委派/S1-导入ClaudeCode会话.md) ✅（Grok）、[S2](委派/S2-导入Codex会话.md) ✅（整合方）导入编码代理会话；T2b ✅、[P3 项目进展简报](委派/P3-项目进展简报.md) ✅、[S3a 会话选择导入（核心）](委派/S3a-编码代理会话选择导入（核心）.md) ✅（2026-09-20 复审并入，界面 S3b 在队列） | 9-29 前                 |
| 3    | 初步体验版     | 场景一最小闭环：你说「把 X 做了」→ 它写委派单和验收测试 → 派给 Codex → 跑验收 → 回报；场景二最小版：从记忆提出关注方向、你勾选、每天自动找一次、首页显示新发现（[W1a 关注方向](委派/W1a-关注方向.md) ✅ 2026-09-20 复审并入；[W1b 概览页新发现](委派/W1b-概览页新发现.md) 退回补测试）                                 | 10 月上旬               |
| 之后 | 贾维斯方向     | 场景二的要求清单提醒；场景三（先建评测基准）；场景四（先电脑）                                                                                                                                                                                                                                                         | 看阶段 3 的真实使用再排 |

| #     | 干什么           | 谁                                                     | 验收条件       |
| ----- | ---------------- | ------------------------------------------------------ | -------------- |
| A1 ✅ | 分析失败耐心重试 | Grok（整合方补：同一来源已另排新任务的旧失败不再重排） | 锁定的验收测试 |
| A2 ✅ | 迁移前自动备份   | Grok（B 档，整合方复审）                               | 锁定的验收测试 |
| A3 ✅ | 记忆桥出处标签   | Grok（整合方把说明文字改为与聊天注入共用）             | 锁定的验收测试 |

### 委派流水线 ✅（用户 2026-09-18 同意，已搭）

用户：整合方（Claude）额度用完时，Grok、Codex 额度再多也只能停下。改法是把整合方的工作**挪到前面、成批做**，把验收**交给机器**。规则写在 [AGENTS.md](../AGENTS.md)「委派流水线」，这里只记做了什么：

1. **任务队列** [`docs/委派/队列.md`](委派/队列.md)：执行方按顺序领。第一批 A1–A3。
2. **锁定的验收测试**：放在各包的 `test/acceptance/`，指纹记在 `docs/委派/锁定验收.json`，`node scripts/acceptance.mjs check` 核对（已进 verify）；已完成任务的验收测试也进 verify。排队中的任务测试本来就不过，不进日常 unit / integration 与类型检查。第一批三个验收测试都先用临时参考实现跑通过（确认做得到），参考实现已丢掉。
3. **一人一个分支**：AGENTS.md 要求每个任务 `git worktree add` 自己的工作目录。
4. **CI**：`.github/workflows/verify.yml`，Windows，每次推送跑完整 verify。2026-09-19 跑通：首次运行暴露三处与本机不同的环境问题（云端编译器认不出 VS 版本、8.3 短路径、导出样例目录不存在），其中短路径是真 bug，已修。
5. **分级合并**：A 档 CI 绿即可由用户合并；B 档等整合方复审。

诚实的边界：队列里的单做完就会停；新概念、跨模块改动、真实数据出问题时的诊断，仍要整合方。估计执行方能接走六七成的编码量。

### 委派流水线 v2（用户 2026-09-19 同意）

用户：Grok 额度不限，整合方的有限，能不能让 Grok 大量开发。v1 跑了七单（A1–A3、T3、S1、T2a，加整合方做的 S2）之后看清了瓶颈：Grok 照委派单写的代码基本是对的；整合方的额度一半花在一单一单写锁定测试（还要写参考实现验证测试），一半花在审查——而审出来的真问题 CI 一个都没抓到，大多只在真实数据、真实模型上才看得出（会话里的注入内容、知识性问题也附一串待办）。v2 的改法（规则见 [AGENTS.md](../AGENTS.md)「用户的原则」「委派流水线 v2」）：

1. **规格代替锁定测试**：整合方每件只写一页规格（要什么、为什么、约束、验收条件、真机检查），执行方把条件写成测试。B 档的测试先给整合方看过再实现。
2. **真机检查归执行方**：`scripts/real/` 里是只输出统计数字的检查脚本（试解析本机会话、真 Hermes 问合成问题），规格写明跑哪个；没跑的不能合并。
3. **A 档不经整合方**：`node scripts/codex-review.mjs <任务号>` 让 Codex（桌面版自带的命令行，只读沙箱）对照规格审，结论「可以合并」且 CI 绿，执行方自己合并；Codex 额度用完就等着，不许跳过。
4. **交付说明写进仓库**（`docs/委派/交付/`），不靠用户转话。
5. **用户的原则写进 AGENTS.md**：不想逐条审核、AI 建议不是定论、隐私优先、如实报告——规格没写到的小决定照这个来。

第一批 v2 任务（2026-09-19 进队列）：R1 恢复失败后重建聊天、R2 CI 动作升级、W1b 概览页新发现（A）；S3a/S3b 编码代理会话选择导入、P3 项目进展简报、W1a 关注方向（B，S3b 为 A）。

整合方留下的：定方向、写规格、复审 B 档、每周抽查一次已合并的 A 档并完整用一遍。Codex 审查脚本写好时 Codex 额度正好用完，只试到「找到命令行、登录与配置正常、开始审查」，完整跑通要等额度恢复后的第一单。

## 委派规则（防止老毛病复发）

1. **委派出去的任务，不许它自己写验收标准。** 验收条件由用户或整合方先写死，执行方只负责让条件成立。27 套疤痕测试就是「自己写代码自己判对错」的产物。
2. **一个任务一个小改动，超过 300 行拆开。** 大块交付无法审，只能「看到全绿就过」。
3. **需要发明新概念/新接口的任务不委派。** `ModelPool` 就是规格填空的产物：字段齐全，值是编的。

## 明确不做（排队，非取消）

Door、模型池、自我升级、多设备协作、角色治理——全部停在设计稿。等日用中真的撞上「这台机器跑不动」或「这次不该发给云端」，那时才知道该做成什么样。

长期目标不变，见 `docs/history/IXAEON_长期愿景与开发路线图.md`。
