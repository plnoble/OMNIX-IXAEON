# IXAEON v0.2 修复复审报告

日期：2026-09-07。用途：交给修复 agent 继续收尾，再交回独立复审。

审核提交：`b021d361e48d4e868f980eeb3816b7ff0e92e2d1`。

对照基线：[2026-09-06 全项目审核与 v0.3 准入条件](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_全项目审核与v0.3准入条件_2026-09-06.md)。本轮检查了 R0–R3 修复、当前实现、审核包和相邻使用场景。

## 1. 先说结论

**这轮修复有实质进展，但还不能判定全部修复，也暂不放行 v0.3 Door 正式实施。**

上次的 15 项核心检查全部通过；原先“工作记录写入了但界面看不到”的检查也通过了。一键验证、现有桌面测试、扩展串联和解包产物检查均通过。不是上一轮问题原封不动。

不过，新增测试确认了两个需要优先处理的问题：

1. **重新分析时会漏掉仍然有效的旧结论。** 例如第一次提取 A，第二次模型明确返回 A＋B，最后数据库只剩 B。原文还在，但派生记忆丢了一条。这是本轮保护逻辑引入的回归。
2. **连续纠正两次后，最后一次人工改口没有被完整保护。** AI 再提出相反约束时，可以不带待讨论标记进入当前理解。

另有待处理状态、简报预算、引用展开、工作记录详情、界面测试覆盖及安全／交付说明的收尾问题。下面共列 **9 组事项**，不要求重写项目或增加新的产品方向。

## 2. 本次实跑结果与边界

| 检查集合                        | 本轮结果             | 说明                                                                                 |
| ------------------------------- | -------------------- | ------------------------------------------------------------------------------------ |
| 完整 `node scripts/verify.mjs`  | 通过                 | lint、格式、类型、构建均通过；单元 16＋集成 135＋五组历史审核回归 61＝212 项测试通过 |
| 现有桌面 E2E                    | 16/16 通过           | 包含重写后的 m2-ui 6 项；测试通过不等于场景覆盖完整，见 RF07                         |
| 扩展 E2E                        | 通过                 | 受控网页，覆盖配对、采集、流式稳定、暂停／继续及非目标站点等                         |
| 真扩展→真实本地服务→SQLite 串联 | 通过                 | 受控网页；配对、刷新不重复、增量、暂停／继续、草稿转正链路                           |
| 现有 win-unpacked 产物检查      | 3/3 通过             | 自定义目录、重启保留、搬迁后无全局 Node 的 MCP 握手与四工具调用                      |
| 上轮独立 UI01                   | 通过                 | 项目页现在能显示真实回写记录                                                         |
| 本轮新增核心检查                | 8 项：1 通过、7 失败 | B01、B02、B03 两种入口、B04、B05、B06 失败；B07 正向对照通过                         |
| 本轮新增界面 BUI01              | 失败                 | 记录已持久化，但失败测试和未完成事项在界面不可见                                     |

这些是不同集合，不能用 212 项通过抵消新增失败，也不能把 7 个失败用例说成 7 个独立根因。

安装包 `apps/desktop/release/IXAEON-Setup-0.2.0.exe` 已核对：122,591,348 字节，SHA-256 与审核包一致：

```text
AE692588CF0AD666DAB47D66FBF1BB95D7FF73D4795CC79B11B34E35BE03BBF2
```

核对文件哈希及启动解包产物，不等于执行过安装器，也不单独证明安装包和当前源码可重现地对应。

本次没有修改业务代码或既有测试，没有访问真实 ChatGPT 账户、调用收费模型、运行安装器、恢复或删除用户真实数据库。新增检查使用合成资料、假模型和隔离数据库。浏览器脚本重建的仅是既有专用测试目录 `D:\Agent\Temp\ixaeon-ext-e2e`、`D:\Agent\Temp\ixaeon-serial-real`；其中旧测试数据被替换，不涉及用户资料。旧审核报告和旧失败 JSON 保留。

这是一轮修复复审和针对性的相邻场景检查，不是完整渗透测试，也不保证不存在其他缺陷。

## 3. C01–C13 逐项结论

“已测场景通过”表示该问题的已有复现与相关回归通过，不表示所有可能组合都得到证明。

| 上轮编号               | 本轮结论                     | 已修好与仍欠缺的部分                                                          |
| ---------------------- | ---------------------------- | ----------------------------------------------------------------------------- |
| C01 验证失败           | 已测场景通过                 | 完整 verify 通过，未通过关闭检查来绕过                                        |
| C02 取消被恢复扫描破坏 | 已测场景通过                 | 启动遗留恢复与日常扫描分开，A06 通过                                          |
| C03 人工归属后错误副本 | 已测场景通过                 | A01 的“搬到 B、重提又在 A 复制”场景通过；仍须保护正常重提，见 RF01            |
| C04 归属与确认混淆     | 部分修复                     | 重要决定不再被错误清除待确认；普通条目仅缺项目时又无法解除待处理，见 RF03     |
| C05 纠正链与在途改口   | 部分修复                     | A04／A05 通过；连续两次纠正仍不完整，并引入普通旧结论丢失，见 RF01／RF02      |
| C06 搜索／问答确认语义 | 已测场景通过                 | A10／A11 通过，搜索拒绝项和问答确认标记已修                                   |
| C07 一段多结论被去重   | 已测场景通过                 | A12 通过，条目身份与依据身份已分开                                            |
| C08 简报预算           | 部分修复                     | A07–A09 通过；接近预算边缘仍超限，见 RF04                                     |
| C09 引用展开           | 原工作引用已修，矩阵未齐     | A13 和 B07 的工作记录引用可展开；人工创建／纠正引用仍失败，见 RF05            |
| C10 工作记录界面       | 部分修复                     | UI01 通过；测试详情和未完成事项缺失，见 RF06                                  |
| C11 密钥降级存储       | 新保存路径已修，升级处理未齐 | A15 通过；旧 `plain:` 配置仍直接读取，提示还承诺不存在的会话密钥方案，见 RF08 |
| C12 六类界面验收       | 明显改善，尚不完整           | 已有非空提取、不采纳、纠正、工作回写；真正双来源冲突与确认动作未覆盖，见 RF07 |
| C13 交付对齐           | 部分完成                     | 版本和包哈希已对齐；隐私文档、验证入口声明及收尾记录仍需同步，见 RF09         |

## 4. 给修复 agent 的具体事项

优先级：P1 为优先阻断问题，影响记忆可信度；P2 为本版既定功能、测试或交付契约未完成，也应在对应放行项中解决。没有要求现在开发 Door、手机端、分布式或 RAM 压测。

### RF01 · P1：保护集合误包含普通 AI 条目，导致重提后丢掉有效旧结论

位置：[extractor.ts 保护查询](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:140)、[提交前重查](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:249)、[旧条目删除](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:300)。证据：B01 失败。

复现：

1. 同一来源第一次生成 `UNCHANGED_OLD_CONCLUSION`，不做任何人工确认／归属／纠正。
2. 第二次模型返回 `UNCHANGED_OLD_CONCLUSION` 和 `BRAND_NEW_CONCLUSION`。
3. 预期两条都存在；实际 current 列表只有 `BRAND_NEW_CONCLUSION`。

原因是 SQL 的括号问题。当前写法相当于：

```text
属于当前来源 OR（属于纠正关系 AND 符合人工保护条件）
```

而不是：

```text
（属于当前来源 OR 属于纠正关系）AND 符合人工保护条件
```

因此普通 AI 旧结论也进入“人工保护集”，同文候选先被跳过；随后新结论触发整批替换，旧行却仍被删除。两份查询都有这个问题。

修复要求：

- 正确划定人工保护范围，统一初次读取和提交前重查，避免复制两套易分叉的 SQL。
- 区分“保留人工条目”和“原子替换普通 AI 快照”，不能一边跳过重建一边删除原行。
- 清理无实际用途的 `protectedStatements` 查询；不要用扩大到全部用户条目的保护范围来绕过问题。
- 不删除原文或批量清空旧理解来让测试通过。

验收：B01 通过；补仅旧结论、旧＋新、旧＋改写、模型失败、人工归属和人工纠正混合场景。正常重提不丢仍有效结论，人工保护也不退化。单纯通过旧 A01／A04／A05 不足以验收此项。

### RF02 · P1：纠正关系只查一层，第二次改口不在保护范围

位置：[extractor.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:140)、[同一问题的提交前查询](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:249)。证据：B02 失败；不是只靠静态猜测。

复现：AI 日志约束 → 用户第一次纠正 → 用户第二次纠正为“项目日志可以发送到远程服务器保存和分析”。随后 AI 提取“项目日志不可以发送到远程服务器保存和分析”。

实际：新相反约束 `state=current`、`needs_review=false`，没有进入待讨论。人工最后一条并未消失，但与它冲突的新条目被当成普通当前理解。

原因：查询只找“直接连接到带本来源 ID 条目的 corrections 对端”。用户纠正产生的后继没有 `extracted_from_source_id`，继续纠正后就越过了这一层查询的覆盖范围。“双向查一层”不等于遍历整条纠正链。

修复要求：

- 从当前来源的相关条目出发，沿实际纠正关系完整追溯，采用递归查询或有去重／防循环的遍历。
- 在提交前重新读取最新结果，覆盖模型等待期间的多次纠正、确认和不采纳。
- 保留原文、历史前驱和人工当前结果。相似度只用于提示潜在冲突，不能替用户决定两句是否等价。
- 不能为了保护一条链，把其他来源／项目的所有用户条目一起混入。

验收：B02、旧 A04／A05 和 B07 同时通过；至少三次连续纠正，并检查在途改口及跨项目隔离。此项不能只靠修 RF01 的括号解决。

### RF03 · P2：只缺项目的普通条目，选完项目仍留在待处理

位置：[itemStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/itemStore.ts:280)、[sourceStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/sourceStore.ts:411)。证据：B03-item、B03-source 均失败。

旧问题是“选项目把所有待处理原因都清掉”；现在改成“选项目一律不清”。两个极端都不符合上轮要求。

复现：普通 `open_loop`“等待性能测量数据”，没有冲突，仅因为项目为空而 `needs_review=true`。分别使用条目归属、整个来源绑定两种入口，选定项目后仍为 true。

修改：按当前事实重新计算待处理原因。缺项目已解决可以解除这一原因；重要 AI 决定尚未确认、冲突等其他原因仍要保留。不要求新建庞大状态框架，可集中实现一个判断函数。

验收：B03 两入口通过，同时旧 A02／A03 必须继续通过；覆盖普通条目、重要决定、冲突、已确认／已不采纳、绑定／解绑。不能将所有条目改成需人工确认来绕开普通条目的规则。

### RF04 · P2：简报接近预算上限时，完整 JSON 仍会超限

位置：[mcpStore.ts 候选试放](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/mcpStore.ts:262)、[最终统计](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/mcpStore.ts:275)。证据：B04 失败。

预算 2,000 字符时，测试得到完整响应长度 **2,002／2,012／2,022／2,032**。这不是原来“双扣预算”和空简报统计为零的问题；那几项已经修复。

原因：条目试放时使用 `chars_used=0`、`truncated=false`，没有为最终截断提示及统计数字留空间。之后仅裁剪任务文本，即使任务已空，仍可能超限；最终统计正确了，但没有再强制验证实际返回值不超过预算。

修改：按最终元数据、提示和统计字段计算；在返回前对真实完整对象作最终预算校验，必要时按明确优先级收缩内容。无法合法满足时返回明确契约错误，不能返回超限的成功结果，也不能一律清空所有结论。

验收：B04 和旧 A07–A09 同时通过；补最小预算、临界位数、中文／转义字符、长项目名、长任务、待分析提示、多条目与单个长条目。不要把允许误差改大来通过。

### RF05 · P2：人工创建／纠正条目的简报引用仍无法展开

位置：[mcpStore.ts getSourceExcerpt](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/mcpStore.ts:449)。证据：B05、B06 失败；工作引用的 A13／B07 通过。

复现：人工创建一条决定，或纠正 AI 决定。`prepare_task` 正常返回该条目及 ref，再把 ref 交给 `getSourceExcerpt`，却报“引用不存在（既不是片段也不是条目 ID）”。

原因：当前支持片段、有 evidence 的条目以及新补的工作记录分支；人工新条目没有普通 evidence 时仍落到错误分支。上轮 C09 已明确要求把人工创建／纠正条目加入引用类型矩阵。

修改：为简报返回的每种合法引用提供诚实、可追溯的展开结果。人工输入就返回人工记录并标明身份；纠正可补充关系，但不能伪造原文摘录。若沿纠正链读取旧来源依据，仍须检查来源授权，不得绕过撤销规则。

验收：B05／B06／B07 通过；通过独立 MCP 进程覆盖片段、AI 条目、人工创建、人工纠正、工作记录、无效 ID、撤销授权和预算。按明确调整后的契约验证，不能仅让异常消失而返回空内容。

### RF06 · P2：工作列表能看见了，但失败测试和未完成事项仍看不到

位置：[Projects.tsx](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/renderer/src/pages/Projects.tsx:6)。证据：新增真实 Electron 界面 BUI01 失败，旧 UI01 通过。

复现通过真实本地 MCP 端点写入一条“部分完成”记录，其中包含：

- 失败测试：`RECHECK_FAILED_TEST`。
- 未完成事项：`RECHECK_UNFINISHED_TASK`。

公开 IPC 查询确认这两个字段已经持久化。项目页能看到任务、执行者、时间、结果、摘要和“用户尚未验收”，但看不到上述两项，也没有可打开的工作详情。当前组件只渲染摘要的前 200 字符等基础字段。

修改：在列表或可展开详情中展示测试结果和未完成事项；有必要时展示变更摘要。可以保持页面简洁，不要求把长记录全部平铺。继续明确“agent 自报，不等于用户决定／验收”。处理刷新和加载失败，不能把请求失败悄悄显示成“暂无记录”。后一点来自当前代码检查，本次未单独做刷新／错误注入测试。

验收：旧 UI01 和 BUI01 同时通过；如采用详情页，允许测试增加真实点击导航，但必须最终看到这两个持久化字段。另补重进页面、重启和项目隔离检查。

### RF07 · P2：六类界面测试仍有名实不符的部分

位置：[m2-ui.spec.ts 场景定义](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/e2e/m2-ui.spec.ts:47)。证据：当前 6 项测试实跑通过，加上逐项检查测试输入、动作与断言。

已改善：现在确实提取非空条目，实际点击不采纳、执行纠正，并验证工作回写及工作引用展开。不能再沿用上轮“全是空态检查”的描述。

仍欠缺：

1. **S4 名为“不同来源矛盾”，实际只有一条 Tauri 方案。** 没有另一份相反来源，也没验证双方同时保留和不自动选边。一个普通“待确认”标记不能证明双来源冲突场景通过。
2. **没有场景实际配置 `uiAction.kind='confirm'`。** 测试有确认按钮分支，但没有用例触发，审核包却声称真实确认点击已覆盖。
3. **S2 的语义对象混淆。** 当前条目是“云同步方案已被用户明确否决”，却对这条理解点“不采纳”并检查它从简报消失。这验证的是“不采纳这条理解”，不是验证“用户否决云同步这件事被正确记住”。

修改：

- 为 S4 构造同项目两个真实来源及相反结论，检查界面、持久化结果和 MCP 均保留双方及不确定性，不自动选边。
- 添加重要决定的真实确认流程，验证操作前后状态和简报标签。
- 分开测试“用户不采纳 AI 建议”和“AI 正确记录了用户否决方案，用户确认这条理解”。两者不能使用同一种否决动作冒充。

验收：六类场景的名称、资料和断言能一一对应；不要删除失败路径、改为空态断言或只增加测试标题。允许复用已有核心语义样例，但不能以核心测试代替真实界面操作。

### RF08 · P2：旧可解码密钥的处理缺失，错误提示承诺了未实现的功能

位置：[ipc.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/ipc.ts:417)、[模型读取入口](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/appRuntime.ts:427)。证据：A15 正向结果＋当前保存／解密调用链检查。没有读取用户真实密钥。

新保存路径已修：系统加密不可用时会拒绝持久化，不再生成新的 `plain:` Base64。

剩余问题：

- `decryptApiKey()` 对旧 `plain:` 仍直接解码；没有发现启动迁移或重新输入告知机制。旧版曾落盘的可恢复原文不会因为升级自动得到保护。
- 错误消息说“可继续使用仅本次会话的密钥”，但当前设置保存与模型读取链路没有实现独立的会话内密钥方案；保存操作实际抛错结束。用户会被指向一条不存在的路径。

修改：旧配置应在安全条件满足时迁移为系统加密形式，或明确提示重新输入／清除；失败时保留可理解、可恢复的状态，不泄露密钥。会话方案不是本轮必须扩展的新功能，可以直接改正提示，明确当前无法保存。

验收：只用占位凭证测试旧 `plain:` 配置、系统加密不可用、解密失败、更换／清除密钥和重启。敏感值不得出现在日志、测试报告、快照或 Git 中。不要求新增全库加密。

### RF09 · P2：把交付声明和用户实际看到的文档同步到真实状态

位置：[REVIEW_PACKET 第 0.16 节](D:/Agent/Project/OMNIX-IXAEON析衍/REVIEW_PACKET.md:219)、[verify.mjs](D:/Agent/Project/OMNIX-IXAEON析衍/scripts/verify.mjs)、[privacy-model.md](D:/Agent/Project/OMNIX-IXAEON析衍/docs/privacy-model.md)、[dev-log-fixes.md](D:/Agent/Project/OMNIX-IXAEON析衍/docs/dev-log-fixes.md)。

已完成：各版本标识 0.2.0、安装包命名和哈希对齐；审核包补充了 MCP 数据可能由编码客户端送往云模型的说明。

需修改：

- 第 0.16 节宣称“15 项＋UI01 已纳入 verify”，但该脚本只加入了 15 项核心检查。UI01 当前可以单独通过，不等于默认验证入口已经运行它。请接入合适的 E2E 入口，并准确写明对应命令。
- `docs/privacy-model.md` 仍称“数据离开电脑的唯一渠道”为模型 API，未同步 MCP 的外部客户端边界；扩展权限清单也漏了实际 manifest 中的 `tabs`。应说明用途，而不是只在审核包补一句。
- 当前 C01–C13 全部勾选完成、确认点击／完整纠正链等声明超出证据，按本报告重新标注。历史小节可保留但应明确其历史属性；不改写旧独立审核报告来抹除失败记录。
- 开发日志需补齐这轮修复和已知剩余项；仍欠缺的非空旧库升级、安装流程和真人验证继续列为未完成。
- 修复后提供同一候选提交的完整输出、产物版本与哈希；不要拿旧包的成功启动来证明新业务修复已经进入产物。

验收：读审核包即可分清“代码已改”“本轮自动化实测通过”“真实环境尚未验证”。旧报告与原始失败证据保留，新结果另存。

## 5. 新增证据与复现命令

本轮新增文件：

- [核心检查 B01–B07（B03 含两个入口，共 8 项）](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/recheck-20260907.test.ts)
- [核心配置](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/vitest.recheck-20260907.config.ts)
- [本轮原始失败 JSON](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/results-recheck-20260907.json)
- [新增真实界面 BUI01](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/recheck-ui-20260907.spec.ts)
- [界面配置：同时运行旧 UI01 和新 BUI01](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/playwright.recheck-20260907.config.ts)
- [界面失败上下文](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/.recheck-ui-20260907/recheck-ui-20260907-BUI01--a7479-d-work-not-just-its-summary/error-context.md)

从仓库根目录执行：

```powershell
# 原有完整门槛
node scripts/verify.mjs

# 新增独立核心检查；当前应失败。修复后结果另存，不覆盖本轮 JSON。
node node_modules/vitest/vitest.mjs run --config apps/desktop/test/review/vitest.recheck-20260907.config.ts

# verify 已构建桌面产物；若只改代码，可先 node scripts/build.mjs
# 当前应为旧 UI01 通过、新 BUI01 失败
node apps/desktop/node_modules/@playwright/test/cli.js test -c apps/desktop/test/review/playwright.recheck-20260907.config.ts

# 以下顺序执行，避免共同端口／浏览器目录冲突
node scripts/test-e2e.mjs desktop
node scripts/test-e2e.mjs extension
node scripts/test-e2e.mjs serial

# 检查已有解包产物，不代替重建候选产物或安装器验收
node apps/desktop/test/review/packaged-20260905.mjs
```

本轮新增核心测试和 UI 测试尚未接入默认 verify／E2E；修复 agent 应把它们纳入合适的持续回归入口。不得因其当前失败而永久排除。

Electron／浏览器检查需要测试进程权限；发现本地 43191 已有用户服务时应停止，不可强杀用户进程。保留本轮 `.recheck-ui-20260907` 失败证据，复跑 UI 时使用新的输出目录或先保留原始记录。

## 6. 建议修复顺序与停止条件

分三批即可，不要混入设备新功能：

1. **记忆可靠性：RF01、RF02、RF03。** 统一人工保护与待处理判断，补连续操作组合。两项 P1 优先。
2. **编码工作闭环：RF04、RF05、RF06。** 简报不超限、引用可查证、工作结果可真正检查。
3. **安全与交付收尾：RF07、RF08、RF09。** 六类界面场景完整、旧密钥安全处理、验证入口和发布材料对齐。

每批交付提交说明、变更文件、对应回归结果和已知剩余问题；不覆盖别人的工作，不删除旧失败证据，不通过改弱断言或扩大允许误差来交差。若采用合理的新契约，例如预算无法满足时明确拒绝、工作详情改为点击展开，应明确记录并让测试验证新契约的实际效果。

本轮停止条件不是无限寻找新功能，而是本报告剩余要求满足、历史回归不倒退、交付证据真实可追溯。

## 7. 现在离 v0.3 还差什么

沿用上轮准入条件，不另起一份更大的计划。

### 工程门槛：尚未满足

- [ ] RF01／RF02 优先修复并独立通过，正常重提与多次人工改口可靠。
- [ ] RF03–RF06 对应的本版功能契约完成。
- [ ] RF07–RF09 的测试、安全升级处理与交付说明完成。
- [ ] 原 212 项、既有 E2E、本轮新增核心和 UI 检查同时通过；不是拼接不同提交的历史成功结果。
- [ ] 用含原文、人工纠正、人工归属和工作记录的非空旧库副本验证升级、备份、恢复与失败处理。
- [ ] 当前候选源码、产物和审核材料对应一致。

### 真实使用门槛：本轮仍未执行

需要用户参与或明确授权：真实 ChatGPT 网页采集、真实模型六类语义检查、真实编码客户端读取背景／查证／回写的小任务，以及安装／升级流程。受控网页、FakeProvider、MCP 握手和 win-unpacked 启动分别有价值，但不能替代这些验证。

通过后再用非敏感项目短期试用，沿用上轮建议的 3–7 天观察窗口；这不是要求现在自动创建监控，也不能用“试用够天数”抵消已知失败。

### v0.3 范围不变

可以继续讨论设备报告格式和细化设计，但先不要把 Door 功能并入正式交付来宣称上一阶段已通过。

放行后的第一步仍是 **D0：设备报告格式＋假探测器＋身份／权限／过期测试**，然后才接 Windows 只读探测。首版仍不做 RAM 压测、多设备分布式、手机 App、模型下载或自动执行。

**一句话：先让它稳定记住你说过的话、尊重你后来的改口，再让它认识你的设备。**
