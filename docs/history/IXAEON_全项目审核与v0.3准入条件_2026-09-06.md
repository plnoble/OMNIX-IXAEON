# IXAEON 全项目审核、修复任务与 v0.3 准入条件

日期：2026-09-06。用途：交给修复 agent 实施，再交回独立验收。

审核源码：`df0adbe68734bb843fa0b925fbb549f64e4ae58a`（main）。

## 1. 先说结论

**当前不通过完整验收，不建议直接把 v0.3 Door 功能接入主线。**

项目已有可工作的基础：导入、浏览器采集、数据库、项目记忆、MCP、备份恢复和打包都不是空架子。上轮指出的分支切换漏触发分析，以及重提时删除人工归属条目的原始场景，已经有修复并通过现有回归。

但连续使用时仍存在影响记忆可信度的问题：人工归属后会在原项目重新生成副本；选项目会清掉待确认状态；分析期间改口后旧决定仍会出现；被否决的建议会被搜索当作当前结论返回。此外，后台工作记录并没有真正展示到项目页面。

本次整理为 **13 组修改事项**，包含业务缺陷、安全边界、测试真实性和交付问题。不是建议重写整个项目；应先统一已有规则在各入口的行为。

### 本次证据强度

- 已有单元／集成测试：151 项通过；已有四组独立审核回归：11 + 13 + 7 + 15 = 46 项通过。
- 桌面端现有 E2E：14 项通过；扩展 E2E 和真扩展→真实服务→真实数据库的受控串联测试通过。
- 独立新增核心／运行时检查：15 项中 14 项失败，1 项正向对照通过。
- 独立新增真实 Electron 界面检查：1 项失败，确认“工作已入库，但页面不可见”。
- 构建和格式检查通过；`verify` 在 lint 阶段失败，单独类型检查也失败。
- 现有解包安装产物的 3 项检查通过。这不是安装器安装／卸载验收，也不能代表新发现的业务问题已解决。

测试通过数和失败数属于不同测试集合；不能用前者抵消后者，也不能把 14 个失败断言说成 14 个互不相关的根因。

## 2. 审核范围与限制

覆盖项目的主要数据链路与应用边界：

1. Windows 桌面端、IPC、来源状态和用户操作。
2. 浏览器扩展的配对、增量采集、分支变化、暂停和恢复。
3. core 的导入、授权、SQLite 迁移、提取、纠正、归属和后台任务。
4. 问答、搜索、MCP 简报、依据展开与工作回写。
5. 导出恢复的既有回归、构建、测试脚本和已有打包产物。
6. 开发计划、审核包和 v0.3 Door 的进入条件。

这是基于当前源码、现有回归和新增场景的工程审核，不代表每一行代码都被形式化证明，也不是完整渗透测试或依赖漏洞扫描。

本次未访问真实 ChatGPT 账户，未调用收费模型，未运行安装器，未恢复／删除用户真实数据库，未做真实编码客户端日用任务。所有复现使用合成资料、临时数据库和假模型。没有修改业务代码或既有测试；只新增独立审核测试、结果和本文。

## 3. 修改事项总表

优先级说明：P1 = 影响记忆可信度、权限／控制或当前交付门槛，必须优先修；P2 = 功能契约／交付问题，也须在对应放行项中解决。

| 编号 | 优先级 | 通俗说明 | 证据 |
| --- | --- | --- | --- |
| C01 | P1 | 一键验证目前过不了 | lint 1 个错误；tsc 2 个错误 |
| C02 | P1 | 正在运行的任务被误判成崩溃遗留，随后取消失效 | A06 |
| C03 | P1 | 你把条目分给 B，重提后 A 又出现一份 | A01 |
| C04 | P1 | 选项目被错误地当作“已处理待确认” | A02、A03 |
| C05 | P1 | 人工纠正链与分析中的改口没有完整保护 | A04、A05 |
| C06 | P1 | 同一条理解在简报、搜索、问答中的确认语义不一致 | A10、A11 |
| C07 | P2 | 同一段原文支持两条结论，问答只保留一条 | A12 |
| C08 | P2 | 简报预算既可能超限，也可能无故丢掉一半内容 | A07、A08、A09 |
| C09 | P2 | 简报给出工作记录引用，却无法展开 | A13 |
| C10 | P1 | 工作记录写入了，用户在项目页看不到 | UI01 + 页面实现 |
| C11 | P1 | 系统加密不可用时，密钥偷偷退化成可直接解码的存储 | A15 + 保存调用链 |
| C12 | P1 | “六类界面验收”有用例名称，但缺对应实际操作和断言 | 现有 m2-ui 测试代码 |
| C13 | P2 | 版本号、审核声明和发布材料需要重新对齐 | manifest、package、审核包与产物 |

## 4. 各项修复要求

### C01：恢复完整验证门槛

位置：[m2-ui.spec.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/e2e/m2-ui.spec.ts:85)。

实跑结果：

- `node scripts/verify.mjs` 在 lint 停止：第 85 行 `doc` 未使用。
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`：第 162、166 行 `window.ixaeon` 可能不存在。
- 单独 build 成功不等于类型检查通过；Playwright 能执行也不等于 `verify` 通过。

修改：消除空循环；为测试中的 preload API 做明确可用性检查或符合既有类型约定的访问。不要关闭 lint／类型规则、把文件排除或只运行 build 来交差。

验收：从仓库根目录运行完整 `verify`，保留本次 HEAD 对应的真实输出；同时通过全部 E2E。

### C02：崩溃恢复扫描误伤仍在执行的任务

位置：[appRuntime.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/appRuntime.ts:230)、[ipc.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/ipc.ts:350)、[jobQueue.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/jobs/jobQueue.ts:243)。

复现 A06：

1. 一个合法分析任务已经进入模型调用。
2. 执行开启采集／自动分析时会调用的 `sweepPendingAnalysis()`。
3. 扫描把数据库中所有 `running` 改成 `queued`，没有确认这些任务是否真的失去了执行者。
4. 再取消这个任务，队列按“排队任务”处理，只改数据库状态，没有中止在途执行。
5. 模型返回后：任务最终变为 `succeeded`，写入 1 条理解，已分析版本推进到 1。

预期：任务保持已取消，不写入结果，不推进分析版本。

修改：将“进程启动时恢复真正遗留任务”与“日常扫描欠分析来源”分开；通过运行代次／实际执行者确认遗留任务。取消必须依据真实执行中的任务 ID 发出中止信号，不能只依赖可能被其他流程改写的状态字段。

验收：A06 通过；另补开关切换、取消、恢复扫描、异常退出重启的组合场景。当前运行任务不能被记成崩溃遗留。

### C03：人工归属保住旧条目，却挡不住错误项目里的新副本

位置：[extractor.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:234)。

复现 A01：来源属于 A → 提取条目 → 用户把条目单独归属 B → 同来源再次提取同一结论。旧条目留在 B，但提取器又在 A 新建一条相同结论。

影响：B 的人工归属没有丢，但 A 的项目背景重新混入了用户已经移走的内容。因此“旧 ID 没被删”不是完整验收条件。

修改：人工归属保护同时约束重提结果的匹配、去重和有效项目；不能只给删除语句增加 `manual_project = 0`。与人工条目关联的新候选应保留人工归属，或明确进入冲突待处理，不得静默在旧项目复制。

验收：A01 通过；至少连续重提三次，检查两个项目的数据库、搜索、简报均无错误副本；再做来源整体改绑。

### C04：项目归属与确认状态仍被混在一起

位置：[sourceStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/sourceStore.ts:414)、[itemStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/itemStore.ts:282)。

复现 A02／A03：新提取的重要决定是 `confirmation=none`、`needs_review=true`。绑定整个来源到项目，或单独为条目选项目，都会把 `needs_review` 清为 false，但用户没有确认结论正确。

影响：条目从 Inbox 消失；MCP 又仍标“待用户确认”，两个入口彼此矛盾。

修改：分别判断“项目待归属、结论待确认、存在冲突”等待处理原因。解决一个原因不能清掉其他原因。可以集中计算，不强制为此新建复杂状态系统。

验收：A02／A03 通过；界面实际选项目后，重要决定仍可确认／不采纳；已确认或明确否决的条目也不能因改项目被错误重新送审。

### C05：纠正链保护不完整，且没有覆盖模型返回前的人工操作

位置：[extractor.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:121)、[itemStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/itemStore.ts:128)。

两个独立场景：

- A04：纠正后的新 `origin=user` 条目没有 `extracted_from_source_id`。保护查询却要求该字段等于当前来源，并没有像注释所说沿 `corrections` 遍历完整链。因此，后续与新人工约束相反的 AI 约束可以不带冲突／待讨论标记进入当前理解。
- A05：保护集合在模型请求前读取。请求等待期间用户完成纠正，模型返回后仍按旧集合写入，于是旧决定再次作为 current 出现。

修改：基于实际的纠正关系追溯人工结果与前驱；在结果提交前用最新人工状态重新协调候选。不能把人工改口仅理解为“保留旧 superseded 行”，也不能依靠字符串相似度证明两句话语义相同。

验收：A04／A05 通过；补连续两次改口、用户确认／不采纳发生在模型等待期间、约束与偏好类的冲突场景。原文和纠正历史保留；不确定冲突让用户决定。

### C06：搜索与问答没有共同遵守确认语义

位置：[mcpStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/mcpStore.ts:348)、[search.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/search.ts:139)、[askStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/askStore.ts:114)。

- A10：用户点“不采纳”后，MCP 搜索与桌面条目搜索仍返回该建议，状态是 current，返回结构没有说明用户已经拒绝。默认简报虽然排除了它，编码 AI 改走搜索仍会拿到误导信息。
- A11：同一个 AI 决定，用户确认前后，问答实际发送给模型的上下文完全相同。问答只在排序中用到确认字段，没有把“待确认／已确认”告诉模型。

修改：集中定义所有消费者的状态规则。默认当前理解排除已拒绝建议；如提供历史搜索，必须显式区分历史／不采纳，保留原文而非删除历史。问答上下文、引用卡和 MCP 输出都应表达 origin、confirmation、有效状态与冲突。

验收：A10／A11 通过；同一组资料同时检查 Inbox、Understanding、Ask、桌面 Search、MCP prepare/search。假模型测试检查真实发送的上下文；另以真实模型验收其理解，不把两者混为一谈。

### C07：问答按依据片段去重，误删不同结论

位置：[askStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/askStore.ts:82)。

复现 A12：一段原文支持一条目标和一条约束，提取结果均已保存。问答组装时只按 `segmentId` 去重，后一条结论被抛弃。本次 `BETA_CONSTRAINT` 没有进入模型上下文。

修改：分开“条目身份”和“原文依据身份”。可以合并重复摘录，不能删除不同的条目陈述；引用映射应保留一段原文对应多个结论的关系。

验收：A12 通过；补一段原文支持目标＋约束＋否决事项的场景，检查预算内均能传给模型。

### C08：简报字符预算实现仍不符合声明

位置：[mcpStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/mcpStore.ts:208)。

三项复现：

- A07：合法输入 `task` 为 4000 字符、`max_chars=2000`，空项目的完整返回值仍达 **4493 字符**。
- A08：12 条约 500 字符的人工决定，本来可以放进 12000 字符的完整响应，却只留下 **6 条**。原因是把已包含条目的整个输出误当作“元数据开销”，又扣了一次。
- A09：空简报 `chars_used=0`，实际序列化结果 **499 字符**，并未统计说明、任务和容器。

修改：先构造真实元数据与必要提示，再分配内容预算；最终校验实际序列化结果，不用 `+120` 估算提示长度。`chars_used` 要按约定的完整响应统计，处理统计字段自身位数变化。任务本身已超过预算时，应清晰拒绝或按明确契约压缩／截断，不能返回超限成功结果。

验收：A07–A09 通过；覆盖最小预算、长项目名、长任务、转义字符、零条目、单个长条目、未分析提示与多条目。保留重要风险／待确认信息，不以无故清空简报来满足长度测试。

注：A07 当前按“合法输入的成功结果应不超限”断言。若修复选择显式拒绝无法满足的输入，可调整为验证明确的契约错误，但不能接受超限结果。

### C09：工作记录引用无法展开

位置：[mcpStore.ts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/mcpStore.ts:441)、[MCP 输入说明](D:/Agent/Project/OMNIX-IXAEON析衍/apps/mcp/src/index.ts:113)。

复现 A13：`record_work_result` 返回工作 ID；下一次简报的 recent_work 用该 ID 作为 ref。按工具说明交给 `get_source_excerpt` 展开，却返回“引用不存在”。解析器只实现 segment 和有 evidence 的 item，没有工作记录分支。

修改：为简报承诺的引用类型提供真实可追溯的展开路径。优先扩展现有工具与契约；清楚返回“agent 自报工作，用户未验收”。人工创建／纠正条目的引用也应纳入同一引用类型矩阵，不能伪造原文依据。

验收：A13 通过；通过独立 MCP 进程依次获取简报并展开各类 ref，验证工作摘要、测试状态、权限及预算。原有片段／条目引用兼容。

### C10：工作回写没有接到用户界面

位置：[Projects.tsx](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/renderer/src/pages/Projects.tsx:1)、[listWorkRuns 后端](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/appRuntime.ts:420)。

独立真实窗口复现 UI01：

1. 新建临时项目。
2. 经生产 MCP HTTP 端点写入 `UI_AUDIT_WORK_MARKER` 工作记录，响应成功。
3. 经公开 IPC `listWorkRuns` 查询，确认记录存在。
4. 打开项目页，实际页面只有项目名、状态、来源、暂停和归档，没有该工作记录。

修改：把已有工作记录查询接入项目界面，展示任务、执行者、时间、结果、测试和未完成事项；标清“agent 自报／用户尚未验收”。处理空态、刷新和项目隔离，不把工作成功当成用户决定。

验收：UI01 通过；再次进入页面和重启后仍可见；另一项目不可见；下一轮 MCP 简报也能读取。若采用独立工作详情页，可相应调整测试导航，但必须测试真实记录内容，不只测项目名称。

### C11：API Key 在加密不可用时静默降级

位置：[ipc.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/ipc.ts:417)、[配置契约](D:/Agent/Project/OMNIX-IXAEON析衍/packages/contracts/src/config.ts:12)。

复现 A15：把 `safeStorage.isEncryptionAvailable()` 设为 false，`encryptApiKey()` 不拒绝保存，而是返回 `plain:` 加 Base64。Base64 可直接解码，不是加密。保存模型设置及首次设置会把此返回值写进配置。

当前界面称“系统加密存储”，返回的提示只讲“数据库未整体加密”；并没有明确告知 API Key 已退化为可恢复原文的存储。与契约“永不明文落盘”不一致。

修改：加密不可用时默认拒绝持久化密钥，或提供明确说明且仅存内存的本次会话方案；不能静默保存可逆编码。已有 `plain:` 数据应有安全迁移／重新输入提示，不能在日志或审核包输出密钥。

验收：A15 通过；补系统加密不可用、解密失败、升级旧配置、清除／更换密钥场景。测试只用占位字符串，不接触真实凭证。本条不要求给全部 SQLite／vault 加密。

### C12：六类界面场景没有真正完成

位置：[m2-ui.spec.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/e2e/m2-ui.spec.ts:130)、[REVIEW_PACKET 声明](D:/Agent/Project/OMNIX-IXAEON析衍/REVIEW_PACKET.md:210)。

现有 4 项测试确实实跑通过，但其断言不支持测试名称中的结论：

- S1 没有生成待确认条目，只查文件存在，并查一个没有实际 ID 的 `source-status-` 元素不可见；这不能证明“待确认可见”或正确分析状态。
- S3 没有执行纠正，只检查理解页为空。
- Inbox 用例没有点击确认／不采纳，只检查空态。
- S6 确实写回了工作，但只检查项目名称和理解页不含该工作；没有检查最近工作可见。UI01 已证明这一遗漏掩盖了 C10。
- S2／S4／S5 没有对应完整的界面行为验证。创建了资料文件不等于跑完该场景。

修改：提供测试专用、可预置响应的模型入口，贯通真实导入→提取→UI 操作→数据库→MCP。避免给生产版增加任何可被外部利用的任意写库入口。每类场景先证明条目实际存在，再点击相应操作并验证前后状态。

验收：六类场景分别有非空数据、真实用户动作和跨层断言；测试名与执行内容一致。失败时不能改成检查空态来通过。核心 7 项假模型语义测试可以保留，但不是这部分界面验收的替代品。

### C13：交付材料、版本与隐私说明需要对齐

现状：项目按 v0.2 交付，但根 package、桌面 package、扩展 manifest、健康接口等仍是 `0.1.0`；当前安装包仍名为 `IXAEON-Setup-0.1.0.exe`。这不单独证明业务错误，但妨碍明确识别修复版、升级路径和回退版本。

修改要求：

1. 放行前明确真实候选版本与提交，统一用户可见版本、MCP／健康信息、产物命名与审核包；不要先改版本号就宣称验收通过。
2. 每轮证据绑定 HEAD 和命令时间。上次 `verify` 成功不能覆盖本轮新增代码带来的 lint／tsc 失败。
3. 删除或改正“六类 UI 已完成”这类超出测试证据的表述，修复后再更新结论；不改写旧审核报告来掩盖历史。
4. 更新隐私说明：除直接模型 API 调用外，MCP 返回的数据也可能由外部编码客户端发给其云模型。本地服务不等于资料永不离机。同步实际扩展权限清单。
5. 补旧版非空数据升级、备份、失败回退说明；重点保留纠正、人工归属、原文引用及工作记录，而不是只测试空库迁移。

验收：源码提交、版本、安装包／扩展包、哈希、测试报告能一一对应；未验证项仍明确保留。

## 5. 给修复 agent 的执行顺序

建议分为四批可独立复核的修复提交，不边修边混入 Door：

1. **R0：验证与控制边界。** C01、C02、C11。先恢复可运行验证，堵住取消绕过与密钥存储降级。
2. **R1：统一记忆状态。** C03–C07。归属、确认、人工纠正、检索和问答采用一致规则；补并发与连续操作测试。
3. **R2：完成编码工作链路。** C08–C10。预算真实、引用可展开、回写可在界面看见。
4. **R3：真实测试与交付。** C12、C13；全部既有和新增回归通过，重建产物、更新审核包，并列出真人验收待办。

实施限制：

- 不删除用户原文，不重建空库，不用批量删除历史理解来掩盖冲突。
- 不修改既有失败证据；修复后的 JSON 另存新文件。
- 可以调整测试以符合明确的新契约，但不得通过删断言、排除文件、接受错误结果来通过。
- 保留现有 Windows／Electron／TypeScript／SQLite／MCP 架构，不为修复另起大型框架。
- 将本次回归接入默认验证；本文新增独立文件目前未接入 `verify`，这是交给修复 agent 的验收清单，不应悄悄遗漏。

## 6. 本次复核结果和复现入口

所有命令从 `D:\Agent\Project\OMNIX-IXAEON析衍` 执行。

| 检查 | 本次结果 | 说明 |
| --- | --- | --- |
| `node scripts/verify.mjs` | 失败 | lint 阶段停止，不能说完整 verify 已通过 |
| 单独 TypeScript 检查 | 失败 | m2-ui 第 162、166 行 |
| 默认 format:check 对应命令 | 通过 | 仅代表所覆盖文件的格式 |
| unit + integration | 151/151 通过 | 包含新补的核心六类场景 7 项和 followup 2 项 |
| 四组已有独立审核回归 | 46/46 通过 | 11 + 13 + 7 + 15 |
| `node scripts/build.mjs` | 通过 | 本次源码重新构建 desktop／MCP／extension |
| desktop E2E | 14/14 通过 | C12 说明其断言覆盖不足 |
| extension E2E | 通过 | 受控网页＋扩展，不是真实账户 |
| serial E2E | 通过 | 真扩展→真桌面服务→真 SQLite，网页仍为受控样本 |
| 现有 win-unpacked 复核 | 3/3 通过 | 自定义目录、退出重启、搬移后 MCP 四工具；未运行安装器 |
| 本次独立核心／运行时检查 | 14 失败、1 通过 | A01–A13／A15 失败；A14 正向对照通过 |
| 本次独立界面检查 | 1 失败 | UI01 记录已保存，项目页未展示 |

桌面 E2E 首次在沙箱内因 Electron 配置目录权限无法启动，申请相应执行权限后复跑 14 项通过；该首次环境错误没有被计为业务缺陷。

产物实测：`apps/desktop/release/IXAEON-Setup-0.1.0.exe`，122,587,174 字节；SHA-256：

```text
F8307ACFC511A8DBA9069B5CEBA75F3781CF4F68DFA764B6C9E2F0A9FC57C196
```

哈希与当前审核包所列一致。本次没有重新执行打包安装器生成流程；检查的是已有解包产物和现有安装文件，不能把它们说成已修复本文问题的新包。

### 新增独立复现文件

- [核心／运行时检查](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/project-audit-20260906.test.ts)
- [Vitest 配置](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/vitest.project-audit.config.ts)
- [本次原始失败结果 JSON](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/results-project-audit-20260906.json)
- [真实界面检查](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/project-audit-ui-20260906.spec.ts)
- [Playwright 配置](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/playwright.project-audit.config.ts)
- [UI 失败页面记录](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/.project-audit-ui-out/project-audit-ui-20260906--8f62a-e-visible-in-the-project-UI/error-context.md)

```powershell
node node_modules/vitest/vitest.mjs run --config apps/desktop/test/review/vitest.project-audit.config.ts
node scripts/build.mjs
node apps/desktop/node_modules/@playwright/test/cli.js test -c apps/desktop/test/review/playwright.project-audit.config.ts
```

UI 检查发现现有 43191 服务时会停止，不要为了跑测试强行关闭用户进程。临时数据库和浏览器测试目录仅含合成资料；测试结果不得上传真实凭证或原始私人对话。

## 7. 推进 v0.3 到底需要什么条件

这里的 v0.3 是 [Door 设备能力感知计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_开发计划_Door设备能力感知.md)，不是当前 M3，也不是 v3.0。

### 门槛 A：工程基础真正过关

进入正式 Door 实施前：

- [ ] 本报告所有 P1 修复并独立复核通过。
- [ ] C07–C09 的遗漏结论、预算及引用问题解决，不能把不可信背景继续交给编码 AI。
- [ ] 一次完整 `verify` 通过，不是把分开成功的历史记录拼成“全绿”。
- [ ] 现有 E2E、受控扩展串联与本次新增测试全部通过。
- [ ] 六类语义场景完成真实界面＋持久化＋MCP 验证，包含非空样本及实际确认／不采纳／纠正。
- [ ] 使用包含原文、人工纠正、项目归属、工作记录的非空旧库副本完成升级／导出／恢复验证。
- [ ] 同一提交对应源码、版本、产物、哈希与审核包，C13 的交付声明对齐。

### 门槛 B：真实使用链路有证据

这些步骤需要用户参与／明确授权，不可由开发 agent 擅自登录、收费调用或安装：

| 真实验证 | 最小要证明什么 | 为什么现有测试不能代替 |
| --- | --- | --- |
| 当前 ChatGPT 网页 | 配对；普通／流式回答；刷新／重开；多标签页；草稿转正；暂停／继续。稳定内容在约定时间进入同一来源 | 当前自动化是受控网页，不代表真实站点结构始终一致 |
| 真实模型 | 六类语义场景及既定项目问答；引用正确，不把 AI 建议当作用户决定，改口优先，资料不足明确说明 | FakeProvider 预先写好了答案，只验证处理规则，不验证模型理解 |
| 真实编码客户端 | 只给项目标识与任务，由客户端读取背景／核对依据，执行获准的小任务，回写后再读取；界面也能看见 | 工具握手成功不等于真实客户端会正确使用整个流程 |
| 安装／升级 | 在获准的干净账户或虚拟机安装、自定义目录、重启、旧库升级、恢复和卸载数据保留说明 | win-unpacked 启动不是安装器全流程 |

真实模型测试先固定判据，保留成功与失败样例、模型／提示词版本与可获得的用量。不要事后根据回答修改“标准答案”。

上述完成后建议用非敏感项目连续试用 **3–7 天**，关注重复建档、漏分析、取消后请求、改口丢失和过时背景。这是沿用原计划的建议观察窗口，不自动创建监控，也不把“观察了一周”当作有缺陷仍可放行的理由。

如果真实验证条件暂时不具备，应明确标为“v0.2 候选试用版，真人验收未完成”。可以继续讨论／细化 v0.3 设计，但不应宣称上一版已正式通过并混入 Door 发布。

### 门槛 C：锁住 v0.3 的最小范围

v0.3 首版只做：**本机 Windows、经授权的只读设备报告、过期状态、有依据的规则建议、可选只读 MCP 摘要。**

开始前把以下约束变成可测试的契约：

- 检测、自动刷新、编码 AI 分享三个开关独立，默认关闭。
- 配置数据与动态读数分开，未知不填 0，过期不当实时，空闲 RAM 不当“安全可用内存”。
- 关闭／清除／退出／恢复后，旧异步结果不能重新写回；不能复用 C02 那种不分执行代次的扫描方式。
- 本机随机安装身份独立于项目备份，恢复别的机器数据不能冒充当前设备或恢复分享权限。
- 不读取私人文件正文、剪贴板、账号、硬件序列号等额外信息。
- 不做 RAM 压测、多设备分布式、手机 App、自动执行、模型下载或联网发现。

### 满足条件后，第一步开发什么

**先做 D0：设备报告格式＋FakeProbe＋身份／权限／过期测试。**

依次推进已有计划的四批：

1. D0：定义报告与规则，用假探测器证明边界正确。
2. D1：接入普通权限、低负载的 Windows 只读探测。
3. D2：设备页面与“能做什么／还缺什么”的规则说明。
4. D3：默认拒绝的只读 MCP、升级和打包复核。

每批审核后再进入下一批；v0.2 原有回归必须一直保留。设备功能本身无需云模型，不能借“设备建议”自动开始计算或下载。

## 8. 可以直接转交给修复 agent 的任务摘要

> 请基于当前实际代码，按本报告 C01–C13 分批修复。先确认 HEAD 和已有改动，不覆盖其他人的工作；不增加 Door 或其他新功能。保留所有旧审核报告和原始失败证据，复用本次独立测试，并补齐六类非空 UI 场景与旧库升级验证。修复后交付独立提交、完整 verify／E2E／打包复核结果、版本和产物哈希，更新 REVIEW_PACKET 与开发日志，并逐项列出仍需用户参与的真实网站、模型、客户端和安装验收。不要仅凭测试名称或模型自述宣布通过。

**最终放行标准：先证明 IXAEON 能可靠地记住、尊重人工决定、让编码 AI 查证，并把工作结果真正反馈给用户，再让它认识设备。**
