# IXAEON v0.2 二次复审：65927c6

日期：2026-09-07。审核提交：`65927c60f589937ba5098270537e5b853deb7a95`。

对照：[上一轮 RF01–RF09 复审报告](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.2_复审报告_2026-09-07.md)。本次只复核这轮修复及相邻操作，不重新扩张开发范围。

## 1. 结论

**大部分修复可以确认，但“RF01–RF09 全部完成”仍不能放行。剩余业务问题集中在 RF03，另有 RF09 的文案和证据记录需要收尾。**

这次不需要把九项全部重做。旧结论重提丢失、多次纠正链、预算超限、人工引用和工作记录详情等已测场景均通过，完整 verify 和现有 E2E 也通过。

剩下的关键问题，用通俗的话说：

> “这条内容还有问题，先别当成处理好了。”这件事本来已经记住了，但只要再给条目选一次项目，系统可能就把待处理标记擦掉。选项目仍然被错误地当成了解决其他问题。

本轮新增 8 项独立检查，**4 项通过、4 项失败**。失败是同一类状态问题的两种情形、各两个入口，不是四个独立模块都坏了。

## 2. 本轮实际验证结果

| 检查                                     | 结果           | 证据边界                                                                            |
| ---------------------------------------- | -------------- | ----------------------------------------------------------------------------------- |
| 当前 HEAD 完整 `node scripts/verify.mjs` | 13 步通过      | 223 项单元／集成／审核回归＋3 项真实 Electron 界面检查通过，包含 UI01／BUI01／BUI02 |
| 桌面 E2E                                 | 16/16 通过     | 包含重写后的六类 m2-ui 场景，实际有确认、不采纳、纠正及两个来源的冲突检查           |
| 扩展 E2E                                 | 通过           | 本地受控网页，不代表真实 ChatGPT 网站验证                                           |
| 真扩展→真实服务→SQLite 串联              | 通过           | 配对、刷新去重、增量、暂停／继续、草稿转正                                          |
| 当前 win-unpacked 复核                   | 3/3 通过       | 自定义目录、重启保留、搬迁后不依赖全局 Node 的 MCP 四工具调用                       |
| 本轮新增相邻场景检查                     | 4 通过、4 失败 | 单独配置运行，尚未纳入现有 verify；因此不与“现有 verify 通过”矛盾                   |

完整通过的 verify 在本机时间约 22:01–22:02 运行。前两次 UI 尝试先遇到受限环境超时，随后受本轮遗留测试进程占用 `43191` 干扰；诊断明确为 `EADDRINUSE`。确认遗留进程退出、端口空闲后，同一源码完整复跑通过。**不把这些环境干扰算成 RF06 的产品缺陷。**

新安装包已独立核对：

- 文件：[IXAEON-Setup-0.2.0.exe](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/release/IXAEON-Setup-0.2.0.exe)
- 大小：122,595,082 字节。
- SHA-256：`3169B563A5F8CF2C61823B40A23563E443A9F6AF29643F7820EBCEB434A96E97`。

与交付说明一致。本次没有运行安装器；解包产物检查不替代真实安装／升级验收。

## 3. RF01–RF09 复核状态

| 编号 | 本轮结论                                                                                                                                |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------- |
| RF01 | 已测场景通过。除 B01 外，补做三轮普通重提，全部有效结论仍在且无多余 current 副本。                                                      |
| RF02 | 已测场景通过。递归查询共用且去重，补做连续三次纠正，最新人工约束被保护、前驱保持 superseded。                                           |
| RF03 | **仍未完整修复。** 普通缺项目条目可以解除待处理，但人工显式置位和人工约束冲突又会被归属操作清掉，详见 F01。                             |
| RF04 | 已测场景通过。原临界循环和新增中文、转义、长任务、不同预算检查通过，完整 JSON 不超限，统计值正确。                                      |
| RF05 | 已测场景通过。人工创建／纠正引用返回真实记录内容和身份；旧来源撤销后不继续返回其旧依据，独立人工记录不受误伤。                          |
| RF06 | 已测场景通过。失败测试、未完成事项和详情可见，重进页面与重启检查通过；有明确加载失败分支。                                              |
| RF07 | 本轮要求的界面场景通过。已区分不采纳 AI 建议与确认“用户否决方案”的记录；S4 确实导入两个来源并验证双方保留。                             |
| RF08 | 已测路径通过。旧 plain 格式迁移／清除、普通读取拒绝 plain、提示不再承诺会话密钥均有实现与测试。真实系统异常／旧库升级仍按未验证项管理。 |
| RF09 | 主要改动完成，但须更正文案和历史证据声明，见 F02／F03。默认 verify 的核心和 UI 入口现已真实接入。                                       |

“已测场景通过”不是对全部可能输入、平台和真实使用情况的保证。用户已明确保留的未验证项不在此冒充完成。

## 4. 剩余修改事项

### F01 · P1 · RF03：归属操作仍会清除非归属原因的待处理状态

位置：

- [ItemService.recomputeNeedsReview](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/itemStore.ts:301)
- [SourceStore.bindProject 内另一套重算](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/sourceStore.ts:426)
- [提取器对人工约束冲突的标记](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/extraction/extractor.ts:293)

#### 情形 A：用户显式置位的 AI 条目仍会被清除

复现用例：`R2-01-item`、`R2-01-source`，均失败。

1. 提取普通 AI `open_loop`“等待性能测量数据”，已有项目，初始 `needs_review=false`。
2. 经已有公开业务方法 `setPendingReview(id, true)` 标为待处理，确认数据库为 true。
3. 单独给条目选择同一个项目，或给整个来源绑定同一个项目。
4. 实际变成 false。没有发生确认、不采纳或明确解除待处理操作。

当前所谓“用户显式置位优先”只对 `origin='user'` 保留旧 true。**条目由谁写出，与待处理标记由谁设置，是两件事。** 用户也可以要求 AI 提取出的条目继续待处理；不能用 origin 推断标记来源。

本例通过核心公开方法复现，不声称本轮在界面点击过一个“置为待处理”的按钮。

#### 情形 B：与人工约束相反的候选，选项目后不再待讨论

复现用例：`R2-02-item`、`R2-02-source`，均失败。

1. AI 提取日志约束，用户纠正为“项目日志可以发送到远程服务器保存和分析”。
2. 再提取相反的“项目日志不可以发送到远程服务器保存和分析”。
3. RF02 的保护正常起效：相反候选为 `current + needs_review=true`，等待用户处理。
4. 仅执行条目归属或整个来源绑定，`needs_review` 就变为 false。

原因：提取器将 `conflictsWithProtected` 编码为 `needs_review=1`，不一定把这个候选改成 `state='disputed'`。重算函数却只把 disputed 当成冲突依据，丢掉了这类真实的待讨论原因。普通 AI 两两相似的冲突检查只看 AI 条目，不能替代人工约束冲突。

待讨论列表按 `needs_review` 筛选，因此该改动会让仍未解决的内容退出待讨论；归属不应该产生这个效果。

#### 还存在实现与说明不一致

并非两个入口真正共用 `recomputeNeedsReview`：实际只有条目归属调用该方法；来源绑定在 `sourceStore.ts` 中复制了另一套规则，而且其 `affected` 查询还包含并未被本次移动的 `manual_project=1` 条目。

#### 修复要求

1. 保存或可可靠追溯待处理的真实原因：缺项目、重要结论未确认、人工约束冲突、用户显式要求等。可以使用小型字段／原因集合或等效实现，不要求另建大框架。
2. 选项目只解除“缺项目”这个原因，不顺便处理人工要求或冲突。
3. 两个入口真正调用同一规则，不仅注释写“共用”。来源绑定只更新其应处理的条目，避免顺带改掉人工单独归属条目的待处理状态。
4. 原先 B03 的普通条目归属后解除待处理必须继续通过。不能退回“一律不清 true”，也不能把全部 AI 条目强制送审。
5. 如果新增持久化字段，补非空旧数据的迁移规则和回归，不能靠清空旧库补齐状态。

验收：本轮四项失败用例转绿，原 A02／A03、B02／B03、人工确认／不采纳以及连续纠正回归继续通过；再补“明确解除待处理”和“人工单独搬到其他项目后来源重绑”的组合检查。允许采用 `disputed` 或其他明确的冲突表示，但不能让选项目成为解除冲突的动作。

这是上轮 RF03“按真实原因重算”的收尾，不是新加产品需求。

### F02 · P2 · RF09：隐私文档仍承诺了不存在的逐次界面展示

位置：[privacy-model.md 第 48 行](D:/Agent/Project/OMNIX-IXAEON析衍/docs/privacy-model.md:48)。

新文案称：“两种渠道调用前界面均展示所用来源范围。”但当前 MCP 读取接口是令牌校验、输入检查后直接调用服务返回结果；没有逐次向桌面界面展示范围／等待确认的流程。见 [localServer.ts](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main/server/localServer.ts:283)。

本地令牌与来源授权是已有保护，但不等于用户会在每次 MCP 调用前看见本次范围。不要让用户以为还有一层实际不存在的提示保护。

修改文案即可，不要求为本轮补一个新的审批弹窗系统。建议表达：

> MCP 调用由已配置的编码客户端发起，受本地令牌、来源授权和工具参数约束；IXAEON 不会在每次 MCP 调用前弹出确认。客户端收到资料后可能转发给其云模型，请只接入可信客户端。

### F03 · 交付记录：历史界面失败上下文并未完整保留

这是证据记录问题，不与 F01 等同，也不据此否定已通过的功能测试。

在本轮首次运行 verify **之前**，已检查 `apps/desktop/test/review/.recheck-ui-20260907/`：其中只有 `.last-run.json`，上一轮 BUI01 的 `error-context.md` 已不存在。因此交付说明中“原始失败错误上下文保留未动”不准确。

旧核心失败 JSON 与新 `-fixed.json` 都在，未发现该核心证据被覆盖。本轮环境诊断生成的同名错误上下文不是上一轮业务失败证据，不能混为一谈。

修改：如有真实存档则恢复并注明出处；没有就如实记为“历史界面上下文缺失，保留旧报告和核心失败 JSON”，不要重造记录。后续各轮使用不同输出目录，归档失败证据后再复跑；不要把会被 Playwright 清理的 outputDir 当成永久历史档案。

## 5. 本轮新增证据与命令

- [新增 8 项独立检查](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/recheck-round2-20260907.test.ts)
- [独立 Vitest 配置](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/vitest.recheck-round2-20260907.config.ts)
- [首次原始结果：4 通过、4 失败](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/results-recheck-round2-20260907.json)
- [重复验证结果：4 通过、4 失败](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/results-recheck-round2-20260907-repeat.json)

重复验证前，将冲突用例的前置状态断言从限定 `current` 调整为允许 `current` 或 `disputed`，避免阻碍合理修复；要求“选项目不能清除待讨论”的最终断言没有放宽。两个结果均保留。

```powershell
# 新增检查，当前预期有四项失败；修复后另存新结果
node node_modules/vitest/vitest.mjs run --config apps/desktop/test/review/vitest.recheck-round2-20260907.config.ts

# 原有完整验证与 E2E，依次执行
node scripts/verify.mjs
node scripts/test-e2e.mjs

# 候选产物复核；先确保已重建对应产物
node apps/desktop/test/review/packaged-20260905.mjs
```

新增测试当前未接入默认 verify，由修复 agent 接入持续回归。保留旧结果，修复后用新文件名记录。Windows UI 检查先确认测试端口空闲；对未知占用者不要强行结束进程，HTTP 健康检查超时也不能直接证明端口空闲。

## 6. 下一步与 v0.3

下一批工作应很集中：**修 F01，改 F02，澄清 F03，然后交回复核。不要重做已经通过的 RF01／RF02／RF04–RF08，也不要混入 Door 新功能。**

当前 v0.3 的工程门槛仍差 F01；修完后再完成已明确保留的非空旧库升级、真实安装、真实网页／模型／编码客户端验证。完整条件继续沿用 [全项目审核的准入要求](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_全项目审核与v0.3准入条件_2026-09-06.md)，不在本轮另加一套更大计划。

本轮只新增审核测试、结果和本文，没有改业务源码或已有测试。没有调用真实模型、访问真实聊天、安装软件或处理用户真实数据库。扩展脚本重建的是已核实的专用临时测试目录，其中旧合成测试数据被替换，不涉及用户资料。

**最终判断：离收尾更近了，但“待处理原因不能被选项目抹掉”这一条，必须真正统一。**
