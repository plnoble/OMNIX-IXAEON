# IXAEON V0.3 开发计划：个人内核、主动研究与最小 Agent 闭环

> 2026-09-11 历史标记：本文件原文保留。当前实施依据为 [V0.3 重整开发计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_重整开发计划_个人Agent内核与Hermes接入.md)；其实施顺序、Hermes 接入、自动记忆和无 URL 研究要求取代本文冲突部分。旧安全/数据/纠正历史与四平台等未完成项按新计划 §9.3 映射继续有效，不以旧计划压倒用户最新目标。

日期：2026-09-08。计划版本：1.0。用途：交给开发 Agent 分批实施，再交给独立审核者验收。

长期方向见 [长期愿景与开发路线图](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_长期愿景与开发路线图.md)。请先读该文件第 0—2 节，再完整阅读本计划。

## 0. 给接手 Agent 的第一条指令

不要继续默认把 V0.3 做成设备配置展示，也不要推倒已有 V0.2 重做。先完成 S0 的现状与契约准备，再按 S1—S5 推进。

本计划取代旧文件《IXAEON_v0.3_开发计划_Door设备能力感知.md》的“下一阶段主线”地位。旧文件保留，供之后的 Door 模块参考；此前可靠性、安全和实机验证要求不因此取消。

用户希望遇到疑问及时沟通。实施细节可自行选择；重大目标、接入、费用、权限、数据与范围选择不能代替用户决定。阻塞一个接入项，不必停止无依赖的隔离开发，但必须保留缺项，不能自行下调验收标准。

### 0.1 当前事实，不要误报

- 编写时 HEAD 为 `393a4d1`，包版本 `0.2.0`，迁移账本最新为 10。
- V0.2 工程修复已按既有记录验收；真实旧库升级、安装器流程、真实网站／模型／编码客户端闭环仍需按记录补齐。
- 当前有其他 Agent 的未提交模型配置、Setup／Settings、主进程契约和测试改动。本计划未审核它们；开始前检查实际状态，保留并协调，不覆盖或回退。
- 本次只编写计划和入口文档，没有重新运行功能验收。旧测试结果不能自动当成未来提交的结果。

### 0.2 开发准入与发布准入分开

S0 和隔离测试库中的 S1—S5 可以推进；不得未经批准迁移用户唯一的实际数据库、访问全部账号或安装替换日用版。

V0.3 接管真实资料、日用升级及最终发布，必须补齐 V0.2 遗留真实验证和本版新增真实验证。若只有自动化通过，交付状态写“工程完成／真实验收待完成”，不能写“全部验收通过”。

## 1. 本版做成什么

通俗目标：用户不用先挑一个项目，IXAEON 就能理解其授权资料中的目标和多个项目；能指出项目之间值得研究的关系；能主动跟进一个获准方向；能把一项批准的小任务交给实际编码工具并核对结果。

### 1.1 本版五项必须交付

1. 个人层记忆与跨项目关系，不用假项目代替个人。
2. ChatGPT、Gemini、Grok、Claude 的真实样本导入，以及多个授权项目登记；ChatGPT 已打开对话的持续增量采集保持可用。
3. 不强制选项目的个人问答与统筹建议，支持纠正、证据和覆盖说明。
4. 至少一个真实公开网络来源的主动研究闭环：关注、执行、研究、去重、提醒、暂停、重启后续接。
5. 至少一个实际编码执行入口：批准任务、派发、收回产物、独立验证、反馈入库。

“多个项目各有摘要”不等于第 3 项；“有研究按钮但无实际联网”不等于第 4 项；“生成提示词让用户手动搬运”不等于第 5 项。

### 1.2 分批但不偷减范围

- 第一批：S0—S2，个人层、输入与兼容迁移。
- 第二批：S3，个人理解和跨项目统筹。
- 第三批：S4，主动研究。
- 第四批：S5，最小执行闭环，再做整体验收。

可交付 `0.3.0-alpha.N` 内测产物，标明本次完成到哪一批；最终 `0.3.0` 不能只包含第一或第二批。某平台缺少真实样本时，如实标记该项受阻并向用户索取最小脱敏样本，不以合成结构代替真实兼容性。

### 1.3 本版暂不做，但没有从长期目标中删除

- iOS／Android／NAS 客户端、跨设备调度、RAM 压测、分布式推理。
- 全账号全历史自动抓取；四个平台所有浏览器页面的实时适配。本版先验收四平台历史输入和 ChatGPT 当前可见对话增量；其他实时接入进入明确后续清单。
- 全模型池、模型下载训练、无人值守批量安装软件。
- 自动替换 IXAEON 运行版本、自动合并主分支、自动生产部署。
- 24 小时后台服务、系统退出后监控、手机推送与外部群发。
- 任意脚本执行、任意网站爬取、大规模镜像互联网。

可以把 IXAEON 自身作为获准小任务的目标，在隔离工作区准备候选修改；本版不负责正式自我升级。这个边界不能被宣传为“已经可以自动进化上线”。

## 2. S0：先把目标、数据和入口说清楚

第一项实际开发工作不是画首页，而是建立个人范围与权限契约，并写会失败的核心验收场景。

### 2.1 现状清点

输出 `docs/v03-baseline.md`，至少记录：

- 当前提交、已存在改动和本次责任边界。
- V0.2 继承模块、验证入口、已知真实验收缺口。
- 新数据、权限、网络和执行入口如何接到现有代码。
- 旧接口和数据库如何兼容；哪些行为因新隐私边界有意收紧。

不把 README 的旧版本文字当成实现事实。先查代码与最新报告，不修改历史失败证据。

### 2.2 三份小清单

1. `docs/v03-source-support.md`：四个平台逐项记录实际格式、样本出处／版本、支持的内容、缺失项、增量能力、真实测试状态。没有公开支持的接口不得臆造。
2. `docs/v03-capability-access.md`：研究入口和编码执行入口的实际可用方式、安装版本、必要凭证／费用、授权方式、故障与取消能力。
3. `docs/v03-acceptance-map.md`：将本文 A01—A22 和真实验收 T01—T05 对应到测试、结果和缺口。实现前先建空记录，不预填通过。

编码入口优先选用户现有可用的一个工具，核实其当前正式支持的自动化方式；不要把开发宿主提供给当前对话的工具，当成 IXAEON 安装后自动拥有的能力。研究入口同理，不假设 ChatGPT/Codex 的搜索权限或订阅随软件一起转移。

### 2.3 代表性样本

先用可公开或合成的资料实现逻辑测试，再用用户批准的脱敏真实格式补兼容性和语义验证。不得扫描用户所有聊天或整个项目父目录。

代表性数据包括：

- 至少三个项目，既有可能关联的，也有应保持独立的。
- 个人长期约束、临时想法、助手建议、用户拒绝、连续纠正、跨项目话题。
- 同一对话编辑／重生成／再次导入；缺失时间、附件不可读取、格式变化。
- 外部新技术资料、互相矛盾的说法、重复转载和恶意指令文本。
- 一个安全的小编码任务，验证器故意失败、超时、取消与重启的情形。

生产代码不能按样本项目名称硬编码关系，也不能将正确答案预写入生产提示词或界面。

## 3. S1：个人层与权限模型

### 3.1 范围是独立维度

引入显式的语义范围，至少包括：

- `personal`：关于用户本人的目标、偏好、限制或跨项目原则。
- `project`：明确属于某个项目。
- `unassigned`：尚未整理，不能确定范围。

主题和项目关联采用独立关系记录。一条个人想法可以关联多个项目；一份对话可以包含多个项目的话题。不要继续用 `project_id = null` 同时表达“个人合法记忆”和“归属失败”。

保留既有项目所有者字段供兼容，新增范围和关联；关联不复制原文、不自动移动原所有者、不等于共享权限。来源整体的项目归属仅作为默认提示，片段和条目的真实范围必须可校正。

### 3.2 人工语义和历史必须继承

- 复用现有 items、evidence、corrections，避免另建一套与人工纠正脱节的个人记忆库。
- 个人目标、偏好、约束可以沿用现有类型；用户原话、AI 提取、助手建议、外部研究和执行自报保持可区分。
- 重要的推断性个人目标和跨项目建议先作为待确认候选；不能从 AI 发言提取为“用户已经决定”。
- 已确认纠正、冲突、搁置、历史、来源版本及失败保留语义继续有效。
- 显式标为 `personal` 只消除“缺少项目”的错误要求，不清除 manual、conflict、unconfirmed 等无关原因。
- `unassigned` 继续保守保留；不得批量把所有旧无归属内容宣布为用户画像。
- superseded 条目不会因改变范围、重新绑定或刷新理解而复活为待处理。

### 3.3 权限必须在后端贯穿执行

区分本地保存／使用、发送给模型、发送给编码客户端、研究查询外发、任务执行。复用和扩展现有权限机制，不在 UI 中单独维护一套宽松规则。

要求：

1. 个人范围不是“所有客户端都可见的全局范围”。旧 localToken 不自动解锁新增个人资料。
2. 逐一覆盖 `prepare_task`、不带项目的 `search_context`、直接 ref 摘录、前后文、工作结果、研究摘要和缓存；不能只改主问答。
3. 当前本机用户的个人视角可以跨授权资料检索；调用云模型时仍按外发许可筛选，不能把“在本地界面发起”视为全量云端授权。
4. 普通编码客户端默认只取得已允许共享的项目背景。需要个人约束时，用户选择具体条目或明确集合后才可分享；单次任务使用有期限、范围受限的授权，不把全局令牌塞给子任务。
5. 混合对话不得因其中一段属于项目，就外发整段私人前后文；不能可靠裁剪时拒绝该摘录，说明范围不足。
6. 从私人来源形成的摘要、关联和报告继承限制；去掉引用或改写成摘要不构成脱敏。用户可以单独批准一份审阅后的任务摘要外发。
7. 撤权立即阻止后续检索、外发和任务工具使用；进行中的晚到结果不能重新建回已删除或撤权的有效派生资料。已经发出的内容无法保证召回，应如实说明。

旧客户端仍可按既有工具格式使用获准项目功能；涉及新增个人信息的全局行为有意收紧，写入迁移说明。不能为了“兼容”让老接口泄露新数据。

### 3.4 数据迁移与导出

- 基于执行时最新迁移账本追加迁移，不修改已经发布的迁移 1—10。
- 旧 `project_id != null` 映射为项目范围；旧空归属保守映射为未整理，不扩权、不自动清待处理、不改纠正链。
- 新的关联、主题、研究、任务、权限与批准信息进入正式导出／恢复和可读导出格式，不能只留在 UI 缓存或独立不可迁移文件中。
- 恢复后关注任务暂停、旧执行许可失效、运行中任务进入待核对；不得恢复即自动联网或重做代码修改。
- 新备份标明格式版本；旧程序拒绝不支持的新格式，不静默遗漏新表。升级前备份，迁移失败可恢复旧库和 vault；不要拿唯一原库试错。

## 4. S2：多来源输入与项目目录

### 4.1 四平台历史导入

用同一规范化结构接入各平台，至少保存平台、本地账户命名空间、稳定对话标识、说话人、消息标识、父子／分支关系（若原格式提供）、原始时间、原文、版本和导入方式。

平台具体格式以 S0 获准样本和实施时正式资料为准。不保证四个平台都提供同一种官方 JSON 或公开历史 API。

- 每个平台至少支持一种实际取得并验证的导出或保存格式；不伪称所有格式均支持。
- 无消息 ID、时间或分支信息时明确缺失，采用说明过的保守去重规则，不凭标题合并。
- 附件、图片、音频未解析时保存可用元数据和缺失提示，不说已理解内容；不自动拉取附件 URL。
- 批次逐项显示成功、失败、重复、待分析和限制。坏文件不让其他已成功项丢失。
- 再导入只处理新增或变化，编辑保留旧版；不能因重提取覆盖人工约束与纠正。
- 标识包含平台和本地账户命名空间，跨账号相同标题／ID 不误合并；不要为了命名空间读取账户密码或 Cookie。

### 4.2 持续输入的真实边界

保留并真实验证现有 ChatGPT 当前已打开、可见对话的增量采集：流式完成、编辑、分支、刷新、暂停、重启和重复提交。

界面明确说明：这不是后台读取整个账号。历史资料导入一次与日常聊天自动增量是两件事；未打开的对话、手机端不可见内容等不能假称已经同步。

为其他平台保留解析／采集适配边界和接入状态，本版不扩展为通用远程浏览器或 `<all_urls>` 抓取器。后续每个平台的持续接入单独验收，不要求用户长期逐段复制聊天来维持系统。

### 4.3 多项目登记，不读取整台电脑

允许一次选择多个项目，或逐个登记，保留各自根目录和授权。每个项目建立目的、当前状态、主要输入输出、已有能力、关联目标和未知项。

先复用现有项目说明／配置快照，不默认读全部源码、密钥、构建产物。没有目录的构想也可以登记为项目或主题，不强迫创建文件夹。

目录搬迁或重命名需确认身份关联，不凭同名合并。代码更改由执行阶段单独授权，导入授权仍然只读。

## 5. S3：个人理解与跨项目统筹

### 5.1 用户可直接使用的入口

新增个人总览和个人问答，或将现有总览／问答调整为显式的“个人视角／项目视角”。不要求为了提问先创建项目。

至少能回答：

- “你目前理解我想做什么？有哪些不确定的地方？”
- “这些项目分别服务于哪些目标？是否有重复建设？”
- “这个新想法和哪些现有项目有关？”
- “结合目前资料，你建议我下一步先做什么？为什么？”

提供可操作的证据、纠正、确认、不采纳和暂缓入口，而不是只展示一段无法修改的用户画像。

### 5.2 项目关系是有状态的提案

关系至少能表达：服务于某目标、依赖、可提供能力、可复用、疑似重复、存在冲突。

每条关系包含参与实体、理由、证据、提出者、时间、状态及最近核对情况。状态区分 `proposed / accepted / rejected / superseded`；实际联通是否验证另存为验证结果，不能把 accepted 当成接口已存在。

跨项目建议必须解释收益、代价、必要前提和保持独立的替代方案。没有足够证据时提出需要核实的问题，不强行生成连接。

用户不采纳后，同样依据不应每次刷新重新催促；出现新实质证据时可以提出带说明的新版本。纠正或撤销依据后，旧关系标记待复核／失效，不继续作为确定事实输出。

### 5.3 最小统筹运行器

建立单独的、有界运行器：目标输入、授权范围、按需检索、结构化提案、工具结果、停止与任务轨迹。先单任务运行，不需要多 Agent 框架。

现有提取器继续把资料当数据处理，不给它联网或 Shell。统筹器通过后端白名单调用检索、证据读取、提案保存，后续接研究和获准任务派发。模型生成的工具名称、参数、项目 ID 与路径必须验证，不能直接执行文本里的命令。

模型适配器不支持原生工具调用时，可以用受验证的结构化动作实现有限循环；无效响应失败退出，不降级为任意脚本执行。没有模型配置时展示资料和缺口，不生成假理解。

### 5.4 覆盖、时效与预算

- 回答包含参与来源／项目、未纳入范围、新内容未分析情况、生成时间和关键依据。
- 从全部获准候选中按问题筛选，不把“全局读取最新 60 条”包装成完整个人理解。
- 限制检索、模型轮数、输入输出和最终简报预算；预算不足时说明哪些未覆盖，不静默截掉反对证据。
- 用户纠正、来源更新、撤权后使受影响缓存失效。语义总结与证据引用必须对应同一有效版本。

## 6. S4：真实的主动研究闭环

### 6.1 关注主题

用户可以从一句话创建关注主题，也可以接受 IXAEON 的候选建议。开始自动运行前展示并确认：研究问题、关联目标、可访问来源／搜索入口、允许外发的主题描述、频率、预算、通知方式。

允许搜索的查询文本需要单独考虑隐私：私人项目名、聊天原文、路径与密钥不能被模型自动塞进公共搜索。默认只使用用户确认的公开主题描述。自动发现兴趣只形成候选，不偷偷开启监控。

### 6.2 最小真实联网方式

实现一个可替换的研究来源适配器，至少能读取用户批准的公开发布页或订阅源，定时发现更新并读取获准的相关正文。第一版可先围绕明确来源运行，不要求全网爬虫。

如果配置了实际可用搜索服务，可扩展发现范围；没有搜索服务就明确写“检查已配置来源”，不能说“已搜索全网”。选择需要付费的搜索或模型服务前确认费用，不借用当前开发对话的隐藏权限。

网络边界：

- 仅允许配置批准的协议和目标，默认公开 HTTPS；禁止 `file:`、本地回环、私网、链路本地、云元数据地址及非预期端口。
- DNS 解析与实际连接均约束目的地址，重定向每一跳重新校验；不只检查 URL 字符串，覆盖 DNS rebinding 和 IPv6 等绕过情形。
- 后续访问本地模型的专用端点不与研究 URL 权限混用，不能从网页发起内网探测。
- 无 Cookie、登录模拟、任意附件下载或网页脚本执行；设置响应大小、总时限、跳转次数、类型和并发上限。
- 失败、登录墙、robots／站点限制、格式变化如实报告；不绕过访问控制。
- 不将网页内容当成工具调用授权。只保留必要证据片段、链接和内容指纹，尊重来源使用限制，不建立无限全文镜像。

### 6.3 研究结果需要有用

每份发现／研究记录区分：

- 新信息是什么，来源链接、来源类型、取得时间、发布／事件时间（未知就留空）。
- 是发布方声明、第三方实测、独立交叉核实，还是本机实验结果。
- 与用户哪个目标、项目或问题有关；这里哪些属于 IXAEON 的推测。
- 是否值得行动、理由、限制和下一步小实验。

外部事实进入研究知识，不直接成为用户偏好或长期决定。研究报告可以提出项目关联或任务候选，未经批准不直接改代码。

### 6.4 本版运行默认值

以下是可调整且需在首次启用时展示的产品默认值，不是服务能力或费用承诺。配置集中管理，测试可覆盖。

- 自动关注默认关闭；用户启用后默认每 24 小时检查一次，支持立即检查和暂停。
- 仅在应用运行时调度；电脑休眠或应用退出期间不执行。恢复后错过的周期合并为一次检查，不连续补跑所有旧轮次。
- 全局最多一个研究任务运行；每主题每轮最多 10 个页面，若有搜索服务最多 2 次查询。
- 每个请求最多 20 秒、2 MiB 响应、3 次重定向；每轮最多 5 分钟、3 次模型请求，模型输入输出另设上下限。
- 自动研究的付费预算未配置时不发起付费调用；不可计价时不得声称受金额硬上限保护，需用户选择明确的请求／用量上限模式后才启用。禁止无限预算和静默换服务。
- 有实际计价依据时记录估算和实耗差异、预留最大调用成本；超预算停止，最多一次有界重试，重试计入预算。
- 通知先用应用内消息；同一主题、同一实质变化只通知一次。仅内容排版改变或重复转载不反复提醒。
- 来源失败不能更新“检查成功／没有变化”时间；界面显示失败原因与上次成功时间，持续失败形成去重故障提示。

计划、检查水位、运行状态、发现记录和去重键落库。重启后核对租约和状态，取消／暂停／撤权使用运行代次使晚到结果失效。测试中使用可控时钟，不依靠真实等待一天。

## 7. S5：把获准任务交给一个真实编码工具

### 7.1 先实现一个端到端入口

根据 S0 的能力清单接入一个用户实际拥有、正式支持的编码工具调用方式。选择由可用性、取消和权限能力决定，不为凑数量同时接所有工具。

不能仅生成可复制提示词，也不能仅模拟一次 MCP 读取就声称 IXAEON 已调度执行。IXAEON 必须实际派发任务、跟踪状态并取得结果。工具缺失、未登录、额度不足时返回可操作的缺口；用户授权未齐时只跑模拟接口，真实验收保持未完成。

### 7.2 任务批准是具体的

每次执行前展示：任务目标、选定项目、明确可修改范围、隔离工作区、交给执行器的背景、工具及可能使用的云服务、资源／费用上限、允许的验证命令、停止方式和成功判据。

批准绑定任务版本、目标提交／快照、上下文摘要及权限范围；任务或范围变化后旧批准失效。网页、模型返回值、MCP 调用不能替用户批准。

本版默认只允许在单个项目的独立工作区或安全副本执行；一次真正跨仓库修改需要单独范围与批准，不因为统筹关系而连带授权其他项目。存在未提交用户改动时先选择安全起点，不自动 stash、reset 或覆盖。

### 7.3 执行隔离不能只靠提示词

- 使用工具本身可核验的沙箱／权限限制，并限制父进程环境、传入凭证、路径与可用工具；独立工作区本身不等于安全沙箱。
- 不把 IXAEON 全局密钥、数据库或全局 MCP 令牌交给执行器；仅发当前任务许可的背景和短期受限访问。
- 目录边界使用真实路径验证，防符号链接／junction 逃逸。禁止访问未获准仓库和私人数据目录。
- 子进程使用固定程序与参数数组，不拼接不可信 shell 字符串；Windows 后台窗口隐藏，限制输出与时间。
- 默认不允许额外联网、依赖安装、提交推送、正式发布和主工作区合并。编码工具必要的模型连接与其他网络访问区分授权；执行方式不能落实约束时不得宣称已隔离，需更换方式或标记受阻。
- 测试命令同样是代码执行，必须在隔离与批准范围内，不能因为名字叫 test 就任意运行。
- 本版全局最多一个编码任务；默认 15 分钟可调整上限，超时停止本次子进程树，不杀用户已有进程，不自动重跑可能已经产生修改的任务。

### 7.4 结果与验收

记录至少区分：草案、等待批准、排队、执行中、待验证、待用户接受、完成、失败、取消、状态不明。

- 执行器自报只进入已有 work_runs 或关联记录，不能直接标为用户验收通过。
- 验证器核对实际改动范围，在候选环境运行批准的检查，保存原始退出码和产物引用。执行器不能通过改写自己的测试报告替代检查。
- 测试代码也被修改时要明确披露；保留独立的验收场景，不能删除失败断言让自身通过。
- 没跑测试写未运行；失败／部分完成如实显示，下一次上下文仍能看到。
- 用户接受候选结果后可更新任务状态和经验，但不自动合并、部署或替换运行版本。接受结果与批准上线是两件事。
- 应用重启遇到不明执行状态，先核对运行和产物，不盲目再派发。重复请求幂等，同键不同任务拒绝；取消后晚到成功回报不覆盖取消状态。

## 8. 实现落点与工程约束

沿用 Electron、TypeScript、SQLite、现有共享契约、IPC、本机服务与 MCP。先模块化，不新增 Redis、微服务或整套 Agent 平台。

下列为仓库相对路径，根目录为 `D:/Agent/Project/OMNIX-IXAEON析衍`；文件分组可按实际结构细化：

- `packages/contracts/src/`：范围、关系、研究、任务和权限契约；所有跨进程输入校验。
- `packages/core/src/db/migrations.ts`：只追加迁移，覆盖旧库和新备份。
- `packages/core/src/storage/needsReview.ts`、`itemStore.ts`、`sourceStore.ts`：统一范围、人工语义与重提取行为。
- `packages/core/src/storage/askStore.ts`、`search.ts`、`mcpStore.ts`、`access.ts`：统一检索与外发过滤，避免局部放开个人数据。
- `packages/core/src/import/`、`projects.ts`：真实格式适配与多项目登记。
- `packages/core/src/extraction/`：版本化提取与模型适配，保留不执行资料指令的边界。
- 新增 `packages/core/src/personal/`、`research/`、`orchestration/` 等小模块：关系与个人视角、研究适配、任务状态与白名单工具；不强制这些具体命名。
- `packages/core/src/jobs/jobQueue.ts`：复用持久任务机制，明确研究可重试与代码执行不可盲目重试的差别，避免长任务阻塞日常采集。
- `packages/core/src/storage/archiveStore.ts`：新增资料进入导出恢复；恢复后的许可和任务按安全默认处理。
- `apps/desktop/src/main/`、`preload/`、`renderer/`：同一后端规则驱动个人、研究、任务界面。
- `apps/mcp/src/`、现有本地服务：旧工具兼容与新范围过滤；增加接口须有实际用户场景，不把管理权限暴露成普通 MCP 工具。
- `apps/extension/src/`：保持现有站点范围和暂停语义，不借机扩大为全网站采集。
- `scripts/verify.mjs`、测试和 docs：新增回归、真实验收记录和如实的网络／隐私说明。

研究引入联网后，更新旧“除模型 API 外无外网请求”的声明和安全测试；改为验证允许的研究目标与禁止行为，不能直接删除无外联测试而不补边界测试。

提示词、模型和配置变更需版本记录。继续保留已有来源引用校验、完整序列化预算、人工记录摘录标识和项目工作记录可见性。

## 9. 自动化验收：A01—A22

每项必须有可运行测试与实际断言；涉及界面的项目需真实 Electron 行为验证，不以数据库断言代替按钮和持久化。必要时一项拆多个用例。

### 个人与输入

- **A01 无项目也能开始**：空项目列表时导入个人资料、形成个人候选、个人问答，不强迫创建“我”项目。
- **A02 范围与待处理原因**：个人范围不产生 no_project；未整理仍保守；改变范围只清自己负责原因，manual／conflict／unconfirmed 及搁置保留。
- **A03 历史纠正可靠**：连续纠正、重提取、改范围、来源重绑和重启后，人工结论不丢、旧 superseded 不复活、无关条目不变。
- **A04 角色与观点**：助手建议、外部研究、用户拒绝和用户决定分开；未知不写成用户目标，候选不自动确认。
- **A05 四平台解析与重复**：每平台格式样本、再次导入、缺失字段、编辑／分支、错误格式都有明确结果；跨平台／账号不误合并，不能声称理解未解析附件。
- **A06 增量与覆盖**：ChatGPT 可见采集、刷新去重、暂停、版本差和失败覆盖；页面说明与真实范围一致。
- **A07 多项目授权**：至少三个目录／构想登记，未选目录、敏感文件、符号链接逃逸被拒，导入不获得执行权。

### 统筹与隐私

- **A08 正反关联**：生成有证据的候选关系；对不该关联的项目保留独立判断，不靠项目名称硬编码，不自动移动资料或修改仓库。
- **A09 关系生命周期**：确认、不采纳、纠正和新证据到来行为正确；重复建议不骚扰，accepted 不冒充已联通，依据失效不继续当事实。
- **A10 个人问答覆盖**：混合多个项目、个人目标和约束按需检索，有未知、冲突、时效和预算说明，不能只取最近一小段冒充完整理解。
- **A11 全入口隐私**：新旧 MCP、无项目搜索、直接 ref、前后文、摘要、研究查询、任务背景和缓存均不泄露未分享个人资料；撤权期间的晚到结果不得绕过，链接项目不构成授权。
- **A12 迁移与恢复**：非空迁移 10 库副本升级到最新且幂等；新表导出可读、恢复完整；旧格式兼容；失败回滚；恢复不自动联网、不复用执行批准。

### 主动研究

- **A13 真实调度结构**：创建关注、启用、立即检查、按时执行、暂停、恢复及重启水位；用可控时钟验证错过周期合并、并发上限、取消晚到结果。
- **A14 发现与去重**：新发布形成发现卡，重复抓取／转载／排版变化不重复通知；无变化与读取失败不能混淆。
- **A15 研究证据质量**：链接和摘录可核对、发布日期与抓取时间分离、矛盾证据保留；外部事实不污染用户偏好，本机未测不称已验证。
- **A16 网络与注入边界**：私网／回环／元数据／非法协议／重定向／DNS 变化／超限输出被拦；网页诱导上传资料或运行命令无效。
- **A17 预算与故障**：未启用无请求、未知付费无自动调用、达到上限停止、重试计费、禁止偷偷换服务；失败可见且不产生成功水位。

### 执行与端到端

- **A18 批准绑定**：无批准、过期批准、修改后旧批准、错误项目与越界路径均拒绝；限权与任务上下文在后端生效。
- **A19 执行隔离**：实际执行适配器的取消、限时、凭证过滤、工作区限制和输出限制有效；模型文本不直接拼接命令，未获准联网／安装不发生。
- **A20 结果不冒充验收**：执行器报告成功但独立测试失败时不通过；未跑保留未运行；失败和未完成项进入界面和下一次背景，用户接受不触发部署。
- **A21 幂等与重启**：重复派发、同键不同内容、取消后回报、重启中断和不明状态不导致重复代码执行或错误完成。
- **A22 全链路可见**：资料导入→个人理解→跨项目提案→关注研究→批准任务→收回结果→反馈记忆；重进界面和重启后仍可查看证据、权限与各阶段真实状态。

## 10. 真实验收：T01—T05

模拟数据验证规则，真实环境验证可用性，两者都需要。真实测试只使用用户批准的脱敏资料、账号、费用和隔离目录。

- **T01 四平台与真人理解**：每个平台一种实际格式完成导入；至少三个真实项目形成理解。用户核对目标、约束、AI 建议和一条跨项目判断，并纠正一项错误，重启后仍以纠正结果为准。
- **T02 日常持续输入**：真实 ChatGPT 网站中新增、编辑或重生成、刷新、暂停／恢复，核对同一来源及正确版本；清楚说明其他平台实时接入未在本版承诺。
- **T03 实际公开网络研究**：用户授权一个真实关注方向和来源，正常路径联网产生有依据的报告。无新消息时如实无变化；更新与去重可另用受控测试源证明，不伪造真实网站发布。若仅检查指定来源，界面不能声称全网搜索。
- **T04 实际编码工具派发**：IXAEON 主动调用真实可用客户端完成批准的小任务，取得实际 diff／产物与独立验证输出；不由人手工把提示词搬过去代替。结果回到 IXAEON，失败与未完成清晰可见。
- **T05 升级与安装**：用批准的真实旧库副本升级，核对数据、权限、纠正、来源、备份与恢复；运行真实安装器安装／覆盖升级／重启，测试新增模块。不能用 win-unpacked 启动代替安装器测试。

其中 T01 的关系是否有价值、建议是否符合用户意图，需要用户判断；测试不能替用户认可产品方向。

## 11. 默认验证入口与交付材料

保留当前 `scripts/verify.mjs` 全部有效回归，新增加的个人／权限／研究／任务核心和必要 UI 测试纳入默认验证。行为因本计划明确改变时，说明替换原因并保留等价不变量，不能直接删除旧失败用例。

每一批先运行受影响测试，交付前完整执行现有 verify、桌面 E2E、扩展 E2E、串联测试和新增端到端检查。使用仓库已有 `package:windows` 流程生成候选包并复核实际产物。

每批至少提交：

1. 本批代码、迁移和测试；提交边界清楚，不混入他人的未提交修改。
2. S0 三份清单及基线记录更新。
3. `docs/dev-log-v03.md`：本批内容、关键选择、用户确认、未完成与下一步。
4. `REVIEW_PACKET.md` 新增本批章节：提交标识、环境、测试命令、退出码、结果路径、失败／未运行、真实验证范围。
5. 必要截图、脱敏研究证据、脱敏执行记录及安装包路径／大小／哈希（实际打包时才填写）。

测试输出采用独立运行目录／时间标识。修复前失败结果只读归档，修复后另存；不要复跑清理旧 outputDir 后再声称失败证据保留完好。私聊全文、API Key、令牌、Cookie 不入仓库或报告。

交付结论必须分别写：实现完成度、自动化结果、真实结果、用户是否接受、未完成项。实现和测试是证据，不自动等于审核者批准上线。

## 12. 必须沟通的情况与默认选择

默认选择：沿用现有技术栈；桌面应用运行期间工作；自动研究关闭；不新增付费服务；执行先单工具、单任务、隔离工作区；正式升级保持用户确认。

遇到以下情况提问，不擅自替换目标：

- 缺某平台真实样本：说明具体缺什么格式，只索取必要脱敏片段。
- 研究入口或编码工具不可用：列出已检查方式、可行替代及所需权限／费用；可以继续 Fake 适配测试，但保留真实交付缺口。
- 数据模型需破坏性迁移、旧能力无法兼容、需要合并多个仓库或新增外发范围：先给影响与备选方案。
- 本阶段工作量需要继续拆批：提出拆分，不自行删除研究或行动闭环。
- 用户想法存在关键矛盾：引用已读依据并问具体选择，不用过去 AI 的建议替用户决定。

## 13. 本版最后的验收问题

请现场回答并演示：

> 导入我允许的聊天和三个项目后，IXAEON 是否能理解我想做什么，提出一条有依据、可纠正的跨项目建议？对于我批准关注的方向，是否会自己检查外部变化并给出有用研究？我批准一项小任务后，是否能实际调用编码工具、核对结果，再把经验留下？

如果只做到单项目简报、个人项目列表、研究界面或可复制提示词，本阶段都没有完整完成。

这是一版能开始认识、研究、统筹和行动的底子，不是完整终局。更广泛的聊天自动接入、Door、模型池和受控自我升级，按长期路线继续成长。
