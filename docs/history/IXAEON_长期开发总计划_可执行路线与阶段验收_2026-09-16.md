# IXAEON 长期开发总计划

副标题：从当前代码，到能理解我、统筹事情、调动所有终端并受控成长的个人 Agent。

日期：2026-09-16。版本：1.0。编制时 HEAD：`08c770e`，已发布迁移截至 23。

**这是供开发 Agent 连续执行的总路线，不是实现完成声明。接收本文件不等于获得全部私人资料、付费额度、设备权限或正式升级批准。本轮仅编制计划，没有对 `08c770e` 进行新一轮独立验收。**

## 0. 给接手 Agent 的第一条指令

不要重新发明 IXAEON 的目标，也不要只写更多规划。读取本文件与 AGENTS 后，核对最新提交、未提交改动、审核和证据，从最近一个尚未满足验收条件的工作包开始实际开发。

当前起点是 **P0：可信基础复核与补齐**。开发方已提交 `08c770e` 并声称修复 RR01–RR08；须验证真实行为，不预先当成失败，也不能凭其提交说明当成已验收。通过后立即推进 P1 的真实桌面路径。已有正确实现复用，不重复建设。

本文件给出长期顺序；近期 P0/P1 的具体要求继承 [M0–M2 施工单](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_下一阶段开发计划_目标驱动个人Agent闭环_2026-09-15.md) 和 [V0.3 重整总计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_重整开发计划_个人Agent内核与Hermes接入.md)。旧安全和真实验收条件不因总计划变长而降低。

## 1. 最终产品是什么：一个人、多个目标、所有获准终端

IXAEON 的主角是用户，不是单个项目、某种模型、某个聊天网站或一台电脑。

最终用户应该可以这样使用它：

1. **“我照常聊天，你逐渐理解我。”** 获准的 ChatGPT、Gemini、Grok、Claude 对话和 IXAEON 自身对话进入同一资料体系，日常支持的增量自动进入，不要求每次手工转发。没有接入的范围明确显示。
2. **“换个窗口，你记得相关的事；换个话题，别硬扯。”** 买手机、做一次 PPT、长期项目和生活偏好互不混淆。普通理解自动形成，用户只需自然纠正。
3. **“看看我的项目哪些能互相帮助。”** 理解各项目的目的、现状和能力，提出具体复用建议；不相关的可以独立，不强行把一切变成 IXAEON 的插件。
4. **“这个方向你帮我持续关注。”** 自己找公开资料、核对变化、判断是否有用，无实质变化就安静；值得行动时给出研究或改进建议。
5. **“把这件事办好。”** 在获准范围内选择模型与工具、组织任务，调用 Codex、Claude Code 或其他执行器，交回真实产物和独立验证，而不是只给一段提示词。
6. **“这台手机、NAS、旧电脑能帮我做什么？”** 根据设备报告和获准实测，说明能承担哪些工作、当前是否可用、有什么限制；合适时主动建议利用已有资源。
7. **“你发现自己哪里不好，就研究怎样改善。”** 从真实失败找问题，提出方法/代码候选，做固定对照和回归，展示收益与风险；正式软件升级由用户确认，失败可回退。

“持续成长”指这些能力可不断扩展，不是无限资源、无限权限、永不出错，也不是必须训练模型权重。知道自己的覆盖缺口、判断某项新技术不值得采用，同样是成熟能力。

## 2. 当前起点与文件关系

### 2.1 已知起点

- 仓库有可继承的桌面、来源/原文、SQLite、项目、授权、纠正、提取检索、MCP、研究、编码任务和 Runtime 适配基础，不是空白项目。
- 最近独立审核基线为 `f71da15`，报告发现 RR01–RR08；随后 `08c770e` 有新修复。**新修复的独立验收状态：待复核。** 本文不把旧失败强行套在新提交，也不替新提交盖通过章。
- 开发记录中的“测试全绿”“M1 完成”必须分别核对实际入口、真实服务和用户行为。合成测试、Node 子进程、真实模型、真实桌面、用户接受是不同证据。
- 当前迁移版本为 23；下一位 Agent 开工仍要读实际账本，只追加新迁移，不改写已发布版本。
- Gemini/Grok/Claude 真实样本沿用用户 2026-09-13 暂缓决定，不反复索取；兼容性缺口如实列出，用户实际使用时再补验证。

### 2.2 哪份文件管什么

| 文件 | 用途 |
| --- | --- |
| 长期愿景与开发路线图 | 用户目的与不能丢失的长期能力 |
| 本总计划 | P0–P6 推进顺序、阶段产物、依赖、验收与接力规则 |
| 架构决策记录 ADR | 为什么选某技术、什么证据可以促使改变 |
| V0.3 重整总计划 + M0–M2 施工单 | 近期具体规格，继续约束 P0/P1 |
| 最新审核、REVIEW_PACKET、开发日志、验收映射 | 实际做到哪里、证据和未完成项；不是愿望清单 |

冲突处理：最新用户明确意图/授权优先；新事实可以推翻旧实现判断；重大取舍写 ADR 并说明影响。不得无记录缩小目标、降低验收、扩大费用或数据范围。历史原始思想、旧计划和失败证据保留，不覆盖。

## 3. 架构：自己的核心，可更换的发动机和工具

保持当前 Electron / TypeScript / SQLite 基础，渐进改进，不为这份计划整仓重写。

| 层 | 它负责什么 | 它不能决定什么 |
| --- | --- | --- |
| IXAEON Core | 原文、记忆、目标、项目、权限、任务账本、经验、版本和恢复 | 不凭模型一句话扩大用户授权 |
| Agent Runtime 适配器 | 让 Hermes 等引擎思考、调用工具、接收结果、继续行动 | 不另建用户资产的唯一副本；不绕过 Core 执行 |
| 工具/执行器适配器 | 搜索、网页读取、文件操作、Codex/Claude 等编码能力 | 不自己批准任务，不以自报成功代替验收 |
| 模型路由 | 在获准提供方中按任务、隐私、质量、成本选模型 | 不因失败静默改用未批准云服务或更贵模型 |
| Door | 报告设备、执行本设备获准任务、回传状态与产物 | 不是另一个独立演化用户画像的“大脑” |
| 交互入口 | 桌面、后续手机/网页/消息入口中的对话、批准、进度和结果 | 不靠隐藏按钮或前端判断承担真正安全边界 |

### 3.1 要保留的技术选择

- Hermes 是首选可替换 Runtime，锁定版本，通过实际协议适配；不全面 fork，不并行重造通用 Agent Loop。现有 Core 降级路径如实标识，不冒充 Hermes 通过。
- Codex 是首个编码执行器，Claude Code 等在真实需求出现时按同一契约接入；选择 CLI、API 或正式工具接口以可控性和可验证性为准，不依赖脆弱界面点击作为首选。
- 原始资料/vault 与 SQLite 是权威；Markdown 是可读保存/导出表示；LanceDB 是首选待评估的可重建语义索引。不是把资料在三处各改一遍，更不是先做一次全量迁库才能使用产品。
- 执行结果通过真实 MCP/所选正式协议回到引擎；本地事件日志不是工具响应。适配器可以换，用户资料、任务、权限和经验不能跟着丢。
- 这些是当前选择，不是宗教。采用新依赖前核对当时官方文档、许可证、维护情况和真实接口，做小规模兼容验证；不得凭旧聊天或宣传臆造 API。

### 3.2 从现在起统一的约束

每次执行关联用户目标/会话、服务端可信 runId、调用者与受众、来源范围、授权版本/有效期、预算、取消代次和幂等键。身份不能由模型参数自报；“数据库里有某个 running 行”“某项目存在一条权限”都不等于当前调用被授权。

使用现有任务和运行账本扩展这些关系，不另做一套平行任务系统。具体表和类型先对照现有代码，必要时新增迁移；本计划描述的是契约，不要求按名词创建同名微服务。

## 4. 总路线与依赖

| 阶段 | 用户得到什么 | 主要前置条件 | 完成后去哪里 |
| --- | --- | --- | --- |
| P0 可信基础 | 成功、权限、预算和停止值得相信 | 最新代码与历史反例可复现 | P1 |
| P1 可日用的个人 Agent | 理解不同话题、统筹三个项目、主动研究、真实办事、复用经验 | P0 相关边界通过 | V0.3 准入；随后 P2 |
| P2 受控自我升级 | 自己研究不足、准备改进、测试，用户批准后升级和回退 | P1 真实执行与独立评测 | P3；也为之后设备/模型升级提供基础 |
| P3 持续资料入口与长期在线 | 更多平台和文件自动进入；有获准在线节点时持续工作 | P1 数据恢复与权限可靠，部署另获准 | P4，能力质量持续改善 |
| P4 Door 多终端能力感知与行动 | 知道每台设备能做什么，能派一件真实工作 | 稳定任务/身份/恢复协议 | P5 |
| P5 模型池与资源调度 | 统筹云、本机、手机、NAS 的实际可用模型与工具 | Core 模型政策；远端能力依赖 P4 | P6 |
| P6 持续成长的个人系统 | 多任务协作、长期目标、能力评估和更高授权内主动性 | P1–P5 的真实质量与治理基础 | 持续迭代，不追求空泛“最终完成” |

这是优先级，不是所有工作都只能串行：例如 P1 可做本机单模型适配，P3 可提前修现有增量入口，P4 可做不碰真实设备的协议测试。但不能让这些旁支挤掉 P1 的真实交付，也不能把依赖未通过的功能投入日用。

P0/P1 继续属于当前 V0.3 重整及准入工作。后续阶段不预先绑定版本号或虚构交付日期。根据实际完成能力定版本，不能用 0.2.x 维护包号规避新内核的发布验收。

## 5. P0：不再只修到旧测试变绿

用户可见结果：合法任务确实能做；不合法的做不了；显示完成的确实完成；说停止就不再继续新动作。

### P0-A 复核最新修复，建立真实起点

1. 检查当前 Git、迁移、锁定 Runtime、默认 verify 与报告范围，不覆盖其他人的改动。
2. 复跑最新独立反例及有效历史回归，另存新结果。区分正向对照、负向对照和“复现故障”的诊断测试，不能将三者都算功能通过。
3. 对 RR01–RR08 做相邻输入检查：同项目但不同任务、另一个活跃会话、过期授权、并发预算、不同无关命令、迟到回报。验收的是规则，不是某句测试字符串。
4. 将真实缺口写回验收表。已修好的不用重复重写；未修好的进入下列工作包。

### P0-B 可信评测，而不是判断命令里有没有 assert

建立由 Core 管理的评测请求，绑定：候选与方法版本、原失败运行、项目、受控副本、固定输入/用例、独立判据版本、预算和批准。

旧方法与新方法实际执行同一组任务；可信验证器比较产物和行为。没用到候选、没复现旧失败、改了题目或判据、只返回退出码零，均不得成为“改进已证实”。候选不能修改验收答案或权威结果。

必须同时有：已知缺陷被判失败、有效修复通过、无关成功程序不通过、修改方法后旧成绩失效。先做一个小而真实的对照，不上大规模自动优化框架。

### P0-C 最终执行处的权限、预算和数据受众

- 内部 Hermes、外部 MCP 客户端、桌面用户、后续 Door 各有明确身份；通用配对令牌只证明是谁，不自动赋予所有工具能力。
- 搜索、读取网页、云渲染、模型研读、编码、结果读取都在实际执行前核对当前授权。正在运行的其他会话不能替本次调用授权。
- 预算在调用前原子预留，结果返回后核算；并发、重试、失败都不能穿透上限。取消/撤权后晚回结果不继续执行或写入。
- 任务状态与任务正文可分级展示。目标、验证输出、代码摘要和路径不是天然公开信息；读取任务也需匹配项目/运行/受众及撤权状态。
- 合法受限调用必须可用，不能把所有功能永久拒绝作为修复。前端缺少新绑定参数时补好正常流程，不把其必失败的诊断测试称为已完成。

### P0-D 隔离、数据恢复和运行清理

- 编码与验证使用可信创建的工作副本，不回退应用数据目录/原项目；核对任务版本、批准、范围、路径替换和链接越界。
- 引擎失败、超时、取消、应用退出均结束本轮拥有的进程和资源；用本地合成进程树验证，不只断言 kill 被调用。
- 网络正文有流式大小、总时长与重定向限制；空结果、失败正文和配额错误如实返回，不冒充已研读。
- 失败来源、候选、运行以明确主键/关系保存，兼容旧记录，不混装 id/client_ref/逗号字符串。
- 新迁移只追加；先用合成旧库与获准真实副本测试，保留历史与源库。备份/恢复不能静默扩大授权或重新派发不明任务。

### P0 出口

原 M0/NP01–NP06 与 RR 风险按行为关闭；默认门禁覆盖新回归；每条安全拒绝都有合法成功对照；缺真实环境验证项如实标注。安全高风险未关闭不扩大真实数据或无人值守使用。

如果同一类风险连续两批只是换关键词、换字符串修补，暂停该模块扩展，先解释数据/控制流和根因，做契约级修复。不要再提交一张“100% 全绿”表替代解释。

## 6. P1：先让它成为能用的个人 Agent

P1-A 对应原 M1；P1-B 至 P1-F 对应原 M2 与 B2–B5。最早交付 P1-A，不能等全部资料和设备接齐才展示产品。

### P1-A 桌面说一个目标，真正办成一件事

工作内容：

1. 在现有对话入口显示理解、找资料、等待批准、执行、验证和完成进度；取消可用，结果可展开查证。
2. 真 Runtime 自己通过正式工具通道读取获准 Core 背景、搜索与研读，给出有来源的方案。用户不用准备全部网址。
3. Agent 生成行动批准卡：目的、项目/非项目范围、文件范围、外发背景摘要、服务、预算、成功条件。用户可以缩小范围或拒绝；修改后旧批准失效。
4. 批准后真实调用编码执行器，使用真实受控副本；独立验证产物，不以 CLI 退出 0 或自报通过代替。
5. 原会话收到结果、实际 diff、验证和不足；用户接受与部署分别处理。需要补充任务时承接原目标，不要求用户重述背景。
6. 建立目标/会话与任务的持久关联，重进/重启能查询；重复派发、取消后的迟到结果、崩溃后的未知状态均有正确处理。

第一例可以是聊天导出记录去重/排序或已有项目的小功能。背景可以是明示合成资料，但引擎、搜索、编码和 UI 调用必须真实；不要只写 note.txt，不要让 FakeCodingExecutor 预制答案替代真实执行。

验收：正常、边界、错误输入三类独立检查；一个已知错误版本确实失败；完整记录从桌面发起到结果回原对话。再验证拒绝批准、范围变化、执行取消和重启。一次真路径通过后立即演示，不继续堆工具名称。

### P1-B 自动且有分寸的记忆

资料分三层：原始历史；某次事件/对话的临时事项；可复用且有适用范围的理解。它们关联，但不互相冒充。

- 新增资料自动排队处理，有覆盖率、进度、暂停、幂等和失败重试；不要求用户逐条点击分析或确认。
- 临时要求留在事件里，稳定偏好按相关性使用。旧会议不变成今天待办，讨论一次手机不自动长期监控。
- 用户原话、AI 推断、外部事实、明确决定、实际执行结果分开标识；自动记住不等于自动获准行动。
- 自然纠正能限制范围或替换当前认识；同会话、新会话、重启后都生效。保存原证据和纠正链，不伪造历史。
- 无关问题可以取零条个人记忆。允许多个独立兴趣，不维护一份不断强塞进所有对话的巨型画像。
- 普通理解不进入“需要你拍板”；真正冲突、重要决定、权限和用户主动标记才进入。保留旧人工标记/搁置/否决含义，不批量清空伪造减负。

验收沿用已有门槛：至少 60 个平衡场景，开发集与独立验收集分开；真实模型三轮；相关事实正确率 ≥90%、无关不侵入 ≥95%、临时要求不升长期约束 ≥95%。每类、每轮分别报告；权限拒绝、证据 ID 与纠正链必须全部正确。不为凑绿删除难题或事后降阈值。

### P1-C 多资料入口与三个项目统筹

先跑实已有 ChatGPT 历史导入和可见对话增量，验证新增、编辑、重生成、暂停恢复、重复导入与撤权；明确浏览器/登录/可见范围。其他三平台格式适配保留合成测试与未实测标签，真实样本按用户原选择后补。

项目登记不要求把所有对话先塞进某个项目。允许个人、主题、项目和暂未归类并存；导入目录不等于允许整仓库外发。文件索引排除密钥、构建缓存等无关内容，取用时核对范围。

三个项目验收：一组确有复用价值，建议说明目的、接口、证据、维护代价；另一个不相关，保持独立。用户否决关系后重启不再强推。项目的“已完成”来自代码/工作记录或明确说明，不从一句设想推断。

### P1-D 主动研究，不是收藏网页

首次启用一个方向时确认：公开主题、可外发内容、服务、频率、预算、通知和实验范围。之后自己拟查询、选择公开来源、比较变化；正常范围内不逐 URL 请用户批准。

将研究产物固定为：新事实与来源/日期、相对旧认识的变化、与用户/项目的关系、适用条件、不确定性、建议行动或不行动。不同主题分开上下文，网页指令不是授权。

来源可以是用户关注、获准领域内新发现、已有失败暴露的问题。重复结果去重，无重要变化不提醒。读取失败说明缺口，不编造“没有新进展”。暂停/撤权/预算耗尽必须真正停止新调用。

起始产品上限继承总计划：每轮 3 次查询、10 页、8 次模型调用、5 分钟；每方向默认每日最多一轮；重试计入预算。云渲染单独授权/计数，模型另限 token/费用。它们是待用户开启时展示的上限，不是本文件发放的额度。

验收：真实公开搜索与研读一次；受控变化源验证新增/不变/去重；到期调度、暂停恢复、重启、离线、限额和通知都验证。仅应用运行时可执行就明确说明，不能宣称已全天在线。

### P1-E 方法真正进入下一次工作

把失败记录或明确纠正转为改进候选，使用 P0 的固定对照，用户批准适用范围后启用。下一次同类任务必须实际使用被批准的方法版本，并记录选择与执行证据。

验收包含：方法前后对照、一个未用于修改方法的新例、一个不适用任务、撤销后不再使用。只在列表查询到“approved”不算复用，保存一段模板也不叫学会。

### P1-F V0.3 准入与小范围试用

- 原 B0–B5、R01–R15、NP01–NP12 对照证据完整，不能因重新命名阶段丢掉旧欠项。
- ≥10,000 条合成混合消息测索引/查询/重建、P50/P95、内存/磁盘和 UI；模型耗时另算。完成 LanceDB/关键词适配比较；嵌入未获准则保留未验收，不静默外发或删除语义检索目标。
- 非空旧库副本升级/幂等/恢复、源库不变、安装器隔离安装和覆盖升级、重启与卸载数据策略有记录；停用引擎后 Core 仍能查询/导出。
- 用户小范围试用，分别反馈是否记错、是否啰嗦、研究是否有用、是否真的少做了手工步骤。用户未试用不填“用户接受”。
- 发布包绑定代码/配置/迁移版本、依赖与哈希；首次接管日用数据、真实安装/升级仍需批准。

P1 的完成标志不是“功能页面齐了”，而是用户能正常聊天、它用对记忆，能研究和办事，而且知道自己的限制。

## 7. P2：受控自我升级，把“自己进化”做成工程能力

用户可见结果：“我发现自己处理这类文件不够好，研究并准备了新版。这是对比和风险，你批准后我升级；出问题可回退。”

### P2-A 改进队列来自真实问题

复用现有失败/研究/任务资产，形成改进记录：影响用户的现象、复现材料、出现次数与范围、当前版本、可比基线、研究依据、预计收益/成本和优先级。不是模型随口说“我可以更强”。

候选可以改 Skill、提示方式、检索策略、工具描述、模型选择、研究流程或实际代码。不要把所有改进都当成换模型，也不要把知识更新说成训练了权重。

重复问题归并，已否决方案记录理由和重新考虑条件。网上新版本只是候选输入，必须判断是否解决本机真实问题。

### P2-B 研究、实验与独立比较

在已批准研究/实验范围内自主调查公开资料，保存原始依据、版本、许可证和适用条件；外部仓库/网页作为不可信输入处理，不直接安装执行其指令。

调用编码执行器在隔离分支/副本生成候选，候选包含基线版本、diff、配置/依赖变化、数据迁移、测试、资源消耗与回退条件。扩大范围或预算先请示。

评测包括固定失败案例、未用于优化的新例、旧能力回归、权限/预算/恢复测试，以及速度/费用代价。定义收益指标后再运行，不能根据结果换指标。评测版本和答案不能由被评测候选修改。

没有改善、质量退步、不可复现或成本超标时，保留证据并拒绝升级。独立审核可以发现新问题，开发 Agent 不得自行把该审核状态改成“通过”。

### P2-C 候选安装与版本批准

用户批准的是具体候选：代码/包身份、配置、权限变化、数据迁移、评测与回退方案。获批后改动任何关键内容需重新批准。

准备独立更新执行路径，不让正在工作的核心进程随意改自己的生产文件。候选包使用可信来源与完整性/身份校验；单独有哈希不能等同于证明发布者可信。

先做无数据库迁移的低风险升级，再做兼容迁移；更换 Runtime 或模型政策使用同样的候选、对比、批准和回退流程，不偷偷追 latest。

### P2-D 升级、健康检查、回滚与数据

升级前检查任务队列，暂停/排空在途写入，备份数据库与必要配置，记录版本和恢复位置。候选启动后验证库可读、授权未变、任务不重复、关键工具可用。

健康失败时回退程序和相容数据。新版本运行后产生的新资料不能在回滚时静默丢失：需要可恢复的写入/变更记录或明确的数据恢复步骤；数据库不兼容时不能简单把旧 exe 启起来假装回退完成。

验收至少覆盖：获批正常升级、无批准拒绝、包被替换拒绝、候选启动失败回退、升级中断恢复、带迁移的副本演练、回滚后新增资料保全。最后才申请日用版本的一次真实升级。

P2 出口：至少一次从真实不足到候选、对比、批准、隔离安装、健康检查与失败回退的完整演练；一次获准正式升级另列证据。没有正式批准也能完成开发和隔离演练，但不能填“已生产升级”。

## 8. P3：持续资料入口、长期统筹与在线节点

用户可见结果：不用经常搬运资料；自己的信息持续进入并保持可查；在获准在线主节点上能持续研究和跟进。

### P3-A 从“支持导入”变成“知道接入到哪里”

为每个连接器保存：平台/账号命名空间、授权范围、采集方式、同步游标、最后成功时间、覆盖区间、缺失内容、失败原因和撤销入口。

按用户实际使用优先顺序逐个平台交付，不要求先拿到四个平台所有账号。先验证官方导出/正式接口或已授权可见页面路径，再做可靠增量；登录或页面变化时如实受阻，不偷偷绕过保护。

接收资料保留作者、角色、消息/分支 ID、来源时间、获取时间、编辑版本和附件状态。相同内容的重复同步不复制记忆；不同事件中的相同句子不被错误合并。

同步默认不意味着立刻把全部历史交给云模型精读。先保存/索引，后台按预算理解；按当前任务补读，并显示已处理覆盖。长历史成本可预估、暂停和分批推进。

文件夹/项目入口增加增量索引、移动/删除/改名跟踪、排除规则。撤销来源授权立即阻止后续取用，同时处理检索缓存、活跃上下文和派生副本；用户删除与撤销外发是不同动作。

### P3-B 可靠检索和个人知识，不是更多摘要

改善跨来源同义表达、文件名/数字精确检索、时间/否定/纠正、附件/文档索引。授权文本、图片、音频或办公资料按需要逐类接入解析能力，不自造完整 Office 编辑器。

知识与用户意图分开：论文观点、网上教程、助手建议不能自动当成用户的偏好和决定。每条可用结论有来源、适用范围、时间、置信状态与可纠正入口。

扩容按真实需求测试：从 10,000 条到 100,000 条合成消息，再到用户获准规模；记录检索质量、延迟、重建、磁盘、后台资源和迁移代价，不承诺固定硬件上无限容量。

### P3-C 一个可恢复的在线主节点

先将现有 Core 的任务调度/服务与桌面显示职责分清，避免另建一个不一致的后端。需要常开服务时，再根据用户设备与许可选择主节点；迁往 NAS/服务器需实际支持与安装批准。

持续任务有持久队列、租约、预算、超时、失败重试、熔断与下次时间。主节点重启后对账，错过周期合并，不集中补发大量收费请求。多个入口共享事实，不各自启动同一任务。

增加至少一个获准的远程交互入口：先做到查看结果、批准/拒绝、暂停，不一次接齐所有消息软件。远程命令必须鉴别用户、设备和重放，凭证不出现在对话或日志。

### P3-D 长期目标管理与低打扰

目标可并存，有状态、优先级、限制、依赖、等待原因和复查时间。Agent 可建议下一步和复用关系，不自行合并项目或改变用户优先级。

研究提醒、任务失败、待批准和可选周回顾分开。用户不需要每天确认同一件事；安静不是隐藏持续故障，长期失联/失败应按既定通知策略提示。

P3 出口：至少一个资料连接器完成真实连续增量和恢复；新增平台各自真实验证，不由合成格式代替。在线节点先做建议不少于 72 小时的获准稳定性观察，包含重启、断网、配额耗尽、安静去重及恢复，保留实际时长；未部署常开节点不能称 24 小时在线。稳定性观察不要求开发 Agent 在对话中阻塞等待，应使用届时可用、获准的监测机制。

## 9. P4：Door，让每台设备成为可理解、可调用的能力入口

用户可见结果：“手机适合做哪些事？NAS 现在是否适合索引资料？这台旧电脑能否承担一个后台任务？”回答来自设备资料与实测，不是凭型号猜测。

### P4-A 最小设备身份与生命周期

先接一台用户选定的真实子设备，不同时开发所有平台完整客户端。建立配对、设备独立身份、能力清单、心跳、在线/离线/受限状态、撤销和最小更新协议。

通信经过身份认证与加密；不默认把主端服务裸露到公网。远程访问方式依据用户网络和平台约束再做小规模验证，不在本文假定已有服务器、隧道或系统权限。

Door 只接收该设备允许的任务和必要数据，不复制全部私人记忆或主端密钥。断线缓存和任务产物有访问限制、保留期与清理规则；撤销设备后不再获得新资料或任务。

### P4-B 安装后先认识设备，不自动压榨设备

分三档：

1. **静态资料**：平台可提供的系统、CPU/GPU、总内存、存储、网络、可用模型/工具；看不到就写未知。
2. **低负载状态**：获准读取的可用内存、负载、电量、充电/温度等；每项有采集时间、可见性与过期规则。
3. **主动实测**：内存压力、模型加载、推理速度、持续负载测试。单独同意，限定时长/资源，随时取消，触发异常阈值自动停止。

用户曾提到的 RAM 压测属于第三档，不是安装后的默认动作。系统限制、后台时长、内存回收、平台分发要求在真实设备上验证；不能承诺手机永远在线、任意后台执行或与 NAS 自动拼成一块内存。

### P4-C 能力报告与任务建议

报告分别记录“硬件声明”“实测结果”“当前可用性”“获准范围”。对可承担任务给出依据、预计时间/资源、质量限制和最后测试时间。设备升级、温度/电量变化或模型变化后相关评估过期。

建议必须联系实际任务，例如把索引放到获准的空闲节点、让手机作为批准/采集入口；没有适合工作时不为了展示多设备而强行派任务。

### P4-D 真派发、断线与恢复

从一个低风险任务开始，记录租约/幂等键、输入范围、输出位置、取消和结果校验。断线不能盲目重派有副作用工作；先查询原任务状态，不能确认时标未知并按策略处理。

P4 出口：一主一子真实配对；设备资料可过期；一次主动实测可停止；一件真实任务完成并回到原对话；断线/撤销/取消/重复请求均验收。之后再接第二类设备。旧 [Door 模块计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_开发计划_Door设备能力感知.md) 作为设计输入，若调整保留低负载、撤权和实测原则。

## 10. P5：模型池与资源调度

用户可见结果：不必每次自己挑所有模型；系统在允许范围内根据工作选择资源，并能解释“为什么这次用了它”。

### P5-A 先分清模型、发动机和设备

模型是推理能力，Runtime 是组织思考/工具循环的执行程序，Codex 等是编码执行器，Door 是设备入口。不要把它们放进一个列表就声称能互换。

模型登记至少包含：提供方/节点、实际版本、支持的模态与工具协议、可用上下文、用量/价格可得性、速度与质量测量、隐私范围、额度/并发、健康状态和最后核验时间。兼容某种 API 不等于工具调用或结构化输出质量合格。

### P5-B 先两种资源，再扩大覆盖

第一批用一个已获准云模型和一个本机模型，或两种用户已有可用资源，验证摘要/抽取与复杂研究的任务选择。随后接 Door 上的手机/NAS 模型；不为了凑类型要求用户购买设备或服务。

按任务需要选模型：短提取、检索重排、复杂研究、编码、图像/语音等有不同评测，不能只用一张通用跑分表。记录必要的输入/输出格式与适配降级；不支持的能力不伪装支持。

### P5-C 隐私、质量、成本一起决策

先过滤不允许接收本次资料或不支持任务的资源，再在剩余资源中比较质量、时延、成本和当前可用性。隐私限制是硬条件，不是一个可以被低价格抵消的评分。

失败回退也必须属于本轮预授权。额度不足时缩小任务或请求选择，不能偷偷换云端、加钱或转发完整聊天。订阅服务没有精确金额就记录真实可得次数/用量和限制，不伪造费用。

提供可解释记录：选了谁、候选为何不选、实际用量、结果质量与回退。用户可固定某类任务的资源、禁用云端或调整预算。

### P5-D 多设备先分任务，不先分一个模型

优先并行处理独立工作，如索引、摘要、不同研究方向、互不冲突的候选测试。所有结果回到同一 Core，保留来源与版本，不能各节点各写一份用户事实。

跨设备分布式推理单列实验：确认框架、平台支持、内存/带宽、延迟、耗电、失败恢复和相对单机收益，再决定是否产品化。收益不成立可以不采用，不能将“联网设备很多”说成“大模型已经能跨设备运行”。

P5 出口：至少两种真实资源在相同任务集合中有对比；一次正确选择、一次授权内故障回退、一次隐私/预算禁止回退；远端节点离线不重复任务；用户可看懂资源选择。增加适配器不能破坏已有模型能力与权限。

## 11. P6：持续成长、长期目标与更高主动性

用户可见结果：它能独立推进已授权的研究和工作，跨多个项目寻找可验证的改进，逐渐减少需要用户干预的机械步骤。

### P6-A 分工协作，不复制多个“大脑”

按实际瓶颈引入研究、编码、审核、文件等专门执行角色。它们使用 Core 的任务与受限上下文，拥有明确职责、预算、输出契约和取消能力；共享的是经授权的事实，不是全部私人资料或凭证。

多个角色协作必须比单执行器有可测收益，且计入协调成本。研究/开发执行者不能批准自己的升级；不为展示架构而先造几十个 Agent。

### P6-B 持续能力评估与平台管理

平台管理首先是可追溯的版本、健康、质量和兼容记录，不是能随意删除/更换引擎的万能管理员。

发现新 Runtime、模型或工具时，先匹配已有不足；在相同任务、预算和隐私限制下比较，再提出采用、升级、保留或淘汰建议。通过 P2 的候选、批准和回退实施，记忆与任务资产继续属于 Core。

### P6-C 主动程度逐步提高

提高自治必须有具体领域、动作、预算、期限和停止条件。先自动做低风险研究，再在用户预授权的隔离范围内准备候选；不要用一个“无限自主”开关解除所有权限。

针对某项获准目标可提出计划、跟进依赖、发现停滞、建议实验与复盘。目标冲突、个人重大选择、新费用、新数据外发和正式升级仍由用户决定。

定期衡量：实际办成率、无效打扰、错误记忆、改进是否有效、资源成本、用户返工量。持续改善这些指标，而不是追求调用工具次数或生成候选数量。

P6 出口是滚动里程碑：每次增加一个范围明确、可撤回的主动行为；对旧行为做回归，出现质量/费用异常自动降级到已验证策略并说明。不存在可以永久取消验收的“毕业版本”。

## 12. 贯穿全程的产品和工程要求

### 12.1 一个事实来源、一条任务链

同一件事能从用户目标查到任务、所用背景、授权、工具调用、产物、独立检查、用户接受和后续经验。记录实际行为与依据，不要求保存模型隐藏思维链。

规划、排队、执行器自报、验证通过、用户接受、已部署分别记录。未知/失败/部分完成是合法状态，不能改名凑完成。状态转换和重试幂等，重要动作写可追溯审计。

### 12.2 数据保全、删除和权限传播

保存来源、角色、时间、分支和编辑历史；索引可重建，不能比权威数据更高权限。新增授权不自动追溯开放全部历史；撤销后从查询、引用、缓存、活跃会话、任务结果和设备副本传播。

密钥不进入日志、提示词、备份明文或设备能力报告。备份/恢复说明密钥与操作系统绑定的限制；恢复后不能静默扩大权限或继续旧批准的动作。已发往外部的内容不能承诺自动收回。

跨设备离线编辑/纠正发生冲突时保留版本并明确处理规则，不以后到覆盖方式悄悄抹去用户决定。正式数据恢复需保护升级后新增资料。

### 12.3 可靠性与资源可见性

每项外部能力有超时、取消、流式输出上限、重试、预算与真实降级状态。资源吃紧优先保证前台交互，后台任务可暂停和恢复。

代码、引擎、模型、配置、索引、数据迁移和 Skill 各有版本，可查是哪次组合产生结果。更新失败有回退或诚实受阻路径；兼容测试不通过继续用已验证版本。

### 12.4 不把外部资料当控制指令

网页、聊天、仓库、插件说明和工具返回都是待判断输入。它们不能新增授权、读取密钥、改评测规则、安装依赖、发送消息或部署。不同入口必须共用强制边界，不能只在提示词写“不要越权”。

新的工具和技能先检查来源、许可、需要的权限与实际执行，再启用；外部依赖变化用锁定版本和兼容测试管理。

## 13. 怎样让 Agent 连续开发，而不是每一步都停下来问

### 13.1 开发任务授权与产品运行授权分开

用户将本文件作为开发任务交给 Agent 后，在其实际工具权限允许的范围内，可连续进行：仓库读取、约定范围内实现、合成测试、隔离副本验证、文档与证据更新、必要的局部重构。无需每个函数、每次测试都再次请示。

本文件不会自行授予：读取未指定账号/日用库、上传全部聊天、支付/无限用量、安装系统组件、修改原项目/其他设备、发送外部消息、正式部署或升级。

| 情况 | 正常处理 |
| --- | --- |
| 同一已批准范围内修缺陷、完善测试 | 继续做，记录改变与结果 |
| 范围/费用不变的实现细节调整 | 自行判断，重要取舍记 ADR |
| 缺一个真实服务/设备授权 | 一次列清最小需求，只阻塞依赖它的验证，其余安全工作继续 |
| 新外发、付费、设备压测、正式升级或跨项目写入 | 说明范围、预算、风险与替代办法，请用户决定 |
| 更换主架构、删除能力、降验收阈值 | 提交证据与影响，不能自行用容易的目标替换原目标 |
| 某阶段通过且下一批在既有授权内 | 保存接力记录，继续下一批，不再重写整套计划 |

把真实验证需要的条件集中成一份启用单：可用提供方/模型、资料范围、目标副本、次数/token/金额上限、设备/安装范围、通知方式。配置有 Key 不等于预算无限；已明确有效授权不反复索取；无法确定则问具体问题，不要求用户重讲整个构想。

### 13.2 阶段门槛不是无限停工，也不是自动发布

完成一个工作包就保存差异和证据。未解决高风险边界不能进入相关真实使用；可以继续不依赖它的隔离实现，但阶段总状态仍标部分完成。

独立审核未做时写“开发验证通过，待独立审核”，不要自己代签。用户接受未发生时写“待试用”。普通下一包开发不需要每次用户批准，生产切换与风险范围扩张仍需具体同意。

## 14. 验收方法：防止再次为了题目而写答案

### 14.1 每个能力必须有三类证据

1. **合法成功**：从实际入口用合法授权把事做成。
2. **错误拒绝/失败**：拒绝越权、取消、错误产物和无效证据；已知坏版本必须测出问题。
3. **相邻变体**：换文字、数据、任务/项目、会话、时间或并发顺序仍正确，不依赖原测试某句话。

不要把拒绝所有请求视作安全完成；不要靠命令字符串中有没有 `assert/fs/test` 推断有无有效检验；不要让测试直接写出答案，再宣称 Agent 写出来了。

真实执行验收需“实际入口 + 实际引擎/服务 + 实际产物 + 独立检查 + 回到用户”。截图可辅助，但不能替代运行记录；本地真 Node 只证明那条检查真实运行，不证明上游模型/编码也真实。

### 14.2 测试资产与证据管理

自动默认回归不消费真实账号额度；真实测试独立入口与授权。历史 checked、失败上下文和原始评分保留，修复另存；审核夹具自身出错如实记录并纠正，不算产品缺陷。

测试定义和评测结果版本化。业务实现不依赖测试名称/字符串；可以因更好的架构调整测试接线，但需要等价行为说明，不删除困难断言。独立验收集不能被优化器当训练答案读取或改写。

### 14.3 每批交付记录

沿用 REVIEW_PACKET、开发日志与验收映射，不再建立相互冲突的“总进度真相”。近期映射原 NP/RR 编号，后续每阶段建立同样的证据索引。

```text
阶段 / 工作包：
基线与交付 commit（未提交则注明差异）：
用户这次能从哪个入口做成什么：
实际改动与迁移版本：
已实现：
自动化通过（命令、退出码、结果路径）：
真实环境通过（引擎/服务/运行 ID、授权与用量）：
独立审核状态：
用户接受状态：
失败、缺口和未验证：
回退/恢复方式：
下一工作包与依赖：
需要用户决定的最少问题：
```

没有证据的字段明确写未验证。业务代码完成、自动化通过、真实环境通过、独立审核、用户接受、生产发布分别报告。

## 15. 防止长远目标消失的覆盖表

| 用户要的能力 | 最早实际落点 | 后续扩大 |
| --- | --- | --- |
| 所有获准聊天与资料逐渐进入 | P1-C 的现有入口和覆盖状态 | P3 逐平台持续接入、附件/文件与规模 |
| 自动理解，不逐条批改 | P1-B | P3 长历史质量；P6 长期目标 |
| 跨窗口记忆，但不串话题 | P1-B 与连续会话验收 | P3/P6 多入口及长期变化 |
| 理解所有项目、判断联系 | P1-C 三项目正反例 | P3 能力登记；P6 多任务统筹 |
| 自己找新项目/技术并判断价值 | P1-D | P2 改进研究；P6 平台评估 |
| 真正操作 Codex/Claude 开发 | P1-A 首个真实执行器 | P2 自升级；按需要增加执行器 |
| 自己发现不足、学会更好方法 | P1-E 固定对照与复用 | P2/P6 改进队列和持续评估 |
| 用户批准后正式自我升级 | P2 | 后续所有能力/引擎升级共用此路径 |
| 不开主电脑也能持续跟进 | P3 获准常开主节点 | P4/P6 远程入口与设备接续 |
| 安装 Door 后知道设备能做什么 | P4 静态/低负载/可选主动测试 | P5 实际资源调度 |
| 云、本机、手机、NAS 模型池 | P1 先保留模型政策和适配边界 | P5 真实多资源；P6 自动评估 |
| 多设备协作和更高主动性 | P4 一件真实远端任务 | P5 独立任务分配；P6 授权内持续工作 |

这张表是能力去向，不是完成清单。任何一行长期没有推进，下一批要解释原因与接力条件；不能悄悄删掉，也不能为了凑全一次性全做。

## 16. 交给开发 Agent 的直接开工指令

下面这段可以与本文件一起交给开发 Agent：

> 请按《IXAEON 长期开发总计划：可执行路线与阶段验收（2026-09-16）》实际开发，不要只给建议或重新写一份宏观计划。
>
> 先读取 AGENTS、长期愿景、ADR、当前施工单和最新审核；检查最新 Git 与迁移版本，保留其他人的改动。编制时 HEAD 为 08c770e，它的修复声明尚待独立复核，不按提交说明自动判通过。
>
> 从 P0 开始，用合法成功、错误拒绝和相邻输入验证可信评测、任务工作区、MCP 运行授权/预算、结果受众、云渲染与停止；发现根因就实现修复，不继续追加原反例关键词黑名单，不把 F03 的“UI 被拒绝”诊断当成功功能。
>
> 相关边界通过后立即完成 P1-A：真实桌面对话 → Core 背景 → 真研究 → 用户批准 → 真编码 → 独立验证 → 结果回原对话。随后继续 P1-B 至 P1-F，交付有分寸的自动记忆、多项目判断、持续研究和可验证的经验复用。
>
> 按工作包连续推进已授权的安全开发、测试和文档，不需要每个小步骤都询问。每批保存实现/自动化/真实验证/独立审核/用户接受/未完成证据；缺真实权限时一次列出最小需求，其余安全工作继续，但不冒称阶段通过。
>
> P1 达标后按 P2–P6 逐阶段推进。未来阶段开工前结合实际环境细化本阶段工作包，不重写或缩小用户目标。未经具体批准，不外发私人资料、不购买或扩大服务额度、不安装系统组件、不写原项目或其他设备、不改唯一日用库、不正式发布升级。

## 17. 本次规划交付说明

本文件把现有长期愿景展开为 P0–P6 的可执行路线，保留当前 Core + 可替换 Hermes 架构、M0–M2/B0–B5 的验收与用户授权边界。没有因新计划而取消旧欠项，没有宣布最新修复已经通过，没有安装模型/引擎、测试真实账号、修改业务代码或发布升级。

**开发的第一步是从最新代码验证并修好可信执行基础；第一个用户可见里程碑是真正办成事；长期终点是覆盖用户获准资料、项目、模型和设备的个人系统。三者同时写清，才不会越做越小，也不会一开始摊成一个做不完的大工程。**
