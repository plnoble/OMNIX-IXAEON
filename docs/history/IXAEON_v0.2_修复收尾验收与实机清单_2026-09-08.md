# IXAEON v0.2 修复收尾验收与实机清单

日期：2026-09-08。审核提交：`b8f216b8d89e3d1a5f8c8e6d1fe841853b8ba4f7`。

对照：[三次复审 N01–N02 报告](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.2_三次复审报告_2026-09-08.md)。本次只复核修复及紧邻的数据边界，不重新扩张开发范围。

## 1. 验收结论

**N01、N02 均通过，可以关闭。本轮未发现需要继续返工的阻断性代码问题。**

之前发现的工程修复项至此可以收尾。当前适合标记为：**v0.2 工程修复验收通过，候选试用版；真实环境验收待完成。**

通俗地说：纠正后的旧内容现在能退出待讨论；“暂不处理”确实能把内容放到可找回的位置，重启后还能恢复，而且没有把未解决的问题偷偷当作已经解决。

下一步应该是实际安装、升级和使用，不是继续叠加一轮大重构，也不是立即混入 Door 发布。这里的通过针对本轮审核范围与已测场景，不是对所有平台、数据和模型行为的无限保证。

## 2. 本轮实际运行结果

复核在本机时间约 13:21–13:26 完成，以下均为本轮独立执行结果。

| 检查                           | 结果       | 说明                                                                             |
| ------------------------------ | ---------- | -------------------------------------------------------------------------------- |
| 完整 `node scripts/verify.mjs` | 16 步通过  | 239 项单元／集成／审核核心检查，加 6 项真实 Electron 检查，共 245 项             |
| 原 N01／N02 核心检查           | 5/5 通过   | 包含原 L01、L02 失败及确认／不采纳、迁移对照，已计入 verify                      |
| 原 N01／N02 界面检查           | 3/3 通过   | LUI01、LUI02 转绿；LUI03 实际验证搁置→重进→重启→恢复，已计入 verify              |
| 本轮新增独立补查               | 5/5 通过   | 迁移命中范围、事务回滚、重开数据库、完整原因保留、历史查询边界                   |
| 桌面 E2E                       | 16/16 通过 | 包含现有六类语义资料界面场景                                                     |
| 扩展 E2E                       | 通过       | 本地模拟网页，不代表真实 ChatGPT 网站                                            |
| 真扩展→桌面服务→SQLite 串联    | 通过       | 合成数据；刷新去重、暂停、草稿转正等已有流程通过                                 |
| win-unpacked 产物复核          | 3/3 通过   | 自定义目录、完整重启保留、搬迁后无全局 Node 的 MCP 四工具与持久化回写            |
| 打包文件与本次重建文件比较     | 全部一致   | app.asar 内 6 个桌面 JS／CSS／HTML 文件与重建结果逐字节一致；随包 MCP 入口也一致 |
| 新增审核代码检查               | 通过       | ESLint、TypeScript 均通过                                                        |

未用合成测试冒充真实模型能力，未用 win-unpacked 启动冒充安装器验收。

## 3. N01／N02 关闭依据

### N01：纠正完成后，旧条目退出操作队列

[correct()](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/storage/itemStore.ts:165)在同一事务内清空被替代旧项的原因；Inbox 通过可选筛选排除 superseded，历史查询仍能找到旧项。

[Migration 10](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src/db/migrations.ts:284)只清理 `superseded + needs_review=1` 残留，没有改写 Migration 9。除原 L04 外，本轮补了真正包含历史残留的 v9 合成库：

- 应清理的两条历史行正确退出待处理；current、disputed 和原本已退出的历史行保持不变。
- 条目数量、旧内容、新旧关联和纠正记录保留；重复运行迁移不再改变数据。
- 纠正中途因无效项目导致写入失败时，旧项状态及全部原因一起回滚，没有“新内容没写成，旧问题先被清掉”的半完成状态。
- 纠正、重新归属、关闭再打开数据库后，旧项不重新成为待处理；其他尚未解决的冲突没有被连带清除。

原 L01、L02、LUI02 均通过，关闭 N01。

### N02：暂缓与解决已分开

[Inbox 搁置／恢复操作](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/renderer/src/pages/Inbox.tsx:46)复用已有 `shelveItem`；[已搁置区](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/renderer/src/pages/Inbox.tsx:145)提供可见的找回与恢复入口。确认、不采纳、搁置、恢复均有错误反馈分支。

LUI03 已实际走过重进页面、关闭应用、重新启动及恢复。它验证了界面状态、待处理标记和未确认身份。本轮再用核心测试逐字段比较，确认 `no_project,unconfirmed,conflict,manual` 四种原因在搁置→重开→恢复后全部保留，只有搁置时间与更新时间按操作变化。

原 `setPendingReview(false)` 仍只解除 manual，不用清空原因来冒充暂缓；原 F01-a 回归也通过。关闭 N02。

## 4. 交付物与证据

候选安装包：[IXAEON-Setup-0.2.0.exe](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/release/IXAEON-Setup-0.2.0.exe)。

- 大小：122,598,699 字节。
- SHA-256：`1DC8A631A7BCAD4D0FFC592E65C1F95A2F8C477277CFA3E2505742CF98B230F6`。

与交付说明一致。包内文件比较针对当前 win-unpacked；安装器本身尚未执行，因此不据此宣称已验证安装器释放文件和安装后的全部行为。

本轮新增证据：

- [5 项独立补查源码](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/closure-b8f216b-20260908.test.ts)
- [独立配置](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/vitest.closure-b8f216b-20260908.config.ts)
- [实测结果 JSON：5/5 通过](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/test/review/results-closure-b8f216b-20260908.json)

这 5 项使用独立配置，尚未纳入默认 verify；建议保留为后续回归，不需要为此再开启一轮功能返工。原 N01／N02 的 8 项核心与界面检查已纳入默认 verify。

本轮未修改业务源码、未提交代码。此前 `-checked` 失败 JSON 和 UI 上下文保留，未被本次复跑覆盖。扩展／串联脚本重建了已核实的两个专用临时测试目录，替换的是旧合成测试数据，不涉及用户资料；历史审核报告未改写。

## 5. 下一阶段：完成真实环境验收

以下沿用既定门槛，不是新增功能需求。应在用户确认测试环境、资料范围及必要费用后执行，不要直接拿唯一一份真实数据试升级。

### 第一步：先确定候选版本和安全测试位置

固定本报告对应的提交、安装包及哈希。在获准的干净账户／虚拟机或隔离测试位置操作；对非空旧库先做可恢复备份，使用副本测试，保留原件和原版本。

### 第二步：完成三组验收

| 验收组             | 最少做什么                                                                                                                            | 通过时应留下什么                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 真实旧库升级与恢复 | 使用含原文、人工纠正、项目归属、授权及工作记录的旧库副本升级；重启后查阅；再验证既有导出／恢复能力                                    | 旧版本、候选版本、升级前后关键记录与纠正链对照、恢复结果；不能只给一个“启动成功”                                         |
| 真实安装／升级     | 运行候选安装器，测试首次安装、自定义目录、重启、覆盖升级；核实卸载的数据保留行为                                                      | 实际安装后的版本、路径、行为记录和失败情况；不能用解包版代替                                                             |
| 真人完整使用       | 用非敏感资料，在真实 ChatGPT 网页采集；让真实模型处理既定六类语义样本；让真实编码客户端自行读取背景、查证依据、执行获准的小任务并回写 | 对话进入同一来源、刷新不重复、暂停有效；纠正优先、引用可核对；回写结果能在界面看到。记录实际模型／客户端及成功和失败样例 |

涉及真实账户、外部模型调用或安装时需要相应授权。验收记录尽量脱敏，不把密钥、令牌或完整私人对话放进审核仓库。

### 第三步：再推进 v0.3

**现在可以继续整理 v0.3 方案；正式推进 Door 实施，仍应先完成上述真实验收。** 不把“工程检查全绿”等同于“已经证明真实使用可靠”。

条件满足后，继续已有 [v0.3 Door 开发计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_v0.3_开发计划_Door设备能力感知.md)：先做 D0 的设备报告格式、假探测器和权限／过期测试，再接本机 Windows 只读检测。首版仍不做 RAM 压测、多设备分布式、手机 App 或自动执行。

完整准入要求保留在 [全项目审核报告](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_全项目审核与v0.3准入条件_2026-09-06.md)。历史报告中的失败结论对应历史提交；本次修复状态以本文为准，不删除过去的失败记录。

**最终判断：本轮修复验收通过。接下来的主要工作，是用真实安装和真实使用证明它，而不是继续扩大修复工程。**
