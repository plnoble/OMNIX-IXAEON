# IXAEON v0.3 开发计划：Door 设备能力感知

日期：2026-09-05。用途：交给编码 agent 实施，完成后进行独立审核。

## 0. 这份计划接在哪一步之后

上一份 [v0.1.1 → v0.2 开发计划](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON_下一阶段开发计划_v0.1.1到v0.2.md) 解决的是“可靠收对话、记项目、给编码 AI 背景、接收工作结果”。

这份定义的是它完成之后的下一阶段，**不替代上一份计划，也不表示上一阶段已验收通过。**

写作时仓库 HEAD 仍为 `ea9da6266439cc60943a9ecc37c0e654ebd79331`。第四次验收发现的问题尚无新的修复提交证明，本次只检查文档与版本状态，没有重跑验收。

进入条件：上一阶段的稳定性问题已解决，日用闭环通过独立审核，真实网站／模型／客户端等必要验收已补齐。条件未满足时，可以讨论本计划，不应把 Door 混入正在进行的修复交付。

## 1. 通俗地说，这一版做什么

**让 IXAEON 知道自己所在的电脑有什么条件，并据此给你建议。**

用户开启设备检测后：

1. Door 读取电脑的基础配置和少量当前状态。
2. 自动交给主 IXAEON，形成可查看、可更新的设备档案，不需要用户反复手动填参数。
3. 用户查看“这台设备能做什么”，得到有依据的说明。
4. 用户评估一类项目任务时，IXAEON 指出资源条件、缺少的验证和授权；不直接替用户执行。

这一版的 Door 是**内置在 Windows 桌面端的本机探测模块**，不是手机 App，也不是独立的第二个人格。数据在当前应用内部传递，不需要新增网络配对。

示例文案，仅展示表达方式，不代表当前电脑的真实参数：

> 检测到物理内存为 32 GiB，最近一次报告的空闲内存为 11 GiB，检测于 1 分钟前。
>
> 当前应用可以继续提供已有的项目记忆功能。
> 本地模型运行能力尚未验证：空闲内存不等于某个模型实际可用的额度，还缺运行环境与加载测试。
> 本版只提供建议，不会下载模型或占用电脑开始计算。

验收重点不是一张漂亮的硬件表，而是：**知道什么就说什么，不知道的明确说不知道；没有授权就不探测、不分享、不执行。**

## 2. 这一版明确不做什么

- 不开发 iOS、Android、macOS 或 NAS 客户端。
- 不接入第二台真实设备，不开放局域网／公网监听，不增加发现、配对和远程控制服务。
- 不做 RAM 压测、磁盘测速、GPU 烤机、跑分或模型加载测试。
- 不下载模型、安装依赖、扫描软件目录或探测本机网络端口。
- 不做 OCR、语音识别、照片索引等新的任务执行器。
- 不自动运行项目脚本，不新增后台常驻服务，不在 IXAEON 退出后继续采样。
- 不扫描照片、文件正文、浏览历史、剪贴板、进程命令行或用户账号。
- 不把设备档案自动混入项目长期决定，也不自动发送给云模型。

此前 [Door 构思记录](D:/Agent/Project/OMNIX-IXAEON析衍/IXAEON-Door_设备能力感知构思.md) 中的安全内存额度、模型大小、温度、速度等是未来测量目标或示例。**没有测量就不能在本版生成这些数字。**

## 3. 第一项开发工作

先完成“设备报告的数据格式 + 只读探测接口 + 自动化测试”，再做界面。

核心接口表达以下含义即可，名称允许随现有风格调整：

```text
DeviceProbe.collect(cancelSignal) → DeviceObservation
DeviceRegistry.accept(observation, localIdentity, permissionEpoch)
DeviceAdvisor.evaluate(profile, taskRequirements) → Recommendation
```

- Probe 只负责读取，不读写项目记忆，不调用模型。
- Registry 校验并保存，不相信报告自己声称拥有的权限。
- Advisor 用明确规则解释，不执行任务，也不把缺失值猜成正常值。
- 权限来自主进程的本地设置，不来自探测结果或渲染层自报字段。

沿用现有 Electron、TypeScript、SQLite、IPC 和契约校验，不引入设备管理平台、消息队列或调度框架。

## 4. 设备报告必须分清的内容

| 内容 | 本版记录什么 | 不能推断什么 |
| --- | --- | --- |
| 基础配置 | OS、架构、CPU 型号与逻辑处理器数量、物理内存；可正常读取时记录 GPU 信息 | 有 GPU 不代表具备某个推理运行环境 |
| 当前状态 | 系统报告的空闲内存、IXAEON 数据卷空闲空间；系统正常提供时记录电池／电源状态 | 空闲内存不等于进程可安全申请的内存；没有电池不等于 0% 电量 |
| 已实现功能 | 当前安装的 IXAEON 已登记功能及限制 | 硬件条件不错不代表 OCR、语音、模型推理已经实现 |
| 用户设置 | 是否允许检测、自动刷新、编码 AI 读取 | 允许检测不等于允许读取个人内容或运行任务 |
| 依据 | 报告编号、采样时间、读取方法、成功／失败／未知状态 | 旧数据不等于设备现在的状态 |

基础要求：

1. 每个可缺失字段带状态：`known / unknown / unsupported / permission_denied / failed`，不能统一填 0、false 或“正常”。
2. 数值保存原始单位，内存与空间统一用字节；界面显示单位与换算方式一致。
3. 每项重要读数标记来源，例如 `os_reported`。本版没有压测，不得标记为 `measured_safe_capacity`。
4. GPU 型号、显存、电池等无法通过普通权限可靠取得时允许缺失；不要求安装驱动或以管理员身份运行。
5. CPU／GPU 使用率、温度、NPU、网速、模型速度、后台执行保证不是本版必填项；未实现就不显示假读数。
6. 不读取硬件序列号、MAC、SSID、用户名称或完整磁盘路径来丰富档案。显示名称默认“本机”，由用户改名。

报告具有 `schemaVersion`、`reportId`、`deviceId`、`probeVersion`、`observedAt`。主进程另记 `receivedAt`。字段、数组、字符串长度与总载荷设置上限；异常结果不能塞满数据库或界面。

建议单份规范化报告上限 64 KiB，字段错误显示局部缺失；不兼容版本或整体超限明确拒绝。不要把原始系统命令输出和任意 JSON 整包保存。

## 5. 设备身份：避免重蹈“刷新就变成新来源”的问题

设备 ID 使用本机安装身份生成的随机标识，不依靠名称、IP 或硬件指纹。

- 改名、刷新、重启、正常升级和切换项目不改变本机身份。
- 同名不合并；重新安装后若安装身份丢失，可形成新设备，不能凭名称自动认亲。
- 本机安装身份与项目数据库备份分开。导出／恢复项目资料不能把另一台设备冒充成本机。
- 旧备份带来的设备记录只能是历史记录，不能因为恢复发生在本机就自动变成“当前在线”。恢复后动态读数全部过期，需重新授权／检测。
- 恢复数据不能把关闭的检测或分享权限重新打开，也不能恢复仍在运行的旧采样器。

本版只接收内置本机 Probe 的结果，不提供让外部客户端写入设备报告的接口。多设备身份校验和配对留到下一版，不能用“知道 deviceId 就允许上报”提前搭一个不安全的入口。

## 6. 授权、刷新和停止

### 6.1 三个分开的开关

| 设置 | 默认 | 含义 |
| --- | --- | --- |
| 允许读取本机设备概况 | 关闭 | 开启前展示字段范围；不开启也能使用原有项目记忆功能 |
| 自动刷新当前状态 | 关闭 | 仅在允许检测后可开启；应用运行期间按固定间隔刷新 |
| 允许编码 AI 读取设备摘要 | 关闭 | 提醒用户：外部编码客户端及其模型可能接收到这些字段；与本地检测分开同意 |

本版不提供“允许自动执行”开关，因为执行器没有实现。不能摆一个无效开关制造能力已经存在的错觉。

关闭检测后立即阻止新采样、取消或使正在进行的采样失效；关闭分享后，所有新的 MCP 读取立即拒绝。已经向外部客户端返回的数据无法承诺收回。

旧档案可在本地查看，但必须标“检测已关闭／历史数据”，不生成基于实时状态的积极建议。提供“清除设备记录”操作并确认范围；不删除原始项目资料。历史导出文件中的副本不会因此自动消失，应明确说明。

### 6.2 更新规则

以下数值是本版的产品默认值，不是硬件性能结论，集中配置并可在测试中覆盖：

- 用户开启检测或点击“刷新”：立即安排一次低负载读取。
- 自动刷新开启时：应用运行期间每 60 秒一次；不允许上一轮未完成就叠加下一轮。
- 动态状态超过 120 秒未更新：标“已过期”；界面和建议读取时都检查，不只依靠计时器改标记。
- 应用重启、恢复备份、休眠恢复或时钟异常后：旧动态状态不能直接视为新鲜，等待重新读取。
- 基础配置在应用启动且授权有效时、用户手动刷新时更新；无需每分钟重复慢速硬件查询。

探测放在不会阻塞渲染／主进程事件循环的执行路径中。单轮设总超时，建议 8 秒；可先展示已取得字段，其余明确标失败或未知。

如使用子进程，只调用固定的只读白名单命令，参数不得由设备名或用户文本拼接；隐藏窗口、限制输出、设置超时，只能终止本轮创建的子进程，不得终止用户进程。

### 6.3 保存与晚到结果

- 动态数据只保留最新有效快照；基础配置变化最多保留最近 30 份历史，不每分钟无限追加完整报告。
- 相同 reportId 重试幂等；相同 ID 不同载荷拒绝；较旧报告不能覆盖较新报告。
- 关闭检测、清除记录、退出或恢复后，旧异步结果不能重新写回。使用运行代次／权限版本等可验证机制处理。
- 清除记录同时关闭检测与分享，用户重新开启前不得自动建回。删除报告后，引用它的建议缓存同时失效。
- 不长期保存含敏感字符串的原始错误。日志只写原因码、耗时、字段状态和必要的内部引用。

## 7. 用户可以看到什么

新增一个简单的“设备”页面，至少包括：

1. 当前设备名称、操作系统、配置摘要。
2. 检测和分享的授权状态，刷新、停止及清除入口。
3. 最近一次成功检测时间；正在读取／部分失败／已过期等真实状态。
4. 每项关键数据的依据，能从建议点回相应报告字段。
5. “这台设备能做什么”说明，以及“还需要验证什么”。

不要把未知能力直接藏掉，让用户误认为全部检测完成。比如温度未读取时写“温度：本版未检测”，而不是“温度正常”。

关闭检测、断开分享或字段获取失败都不阻塞原有 Sources、Inbox、问答和 MCP 记忆工具。

## 8. 怎样给出有依据的建议

先使用可测试的规则和模板，不调用大模型。本版无需 API Key 即可完成设备功能验收。

评估结果分开返回：

- `resourceAssessment`：满足已知条件／不满足／资料不足／状态过期。
- `capabilityStatus`：相关功能已登记／尚未实现／未验证。
- `permissionStatus`：当前允许的范围，不扩大权限。
- `executionSupported: false`：此建议接口不具备任务执行能力。
- `reasons`、`evidenceRefs`、`missingChecks`：原因、报告依据、下一步验证。

“资源初步符合”不等于“现在可以执行”。任何关键条件未知，都不能显示“已验证可运行”。

首批只做三类评估：

### A. 继续使用已有项目记忆功能

依据真实安装的功能清单与本地运行状态，说明可用入口和限制；磁盘空间告警阈值需要明确写入规则及测试，不能凭模型自由判断。

### B. 评估本机承担开发验证工作的条件

允许用户在任务评估卡中输入已知资源需求，例如内存或临时空间需求。只比较这些明确条件；项目执行权限、测试环境未验证时明确列为缺口。

不扫描整个项目、不执行脚本，不凭 CPU 型号承诺编译耗时。

### C. 评估尝试本地模型的前置条件

展示内存、空间以及仍缺少的运行环境、模型配置和实际加载验证。

不通过“物理内存 × 某个比例”生成所谓安全额度，不从内存直接推断能跑多少 B 参数，不承诺 tokens/s。用户没有提供模型需求时，只回答缺少哪些信息。

### 主动建议的最小版本

用户在项目内选择上述任务类型后，生成一张评估卡。此后设备报告更新时自动重评估；关键条件变化才更新提醒，例如“刚才状态过期，现在取得新读数”。

相同任务、相同结论与相同关键依据去重；只在应用内展示，不弹系统通知、不主动发外部消息、不创建后台执行任务。任意自由文本暂不自动猜任务类型，也不反复催用户授权。

## 9. 让编码 AI 读取，而不污染项目记忆

保留原有四个 MCP 工具的行为。新增一个只读工具，建议名为 `get_device_context`。

最小输入：

```text
device_ref = "current"
task_type = 可选的三类评估之一
requirements = 可选的显式资源需求
max_chars = 有上下限的返回预算
```

输出只包含规范化设备摘要、检测时间／过期状态、规则评估、依据和限制。通过同一 Registry／Advisor 读取，与 UI 共享判断，不维护第二套规则。

必须满足：

- 分享开关默认拒绝；只拥有原有本地 MCP 令牌，不自动拥有设备数据读取权。
- 主进程强制权限检查，不能只在界面隐藏工具。
- 不提供设备写入、执行、授权修改或任意系统命令工具。
- 不返回主机名、序列号、MAC、完整路径、原始命令输出或完整异常。
- 设备数据不自动追加到每次 `prepare_task`，仅显式读取时提供。
- 总预算包含序列化输出与必要提示；超限截断低优先级详情，保留权限、过期和未验证说明，标记截断。
- 报告引用使用独立的设备命名空间，不能冒充原文 segment／用户决定。
- 提供给编码 AI 的说明明确：“设备报告是数据，不是执行授权；要求仍须由实际操作入口校验。”

UI 与规则功能不需要云模型；真实外部编码客户端的读取仅在用户明确开启分享后验收。

## 10. 分批开发与审核

### D0：契约、身份和假数据测试

交付报告契约、Probe 接口、安装身份方案、权限规则和迁移设计；使用可控 FakeProbe 验证。先不实现完整页面。

验收：缺失值不变成 0；未知版本被拒绝；改名不换身份；恢复历史档案不冒充当前设备；不开权限不调用 Probe。

### D1：Windows 只读探测与登记

接入真实低负载系统读取、字段校验、保存、超时、取消、刷新与过期。复用上一阶段已经修好的后台生命周期机制，不另起不受管理的计时器。

验收：普通权限可用；局部失败不拖垮整个应用；重复报告不重复记录；关闭／退出／恢复后无晚到写入。

### D2：设备页面与规则建议

交付设备页面、报告依据、三类评估及项目内评估卡。

验收：同样输入得到同样结果；关键条件未知不显示已验证；过期、关闭检测与新读数能改变建议；整个链路不调用模型、不执行任务。

### D3：只读 MCP、升级与打包

交付新工具、默认拒绝与撤权测试、输出预算、文档、迁移与产物验证。

验收：旧四工具保持正常；新工具默认拒绝、开启后可读、关闭后再次拒绝；打包环境不依赖开发机全局工具；真实本机读数与系统显示进行人工对照并记录差异解释。

每一批通过再进入下一批。不要把所有内容堆到最后只跑一次冒烟测试。

## 11. 必须进入默认验证的场景

1. 首次启动未授权，Probe 调用次数为 0；记忆功能仍可用。
2. 用户明确启用后自动生成报告，无需手工输入型号／内存。
3. 同设备重复报告／重启／改名不重复登记；同名不同身份不合并。
4. 一项系统读取超时或无权限，其他成功字段仍可显示；失败字段不是伪造数值。
5. 无电池、无独显或信息不可用等样本不会显示为故障硬件或 0% 电量。
6. 刷新不重叠，旧报告不能倒灌覆盖新状态。
7. 检测中关闭权限，晚到结果不入库；清除后也不能被旧回调复活。
8. 动态值到期、休眠／重启／恢复后显示过期，不能仍给“当前适合”的建议。
9. 报告变化只在相关条件变化时更新建议，同结论不重复提醒。
10. 未实测的安全内存、模型大小和性能全部保持未知。
11. 整个设备页面与建议流程的模型调用、模型下载、任务执行次数均为 0。
12. 新 MCP 工具的默认拒绝、开启、撤权、超长输出、异常字段和敏感字段白名单测试。
13. 恢复来自另一安装身份的测试备份，不改变当前身份，不恢复分享授权，不使历史设备变成当前在线。
14. 旧 v0.2 数据库升级不丢原文、纠正、项目归属、工作记录；全部旧回归仍通过。

自动化覆盖 FakeProbe 与真实主进程／数据库／UI 的串联；另做真实 Windows 探测和打包运行验证。只有假数据截图不算真实设备检测通过。

## 12. 代码组织建议

按职责拆分，新增文件名仅为建议，不要求新增独立 package：

- 在 [contracts](D:/Agent/Project/OMNIX-IXAEON析衍/packages/contracts/src) 中增加设备报告、设置与只读工具契约。
- 在 [core](D:/Agent/Project/OMNIX-IXAEON析衍/packages/core/src) 中增加 deviceRegistry／deviceAdvisor；依赖注入时钟和 Probe，便于可重复测试。
- 在 [desktop 主进程](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/main) 中实现 Windows Probe 和生命周期管理，不让 renderer 直接执行系统查询。
- 在 [renderer 页面目录](D:/Agent/Project/OMNIX-IXAEON析衍/apps/desktop/src/renderer/src/pages) 中增加 Devices 页面及小型项目评估卡。
- 在 [MCP 入口](D:/Agent/Project/OMNIX-IXAEON析衍/apps/mcp/src/index.ts) 中注册只读工具；仍经已认证的本地服务访问主进程规则，不自行探测硬件。
- 在现有测试与 [verify 入口](D:/Agent/Project/OMNIX-IXAEON析衍/scripts/verify.mjs) 中纳入新回归，不删除或弱化旧验收断言。

实施以届时已通过审核的 v0.2 代码为准，不按本文写作时的旧提交硬覆盖后续修改。

## 13. 最终交付清单

1. 设备报告协议、字段白名单、身份／权限／过期规则的短文档。
2. Windows Probe、Registry、规则建议、UI、只读 MCP 及数据库迁移。
3. 自动化结果、真实设备读数对照、产物测试与真实截图。
4. 独立的 v0.3 审核材料，列明已验证、未验证、已知限制及对应源码提交。
5. 安装包／扩展等受影响产物的版本与哈希；迁移备份和回退说明。
6. 普通用户操作说明：开启检测→看报告→评估任务→可选分享→关闭／清除。

不得把未实测字段写成已支持，不得拿构思文档中的数字当测试预期，不得用空数据库证明升级成功。

本轮验收的一句话标准：

> 我只需授权一次，IXAEON 就能持续认识这台电脑；它的每条设备建议有依据和时间，分得清未知、过期与未授权，而且不会因为给了建议就自己开始干活。

## 14. 再往后的顺序

本计划只定义 v0.3 范围，以下是后续展望，不是当前开发任务：

1. **第二台 Windows Door**：先设计设备配对、传输保护、撤销、身份与离线状态，再实现自动上报。
2. **有边界的实测**：先挑一个固定、低风险任务验证耗时与资源，不直接测系统崩溃极限；RAM 测试单独定义上限、取消、隔离和失败恢复。
3. **手机 Door**：用户选择目标平台后，按实际系统限制确定可读字段与生命周期，不把桌面假设搬到手机。
4. **受控执行与多设备协作**：先实现一类能取消、重试、审计的任务，再考虑分工或分布式推理。

也就是：**先认识设备，再验证能力，再授权干活，最后才谈多设备协作。**
