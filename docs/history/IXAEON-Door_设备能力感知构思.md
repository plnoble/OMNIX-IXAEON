# IXAEON Door：设备能力感知与机会式调度构思

记录时间：2026-09-04  
状态：构思记录，不属于当前 IXAEON v0.1 开发范围  
灵感来源：MLXHub 的 RAM 能力测量与多设备分布式推理

## 1. 这次构思的核心

用户提出：

> IXAEON Door，也就是子 IXAEON，可以借鉴 RAM 压力测试这样的功能。安装到一台设备后，先检测这台设备的实际情况，把结果交给主 IXAEON。以后当我问“这台设备能做什么”时，IXAEON 可以直接按照真实数据分析；当某件事正好可以使用这台设备时，IXAEON也可以主动提出建议。

这使 IXAEON Door 不再只是“把资料传给主 IXAEON 的入口”，还多了一个重要身份：

> **Door 是主 IXAEON 在每台设备上的能力探针、连接入口和受控执行端。**

Door 先认识设备，再让主 IXAEON 认识设备。主 IXAEON 不需要依靠用户回忆硬件型号，也不需要凭模糊印象猜测设备能做什么。

## 2. 这与此前设想的关系

此前已经提出：

- 子 IXAEON 可以安装到手机、电脑、NAS 等设备。
- 它先报告“我是谁、能做什么”，再报告“用户允许我看什么”。
- 它不是另一个人格，也不是一群彼此独立的小 IXAEON。
- 主 IXAEON 始终是同一个整体，Door 更像它伸向不同设备的门、眼睛、手和神经末梢。
- 移动设备可能断网、没电、发热或被系统杀后台，因此只能是机会式 Worker，不能承担核心数据库和关键调度。

这次构思把“能做什么”进一步具体化：Door 不能只读取产品型号和标称参数，还要尽可能测出设备在真实环境中的可用能力。

## 3. 三个容易混淆、但必须分开的概念

主 IXAEON 判断能否使用一台设备时，不能只看“硬件支持”。必须同时判断三件事：

| 概念                    | 通俗解释                       | 例子                                                 |
| ----------------------- | ------------------------------ | ---------------------------------------------------- |
| 设备能力 Capability     | 这台设备理论上和实测上能做什么 | 手机有 12GB RAM，能运行某个 3B 本地模型              |
| 当前可用性 Availability | 它现在是否适合做这件事         | 手机正在充电、温度正常、空闲内存充足                 |
| 用户授权 Permission     | 用户是否允许 IXAEON 这么使用它 | 只允许充电时运行，不允许读取照片，不允许使用移动网络 |

只有三者同时满足，主 IXAEON 才可以建议或派发任务。

因此：

> **“能做到”不等于“现在适合做”，更不等于“用户允许做”。**

这是 IXAEON Door 最重要的安全边界之一。

## 4. Door 安装后首先做什么

Door 第一次启动时，应先进行一次分层设备体检。

### 4.1 第一层：安全的基础识别

这一层只读取设备配置，不扫描用户生活内容，原则上可以自动进行：

- 设备种类：手机、平板、电脑、NAS、掌机或服务器；
- 操作系统、版本和架构；
- CPU、GPU、NPU/神经引擎的大致能力；
- 物理 RAM、显存或统一内存；
- 可用存储空间；
- 网络接口和局域网连接能力；
- 是否支持摄像头、麦克风、定位、蓝牙等硬件；
- 是否支持后台运行、常驻服务、容器、虚拟化或本地模型框架；
- 当前是否接通电源、电池健康和充电状态。

设备型号和标称规格只是初步信息，不能直接成为最终调度依据。

### 4.2 第二层：轻量、可停止的能力测试

在用户同意后，Door 可以做不会伤害设备的轻量测试：

- 当前进程安全可用的 RAM，而不是只看物理 RAM；
- 内存分配和释放是否稳定；
- 磁盘顺序读写与小文件读写；
- 局域网带宽、延迟和稳定性；
- CPU/GPU 短时计算速度；
- 本地模型的加载时间、首 Token 时间和生成速度；
- OCR、语音识别、Embedding、图片缩略图生成等典型小任务速度；
- 短时负载下的温度变化和降频情况；
- 后台运行是否会被系统快速暂停或杀死。

测试必须有明确上限、进度、停止按钮和冷却策略。它的目标不是把设备逼到崩溃，而是找到一个可以长期、安全使用的范围。

### 4.3 第三层：用户授权的内容入口

这一层绝不能因为“安装了 Door”就自动开启。例如：

- 是否允许读取某个照片相册；
- 是否允许读取通知、剪贴板、浏览记录或文件目录；
- 是否允许使用摄像头、麦克风和定位；
- 是否允许执行自动化操作；
- 是否允许把某类资料传给主 IXAEON；
- 是否允许只在局域网传输，或可以通过加密远程连接传输。

设备配置检测和用户内容读取必须是两套权限。扫描硬件不能成为扫描生活的借口。

## 5. RAM 压力测试应该借鉴什么

MLXHub 最值得借鉴的不是“测出一个最大数字”，而是以下思路：

1. 不相信单纯的型号表和物理内存数字。
2. 测量当前 App 或 Worker 实际能安全使用多少资源。
3. 给运行时、系统、缓存和突发情况留出安全余量。
4. 根据实测结果决定可以加载什么模型、承担什么任务。
5. 设备状态变化后重新评估，而不是一测定终身。

Door 的 RAM 测试可以产生类似以下结果：

```text
物理内存：12 GB
当前进程可用额度：6.8 GB
短时稳定分配：5.9 GB
建议长期任务上限：4.7 GB
建议模型权重上限：3.8 GB
保留给 KV Cache、运行时与系统：0.9 GB
测试时温度：正常
结果有效期：7 天，或系统升级/重启后重新检测
```

其中最重要的是“建议长期任务上限”，而不是“曾经勉强申请成功的最大值”。

### RAM 测试的安全原则

- 分阶段增加压力，每阶段都允许取消；
- 分配后实际触碰内存，避免得到虚假的可分配数字；
- 监测系统内存警告、温度和应用状态；
- 永远保留安全余量；
- 不以触发 OOM、Jetsam 或系统卡顿作为测试成功标准；
- 测试中断或 App 被系统终止后，能够识别上一次测试未完成并回退到保守值；
- 测试结果带时间、系统版本、Door 版本和环境状态；
- 系统升级、重大配置变化或长期未检测后，旧结果降低可信度。

## 6. Door 应该交给主 IXAEON 什么数据

Door 不应把一堆未经解释的系统指标全部倒给主 IXAEON。它应生成一份结构化的“设备能力档案”。

示例：

```json
{
  "device_id": "door-redmagic-01",
  "device_type": "android_phone",
  "display_name": "红魔手机",
  "door_version": "0.1.0",
  "profile_updated_at": "2026-09-04T08:00:00Z",
  "static_profile": {
    "os": "Android",
    "architecture": "arm64",
    "ram_bytes": 12884901888,
    "storage_free_bytes": 214748364800,
    "accelerators": ["gpu", "npu"]
  },
  "measured_capacity": {
    "safe_ram_bytes": 5046586572,
    "lan_latency_ms": 7,
    "lan_mbps": 420,
    "sustained_compute_score": 0.72,
    "thermal_headroom": "normal"
  },
  "current_state": {
    "online": true,
    "battery_percent": 82,
    "charging": true,
    "temperature": "normal",
    "idle": true,
    "network": "trusted_wifi"
  },
  "capabilities": [
    {
      "task_type": "local_llm_inference",
      "level": "small_model",
      "limits": { "recommended_model_gb": 3.8 }
    },
    {
      "task_type": "speech_to_text",
      "level": "available"
    },
    {
      "task_type": "image_indexing",
      "level": "available"
    }
  ],
  "permissions": {
    "allow_background_compute": true,
    "charging_only": true,
    "trusted_wifi_only": true,
    "allowed_data_scopes": [],
    "allow_automatic_execution": false
  }
}
```

具体字段以后可以调整，但必须保留以下含义：

- 设备是谁；
- 它理论上有什么；
- 实际测出了什么；
- 它现在是什么状态；
- 用户允许它做什么；
- 数据是什么时候测的；
- 结果有多可信、何时应该重测。

## 7. 主 IXAEON 如何使用这些数据

### 场景一：用户主动询问

用户问：

> 这台红魔手机现在可以做什么？

主 IXAEON 不再泛泛回答，而是根据最新能力档案回答：

```text
它目前接通电源、温度正常、安全可用 RAM 约 4.7 GB。

适合：
1. 跑小型本地模型，处理分类、脱敏和简单摘要；
2. 做离线语音转文字；
3. 在夜间给照片生成缩略图和本地索引；
4. 作为临时局域网推理节点。

不适合：
1. 保存 IXAEON 核心数据库，因为手机可能离线；
2. 承担不能中断的长任务；
3. 在当前授权下读取照片，因为你还没有开放相册权限。
```

回答必须说明依据来自哪次检测，并标明动态状态可能已经过期。

### 场景二：主 IXAEON 主动建议

当出现适合该设备的任务时，主 IXAEON可以建议：

```text
红魔手机现在正在充电、处于空闲状态，温度正常。
今晚的 8,000 张照片缩略图和初步分类可以交给它处理，
预计不会占用主电脑。要授权这一次任务吗？
```

建议中应包含：

- 为什么是这台设备；
- 它当前的状态；
- 需要什么数据权限；
- 预计用时、电量、网络和存储消耗；
- 任务能否随时停止；
- 失败后会发生什么。

### 场景三：预授权后的机会式执行

用户可以设置规则：

```text
当红魔手机接通电源、连接家庭 Wi-Fi、温度正常且空闲时，
允许自动执行可中断的图片索引和语音转写任务。
```

即使有预授权，Door 也只能领取符合规则的任务。主 IXAEON 应保存派工原因和执行记录，用户可以随时暂停、撤销和查看。

## 8. 从“设备体检”到“任务建议”的基本流程

```text
Door 安装与配对
      ↓
基础设备识别
      ↓
用户同意轻量能力测试
      ↓
生成设备能力档案
      ↓
主 IXAEON 的 Device Registry 保存档案
      ↓
任务出现时由 Capability Matcher 匹配设备
      ↓
检查：设备能力 + 当前可用性 + 用户授权
      ↓
向用户建议，或按预授权派发
      ↓
Door 执行并回报结果、资源消耗和失败原因
      ↓
主 IXAEON 更新对这台设备的认识
```

这个循环让 IXAEON 不只是保存一张静态硬件清单，而是逐渐学会：

- 这台设备标称上能做什么；
- 它实际做某类任务有多快；
- 它在什么条件下容易过热或掉线；
- 用户愿意让它做什么；
- 下次应该不应该再把类似任务交给它。

## 9. 与 MLXHub 多设备分布式的关系

MLXHub 会先测量每台设备的内存，再按能力把模型层分给不同设备。Door 可以借鉴这种“先探测、再分配”的思路，但 IXAEON 不应立刻照搬模型切层。

IXAEON 更适合先做“工人池”：

- 手机负责语音转写、图片预处理和隐私过滤；
- 旧电脑负责批量 OCR、Embedding 或测试；
- NAS 负责原始档案和备份；
- GPU 电脑负责大模型、视频和重计算；
- 主 IXAEON 负责记忆、权限、调度、判断和向用户解释。

这些设备首先是按任务分工，不是把 RAM 简单相加。

以后如果本地模型确实大到一台设备装不下，可以：

1. 让 IXAEON 调用 MLXHub 之类已经实现分布式推理的服务；
2. 把它当成一个特殊的本地模型 Provider；
3. 只有在需求和收益被真实数据证明后，再考虑 IXAEON 自己管理模型分片。

## 10. Door 的身份边界

虽然可以通俗地称它为“子 IXAEON”，但实现上应避免让每个 Door 变成一个独立人格。

建议的理解是：

- 主 IXAEON 是唯一的长期身份和决策中心；
- Door 是设备上的本地代理；
- Door 有局部判断能力，但没有脱离主 IXAEON 的长期目标体系；
- Door 可以在离线时完成已授权的小任务，恢复连接后同步结果；
- Door 不单独修改主记忆，不单独扩大权限，不替用户做高风险决定。

一句话：

> **Door 可以有手脚和反射，但不另长一个主人不知道的脑子。**

## 11. 建议的渐进开发路线

这项能力不应进入当前 v0.1 修复范围。可以在主 IXAEON 的原文、权限、MCP 和自动采集稳定后，按以下顺序发展。

### Door A：只读设备档案

- 配对 Door；
- 读取安全的基础设备信息；
- 做轻量能力测试；
- 主 IXAEON 可以回答“我有哪些设备”和“它们大致能做什么”；
- 不执行任务，不扫描用户内容。

这是最适合作为 Door 第一版的范围。

### Door B：动态状态与建议

- Door 定期或在状态变化时上报电量、充电、温度、网络和负载；
- 主 IXAEON 可以发现适合的设备并向用户提出建议；
- 仍由用户逐次确认执行。

### Door C：受控 Worker

- 定义少量标准任务，例如 OCR、语音转写、Embedding、图片索引和小模型推理；
- 支持预授权规则；
- 支持暂停、取消、失败重试和执行审计；
- 移动设备只承担可中断、可重试任务。

### Door D：多设备联合任务

- 多台设备协作处理批量任务；
- 根据实测能力动态分片；
- 节点掉线后重新分配；
- 必要时接入 MLXHub 等分布式模型服务；
- 是否进行模型层级分布式推理，由真实需求和测试决定。

## 12. 主 IXAEON 需要增加的长期模块

未来可能需要以下概念，但现在只记录，不立即开发：

- **Device Registry**：保存设备身份、能力档案、状态和历史测量；
- **Capability Probe**：Door 端的能力检测器；
- **Capability Manifest**：Door 上报的标准数据格式；
- **Capability Matcher**：把任务需求与设备能力进行匹配；
- **Device Policy**：用户对每台设备设置的权限、时间和资源规则；
- **Worker Scheduler**：派发可中断任务并处理掉线、重试和回收；
- **Device Observability**：记录温度、失败率、耗时和真实效果；
- **Recommendation Engine**：发现闲置资源和现实需求之间的匹配机会，并向用户解释建议。

## 13. 必须避免的误区

1. **不能安装后就扫描整台设备。** 硬件配置检测与个人内容授权必须分开。
2. **不能把标称 RAM 当成可用 RAM。** 调度使用带时间戳的安全可用额度。
3. **不能为了测极限把设备弄崩溃。** 测试目的是寻找稳定工作区间。
4. **不能把所有设备当成同一种 Worker。** 手机、NAS、GPU 电脑的职责天然不同。
5. **不能让手机承担核心数据库。** 手机会离线、没电、发热、被带走或被杀后台。
6. **不能看到闲置资源就自动占用。** 没有授权时只能建议。
7. **不能只说“可以使用这台设备”。** 必须说明为什么、依据是什么、需要什么权限。
8. **不能把多设备宣传成简单相加 RAM。** 任务分工和模型分层是两种不同事情。
9. **不能为了追求酷炫而先做分布式。** 第一价值是让 IXAEON 知道现实世界里有什么资源可用。
10. **不能让 Door 各自形成互相冲突的长期记忆。** 长期身份和最终记忆归主 IXAEON 管理。

## 14. 尚待以后决定的问题

- Door 的正式中文名是否叫“析衍门”；
- 主 IXAEON 与 Door 默认只走局域网，还是支持加密远程连接；
- 动态状态应该多久上报，如何避免耗电；
- 设备离线后，能力档案多久算过期；
- 哪些基础配置允许自动检测，哪些测试必须逐项征得同意；
- 自动执行的默认边界在哪里；
- 能力测试结果保存多久，用户能否删除历史；
- 不同系统如何统一描述 GPU、NPU、后台能力和内存限制；
- Door 是否可以彼此直接通信，还是所有任务都由主 IXAEON 协调；
- 当主 IXAEON 不在线时，Door 可以独立完成哪些已经授权的任务；
- 是否允许借用家庭成员的设备，以及如何做到设备所有者明确同意和随时撤销。

## 15. 当前形成的一句话定义

> **IXAEON Door 是安装在各类设备上的轻量本地代理。它在不扫描用户生活的前提下识别并实测设备能力，将带时间和权限边界的能力档案交给主 IXAEON；主 IXAEON据此回答设备能做什么，并在现实任务与闲置资源匹配时，向用户提出可解释、可授权、可撤销的使用建议。**

## 16. 参考样本：mycellm

mycellm 是一个较新的开源分布式 AI 推理项目，也提供 iPhone 和 iPad App。它允许设备在三种范围内使用模型：

- 完全在本机运行；
- 把任务交给用户自己的 Mac、iPad、家庭服务器等可信设备；
- 把任务交给公共网络中的陌生算力节点，并通过积分记录算力供给与使用。

从目前公开架构来看，mycellm 最清楚、最值得借鉴的主路径，不是把多台手机的内存简单拼成一台大机器，而是：

```text
设备加入网络
    ↓
声明身份、模型和当前能力
    ↓
规划器寻找可用节点
    ↓
按照信任范围、资源状态和路由条件筛选
    ↓
把一次完整请求交给合适节点
```

因此，MLXHub 和 mycellm 给 Door 的启发不同：

| 参考对象 | 主要启发                                                               |
| -------- | ---------------------------------------------------------------------- |
| MLXHub   | 实测每台设备的可用内存，并在多台苹果设备之间分配模型层                 |
| mycellm  | 让每台设备成为有身份、有实时状态、有信任边界、能够接受或拒绝任务的节点 |

### 16.1 Door 应借鉴的实时设备状态

mycellm 的 iOS 节点会观察：

- 温度状态以及是否发生降频；
- 电池电量、是否充电和是否开启低电量模式；
- 网络类型、是否按流量计费、是否受限；
- 可用存储；
- App 在前台还是后台；
- 当前设备是否适合继续向其他节点提供推理服务。

如果设备过热、进入低电量模式或电量低于安全线，它会在本机把自己从“算力提供者”降级为“只使用别人的算力”。这个变化不需要等待中央调度器批准。

Door 应采用同样的基本原则：

> **主 IXAEON 可以选择和建议设备，但 Door 保留最后的本地否决权。**

即使主 IXAEON 数秒前判断手机适合执行任务，Door 在真正开始前也必须再次检查本地状态。如果温度、电量、网络或用户活动已经变化，Door 可以拒绝、暂停或延期。

### 16.2 从“一次检测”改成“两类信息”

研究 mycellm 后，Door 上报给主 IXAEON 的信息应明确分为：

1. **能力声明**：变化较慢，例如设备架构、安全 RAM 上限、已安装模型、支持的任务类型；
2. **实时状态**：变化很快，例如温度、电量、充电、网络、前后台和当前负载。

主 IXAEON 不能用昨天的电量和温度调度今天的任务。能力声明可以缓存较久；实时状态必须带时间戳，并在派发任务前重新确认。

### 16.3 引入“只规划，不执行”

mycellm 提供了一个很有价值的思路：先询问规划器准备怎样安排请求，而不立即运行。

IXAEON 以后也应提供类似的 Task Plan：

```json
{
  "task": "transcribe_audio",
  "selected_device": "desktop-gpu-01",
  "considered_devices": [
    {
      "device": "iphone-01",
      "accepted": false,
      "reason": "low_power_mode"
    },
    {
      "device": "tablet-01",
      "accepted": false,
      "reason": "untrusted_network"
    },
    {
      "device": "desktop-gpu-01",
      "accepted": true,
      "reason": "model_ready_and_charging"
    }
  ]
}
```

这样用户不只知道“最后用了谁”，还可以追问：

- 为什么不用另一台设备；
- 是能力不足、当前不可用，还是没有授权；
- 如果改变某个条件，结果会不会不同。

这让调度成为可解释、可检查的决定，而不是黑箱。

### 16.4 把信任范围放到每个任务上

mycellm 将路由范围分成本机、可信设备和公共节点。Door 可以借鉴这个分层，但 IXAEON 的默认规则应更保守：

| 信任范围            | 含义                       | 默认可处理的数据                       |
| ------------------- | -------------------------- | -------------------------------------- |
| `local_only`        | 只能在资料所在设备处理     | 私密原文、密钥、个人记忆               |
| `trusted_devices`   | 可以交给用户明确配对的设备 | 用户授权的照片、录音、文档与派生数据   |
| `external_provider` | 允许交给外部云或公共算力   | 默认仅限明确授权且最小化后的非敏感内容 |

信任范围必须属于具体任务和具体数据，不能只设置一个全局“允许联网”开关。

mycellm 的公开说明也表明，公共算力节点可能看到完整提示词；敏感信息扫描只能降低风险，不能构成保密保证。因此 IXAEON 不应把聊天原文、长期记忆或私密文件自动发送给公共算力。即使未来支持公共节点，也必须逐次或按清晰规则授权，并尽量只发送完成任务所需的最小信息。

### 16.5 Door 的角色应受到限制

mycellm 的 iOS 客户端有意不承担协调器、管理后台和全局账本等职责。这进一步支持此前的判断：

- Door 可以探测能力、执行任务、保留局部队列和短期缓存；
- Door 不负责主 IXAEON 的核心记忆；
- Door 不成为整个设备网络的唯一调度器；
- Door 离线或被系统暂停，不应破坏整个 IXAEON；
- 主 IXAEON 失联时，Door 只能完成事先授权、可以安全重试的任务。

尤其在 iOS 上，App 进入后台后可能很快被暂停。因此手机 Door 必须明确上报类似 `serving_requires_foreground` 的限制，只承担机会式、可中断、可恢复的工作。

### 16.6 内存管理不能止于安装时压力测试

mycellm 的后续版本加入了运行时内存压力监测：压力升高时清理缓存，严重时卸载闲置模型，并根据实测值修正预估模型。Door 应把这些机制纳入长期设计：

1. 安装时进行安全能力检测；
2. 模型加载前根据权重、上下文和缓存进行预检；
3. 如果资源不足，缩短上下文或选择更小模型；
4. 运行中持续监听系统内存压力；
5. 必要时释放缓存、卸载闲置模型或停止任务；
6. 将实际峰值、耗时、温升和失败原因反馈给主 IXAEON，修正下一次估算。

也就是说，设备画像不是一次生成的说明书，而是一份会被真实任务不断校准的经验档案。

### 16.7 不应照搬的部分

mycellm 的公共算力与积分市场不适合成为 Door 早期目标：

- 公共节点存在提示词暴露风险；
- 积分体系目前仍处于早期阶段，身份滥用和作弊问题并未彻底解决；
- 引入陌生算力会同时带来身份、支付、滥用、内容安全和责任边界问题；
- 这些问题会把 Door 从个人设备协作工具迅速扩大成一个平台型项目。

IXAEON 初期更适合记录“资源成本”，而不是发行积分：

- 本次任务耗时；
- 峰值内存；
- 设备温升；
- 耗电与网络流量；
- 对用户前台操作的影响；
- 成功率和中断次数。

这些数据足以帮助主 IXAEON逐渐学会如何珍惜和使用用户自己的设备。

## 17. 研究 mycellm 后修正的 Door 定义

Door 不应只被定义成设备检测器，而应同时具有四个角色：

1. **探针**：识别和实测设备能力；
2. **守门员**：根据本地实时状态和用户规则决定是否接任务；
3. **执行器**：运行已经授权、符合能力边界的标准任务；
4. **汇报器**：返回结果、资源消耗、拒绝原因和能力变化。

修正后的派工流程是：

```text
Door 本地探测与本地规则
      ↓
上报能力声明和实时状态
      ↓
主 IXAEON 更新 Device Registry
      ↓
任务规划器列出候选设备和排除原因
      ↓
检查数据的信任范围与用户授权
      ↓
Door 在执行前进行最后一次本地检查
      ↓
接受、延期或拒绝
      ↓
返回结果和实测资源数据，校准设备画像
```

这一修正不改变主 IXAEON 的核心地位。它只是让每扇 Door 都拥有必要的“反射能力”：会观察自己的身体状况，会守住本机权限边界，也会在不适合工作时说“不”。

参考资料：

- mycellm iOS 项目：https://github.com/mycellm/mycellm-ios
- mycellm 主项目：https://github.com/mycellm/mycellm
- 网络与信任范围：https://docs.mycellm.dev/architecture/networks/
- 公共节点发现与路由：https://docs.mycellm.dev/architecture/public-bootstrap/
- iOS 更新日志：https://github.com/mycellm/mycellm-ios/blob/main/CHANGELOG.md
- 已知问题：https://github.com/mycellm/mycellm/blob/main/KNOWN_ISSUES.md
- 服务条款与公共节点隐私说明：https://mycellm.ai/terms/
