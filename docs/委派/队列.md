# 委派队列

执行方（Grok、Codex 等）按顺序领活，不用等人派。怎么领、做完的标准、怎么合并，见 [AGENTS.md](../../AGENTS.md)「委派流水线」。

规则摘要：

- 领第一个「待领」且依赖都已完成的任务，把状态改成「进行中（你的名字）」并提交这一行的改动；
- 每个任务在自己的分支和工作目录里做（`git worktree add ../ixaeon-<任务号> -b <你的名字>/<任务号>`）；
- 做完的标准：`node scripts/acceptance.mjs run <任务号>` 全过、`node scripts/verify.mjs` 全过、推送后 GitHub 上的 verify 检查是绿的；
- **锁定的验收测试不许改**（指纹在 [锁定验收.json](锁定验收.json)）。觉得测试本身有错：停下，在交付说明里写清楚。

## 可以领的

| 序号 | 任务                | 委派单                       | 档位 | 依赖 | 状态 |
| ---- | ------------------- | ---------------------------- | ---- | ---- | ---- |
| 1    | A1 分析失败耐心重试 | [A1](A1-分析失败耐心重试.md) | A    | 无   | 待领 |
| 2    | A3 记忆桥出处标签   | [A3](A3-记忆桥出处标签.md)   | A    | 无   | 待领 |
| 3    | A2 迁移前自动备份   | [A2](A2-迁移前自动备份.md)   | B    | 无   | 待领 |

档位：**A** = CI 绿即可由用户合并；**B** = 整合方复审后合并（迁移、删数据、权限与隐私、提示词、改变记忆含义的）。

## 整合方正在设计（写好委派单和锁定的验收测试后才进上面的表）

| 任务                   | 说明                                                |
| ---------------------- | --------------------------------------------------- |
| T1 待办表（迁移）      | 整合方自己做：迁移只由整合方写                      |
| T2 从对话里提出待办    | 整合方设计接口与验收，实现可委派                    |
| T3 待办列表界面        | 依赖 T1、T2                                         |
| C3 测试套件合并        | 整合方先逐个分类（留 / 并 / 删及理由），执行可委派  |
| 场景一：开发整合方     | 见 [三周任务单](../三周任务单.md)「用户的目标场景」 |
| 场景二：主动研究与提醒 | 同上                                                |
