# G05 Codex 审查

- 时间：2026-09-26T12:47:05.091Z
- 分支：grok/G05（c8589ce），对照 docs/委派/G05-准备阶段也能取消.md
- Codex：0.155.0；上下文由脚本喂入（不让 Codex 跑命令）

- 必须改：`docs/委派/锁定验收.json:154`：用于补齐两处验收漏洞的 `g05-cancel-preparing-extra.test.ts` 未登记指纹，G05 的锁定验收仍只包含原有三条测试。应补充登记，保留已有测试及指纹。
- 建议：`docs/委派/交付/G05.md:65`：需要人工确认：Hermes adapter 是否在实际启动前检查取消。`adapter.start` 返回 `cancelled` 不能证明此前没有启动或外发；提供的测试均替换了真实 `AgentSession`，无法验证这条契约。
- 建议：`docs/委派/交付/G05.md:66`：需要人工确认：界面是否确实只根据持久化消息状态显示取消。丢弃 `cancelAsk` 返回值、没有“已停止”字样，本身不能排除取消失败时提前显示“已取消”；相关界面代码未提供。

结论：需要修改
