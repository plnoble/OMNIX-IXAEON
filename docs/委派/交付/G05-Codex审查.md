# G05 Codex 审查

- 时间：2026-09-26T12:39:58.187Z
- 分支：grok/G05（7e0b398），对照 docs/委派/G05-准备阶段也能取消.md
- Codex：0.155.0；上下文由脚本喂入（不让 Codex 跑命令）

必须改：docs/委派/交付/G05.md:58，将契约 2 的“每一次外发之前检查”缩成“补齐后检查一次”，放宽了规格；现有测试替换了整个 `session.run`，无法验证内部外发。需要人工确认：真实会话在异步准备后、启动 Hermes 和调用 Core 前是否检查取消，并补充对应证据。
必须改：docs/委派/交付/G05.md:59，“没有改界面”不能排除契约 3 的验收要求。需要人工确认：界面收到 `cancelled: false` 时不会显示“已停止”；交付需提供验证依据。
建议：docs/委派/锁定验收.json:155，补充测试修复了此前验收覆盖的关键缺口，却未登记到 G05 的锁定清单；建议追加其指纹，保留原锁定测试及指纹。
结论：需要修改
