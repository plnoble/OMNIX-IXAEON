# P3 项目进展简报 交付

- 分支：grok/P3，基于 main faf1f1a；档位 B
- 执行方：Grok
- 历史：第一版只交测试；整合方 2026-09-19 补全条件 4 的断言并重锁后，本版交实现。

## 已实现

`packages/core/src/runtime/projectBrief.ts` 新函数 `buildProjectBrief(db, projectId, opts?)`：现查现拼「项目近况」——最近提交（14 天内、最多 20 条，`git log` 只读、10 秒超时）、这个项目最近的 5 个编码代理会话（标题、日期、最后一句用户的话前 100 字）、最新的 5 个编码任务（目标第一行 + 中文状态，同待办页对照表）。整段 ≤2000 字：先截提交（最旧先截），再会话，最后任务；`counts` 是实际带上的条数。session.ts 的 Hermes 路径在记忆后面接上这段（Core 兜底路径不加）；回答带 `meta.projectBrief`；回答下面一行灰字显示计数。

改动（`git diff --stat origin/main...HEAD`）：

```
 apps/desktop/src/main/appRuntime.ts                |   2 +
 apps/desktop/src/renderer/src/pages/AskMessage.tsx |  21 +++
 packages/core/src/index.ts                         |   1 +
 packages/core/src/runtime/projectBrief.ts          | 114 +++++++
 packages/core/src/runtime/session.ts               |  21 ++-
 scripts/real/p3-hermes-brief.ts                    | 140 +++++++++
```

与规格不一样的地方：无。「登记读取是门槛」按 sources 里 `kind='project_snapshot'` 判定；git 失败/超时/非 git 仓库都跳过提交那块，不报错。

## 验收测试

测试由整合方锁定，本交付未改测试一行（verify 的 acceptance-lock 核对通过）。

- `packages/core/test/acceptance/p3-project-brief.test.ts`：条件 1、2、3、4、5
- `apps/desktop/test/acceptance/p3-brief-line.test.ts`：条件 6

## 自动化通过

- `node scripts/acceptance.mjs run P3`：**5/6 通过；条件 5 的断言全部通过**，失败发生在它自己的 afterEach 清理：同一条测试里 `seedProject` 调了两次，第一个库的句柄没有关，Windows 不允许删除打开中的 SQLite 文件 → `rmSync` EPERM。已用独立最小复现确认（两个库只关第二个，删第一个目录必 EPERM；都关掉就能删）。这是锁定测试自身的缺陷，我不改（AGENTS.md：测试本身有错由整合方改）；Linux CI 上不存在这个问题。条件 5 验证的行为（非 git 仓库、git 超时 → 跳过提交那块不报错）在本机是通过的。
- `node scripts/verify.mjs`：全部通过（lint、format、typecheck、unit、integration、acceptance-lock、acceptance、build、review 各轮、真实 Electron UI 各组）。
- GitHub 上的 verify：推送后看，结论补记在「已知缺口」。

## 真机通过

`node_modules/.bin/jiti scripts/real/p3-hermes-brief.ts`（临时库 + 合成项目 + 真 Hermes，两次都带上了近况，模型实际用到了）：

```
=== 问：这个项目做到哪了？｜引擎 hermes｜14 秒
带上近况： {"commits":3,"sessions":1,"tasks":2}
--- 回答：
当前项目进展如下：
- **核心功能与稳定性**：
  - 项目基础骨架已搭建完成（09-17）。
  - 已解决导入乱码问题（09-18）。
  - 已补充重试机制（09-19）。
- **当前任务状态**：
  - **交付说明**：已完成草稿，待最终完善。
  - **验收测试**：已排队，准备跑通并补齐测试用例。

=== 问：接下来该做什么？｜引擎 hermes｜2 秒
带上近况： {"commits":3,"sessions":1,"tasks":2}
--- 回答：
接下来主要聚焦在补齐自动化验证与完善交付文档，确保近期的乱码修复与重试机制稳定可靠，为项目交付做好准备。
```

如实说：模型把近况里的三条提交、两个任务都用上了（回答逐条对应）；会话「修导入乱码」的标题与提交「修好导入乱码」内容相近，单独看不出来自哪块，但提交与会话都确实带上了。

## 已知缺口

- 条件 5 在本机因测试自身的 db 句柄泄漏报 EPERM（断言通过；见「自动化通过」），等整合方决定是否修测试。
- GitHub CI：推送后看，红了立刻修（结论补记在这里）。
- 用户接受：未发生（B 档，等整合方复审）。
