# P3 项目进展简报 交付

- 分支：grok/P3，基于 main `096331d`；档位 B（**先只交测试**）
- 执行方：Grok

## 已实现

未实现。本单按 v2 B 档先把规格 6 条验收条件写成测试，等整合方看过并锁定后再写代码。

实现时按规格：

- `packages/core/src/runtime/projectBrief.ts`：`buildProjectBrief(db, projectId, opts?)`
- Hermes 路径有 `projectId` 且 `block` 非空时接在记忆后面；Core 兜底不加
- 回答 `meta.projectBrief = counts`；界面灰字 `ask-project-brief`

## 验收测试（v2 规格任务）

| 文件                                                     | 条件                                             |
| -------------------------------------------------------- | ------------------------------------------------ |
| `packages/core/test/acceptance/p3-project-brief.test.ts` | 1、2、3、4、5（条件 1 也核 `projectBrief` 计数） |
| `apps/desktop/test/acceptance/p3-brief-line.test.ts`     | 6 界面灰字                                       |

当前 5 红 1 绿：条件 3（没选项目）在现实现上已经成立。未跑 `acceptance.mjs lock`。

## 自动化通过

- `node scripts/verify.mjs`：EXIT=0（未锁定的验收测试不进日常门禁）
- GitHub 上的 verify：没看（推送后查）

## 真机通过

规格要求的真 Hermes 问答在实现阶段跑。本阶段没跑。

## Codex 审查（A 档）

B 档，不跑 Codex。等整合方复审测试。

## 已知缺口

- 条件 1 的计数从 `AgentSessionResult.projectBrief` 或 `meta.projectBrief` 读，实现时二选一写进回答 meta 即可。
- 条件 4/5 直接调 `buildProjectBrief`；超时用 `opts.runGit` 注入失败。若实现不接受该可选参数，整合方改测试或实现加注入点。
- git 提交日期用相对「今天」的 1/2/20 天前，避免写死日历。

## 整合方审核测试（2026-09-19）

规格 6 条都覆盖：1–3 走假 Hermes 截 `prompt.submit`；4–5 调 `buildProjectBrief`（超时用 `opts.runGit` 注入）；6 看灰字 `ask-project-brief`。条件 3 现实现已绿。截断只断言 ≤2000，顺序「提交→会话→任务从每块末尾截」实现时按规格做。已锁定指纹。

## 用户接受

未发生。
