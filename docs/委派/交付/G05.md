# G05 准备阶段也能取消 交付

- 分支：grok/G05，基于 main `87eab16`；档位 A
- 执行方：Grok

## 已实现

提问一开始（语义补齐、组装上下文之前）就把这一轮登记进可取消集合。准备期间点取消返回成功；补齐结束后、启动会话之前再检查一次，已取消的不启动 Hermes、不调用模型，回答按 `cancelled` 收尾。已经在回答的取消行为不变：取消仍通知会话停掉，迟到的分段不再写入。

```
apps/desktop/src/main/appRuntime.ts | 29 行新增、4 行删除（源码，计入 300 行上限）
apps/desktop/test/acceptance/g05-cancel-preparing.test.ts（新增，验收测试不计入）
docs/委派/锁定验收.json（登记指纹）
```

和规格不一样的地方：无。准备阶段取消的返回值里 `engine` 记 `'missing'`（这一轮没有启动任何引擎），`notice` 为「用户在准备阶段取消」。

## 验收测试（v2 规格任务）

`apps/desktop/test/acceptance/g05-cancel-preparing.test.ts`，已锁定。

| 用例 | 规格条件 |
| --- | --- |
| 补齐还没结束时取消，返回成功；放行后替身调用 0 次，回答状态 cancelled | 条件 1 |
| 不取消时照常调用模型，回答 complete | 条件 2 |
| 回答进行中取消，返回成功，取消之后的内容不再写入，状态 cancelled | 条件 3 |

## 自动化通过

- `node scripts/acceptance.mjs run G05`：见本次提交前的输出。
- `node scripts/verify.mjs`：全过（2026-09-24，本机）。
- GitHub 上的 verify：推送后补运行号。

## 真机通过

规格不要求真机检查（受控替身覆盖）。

## Codex 审查（A 档）

待跑 `node scripts/codex-review.mjs G05`，结论随后写进 `G05-Codex审查.md`。

## 已知缺口

- 无。

## 用户接受

未发生。
