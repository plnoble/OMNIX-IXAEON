# X1 提炼打捞依据 交付

- 分支：grok/X1，基于 main `264544d`；档位 B
- 执行方：Grok
- 本版：**先只交测试**，实现等整合方看过并锁定后再写。

## 已实现

尚未实现。本单只把规格 7 条验收条件写成测试。

## 验收测试（v2 规格，先只推测试，不锁定）

`packages/core/test/acceptance/x1-salvage-excerpt.test.ts`：

1. 拼接摘录打捞出最长连续一段；入库 `excerpt` 与原文逐字相同（含换行），不是模型给的串。
2. 整段都是编的：打捞不到，不入库。
3. 打捞出的最长一段规范化后不足 15 字：不入库。
4. 3 条结论里 2 条靠打捞、1 条编的 → 入库 2、丢 1，任务成功；审计 `extract.excerpt_salvaged` 里 `salvaged=2`、`dropped=1`。
5. 全部打捞不到 → 整份作废，旧理解不变。
6. `EXTRACT_SYSTEM_PROMPT` 含规格那句；`EXTRACT_PROMPT_VERSION === 'v6'`。
7. `requeueFailedExtractions`：只排因依据问题失败的来源，每个来源一个；网络、限流失败的不动；返回值等于实际排的个数。

新符号尚未导出：测试用运行时取 `groundExcerptInSegment` / `requeueFailedExtractions`，不静态导入，免得先交的测试把门禁 typecheck 挡掉。

## 自动化通过

- `npx vitest run --project acceptance packages/core/test/acceptance/x1-salvage-excerpt.test.ts`：现实现 1 通过 / 6 失败（条件 5 是现有「全部对不上就整份作废」的回归；其余等实现）。
- `node scripts/acceptance.mjs run X1`：未锁，不跑。
- `node scripts/verify.mjs`：验收测试不进日常 unit/integration（排队中的任务测试本来就不过）。推送后看 GitHub verify。

## 真机通过

没跑。规格写明执行方跑不了真机提炼（Key 在 Electron 加密存储里）；等实现后再在交付里写「待用户在应用里点一次『重新分析失败的资料』验证」。

## Codex 审查（A 档）

本单 B 档，不跑 Codex。

## 已知缺口

- 资料页「N 条依据按原文截短后保留」和「重新分析失败的资料（N 份）」按钮未写测试：规格验收条件 1–7 没写界面，留给实现阶段。
- 未锁定（B 档锁定是整合方的步骤）。
