# R2 CI 动作升级 交付

- 分支：grok/R2，基于 main `820c7dd`；档位 A
- 执行方：Grok

## 已实现

`.github/workflows/verify.yml` 三个 action 升到以 Node 24 为目标的当前主版本（查各自 `action.yml` 的 `runs.using`）：

| 原来                    | 升到  | `runs.using`    |
| ----------------------- | ----- | --------------- |
| `actions/checkout@v4`   | `@v7` | node24（v5 起） |
| `actions/setup-node@v4` | `@v7` | node24（v5 起） |
| `actions/cache@v4`      | `@v6` | node24（v5 起） |

不改门禁步骤、runner、`--ignore-scripts` + `pnpm rebuild electron`。缓存键仍是 `pnpm-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}`，没变。

查过的发布：checkout 最新 `v7.0.1`（2026-07-20）；setup-node 最新 `v7.0.0`；cache 最新 `v6.1.0`。v4 三条都还是 `node20`，GitHub 才会强制跑在 Node 24 上并报警。

```
 .github/workflows/verify.yml | 6 +++---
 docs/委派/交付/R2.md         | （本文件）
```

## 验收测试（v2 规格任务）

规格写明这一单没有验收测试：`acceptance.mjs` 的 `run` / `lock` / `done` 三步跳过。

## 自动化通过

- `node scripts/acceptance.mjs run R2`：跳过（规格无锁定测试）
- `node scripts/verify.mjs`（rebase 到 `846b9a4` 后本机重跑，EXIT=0）：

  ```
  ✓ lint（ESLint） — 4.3s
  ✓ format:check（Prettier） — 2.1s
  ✓ typecheck（tsc --noEmit） — 4.1s
  ✓ acceptance-lock（锁定的验收测试没被改） — 0.1s
  ✓ acceptance（已完成任务的锁定验收测试） — 3.2s
  ✓ review-p3a-connector-registry（P3-A 连接器注册表：游标/覆盖/失败/撤销/导入接线 6 项） — 1.8s
  全部通过（IXAEON v0.2 验证完成）
  ```

- GitHub 上的 verify（rebase 后待合并提交 `d9b7033`）：绿（运行 [35444794760](https://github.com/plnoble/OMNIX-IXAEON/actions/runs/35444794760)，job databaseId `105901752385`）。更早一次 [35439422513](https://github.com/plnoble/OMNIX-IXAEON/actions/runs/35439422513) 是 rebase 前的记录，只作对照。

## 真机通过

规格第 2 条（annotations 应为空），查的是 rebase 后这次运行：

```
gh run view 35444794760 --json jobs --jq ".jobs[0].databaseId"
105901752385
gh api repos/plnoble/OMNIX-IXAEON/check-runs/105901752385/annotations
[]
```

没有「Node.js 20 is deprecated」。checkout@v7 / setup-node@v7 / cache@v6 三步都是 success。缓存键未改。

## Codex 审查（A 档）

2026-09-19 重跑（脚本已改为 stdin 喂上下文）：第一次结论「需要修改」，因为交付还写着 rebase 前的运行号。已换成 `35444794760` / `105901752385`，下面再审一次。

## 已知缺口

- 只升了 `verify.yml`。仓库里如果还有别的 workflow 用 v4，本单没动（规格只点名这一份）。
- 缓存键没变；若 actions/cache v6 读不了 v4 写的条目，第一次会 miss 再写新的，交付时看运行记录。

## 用户接受

未发生。
