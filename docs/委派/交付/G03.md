# G03 关注方向接受你说的目标 交付

- 分支：grok/G03，基于 main `daf0c82`；档位 B
- 执行方：Grok
- 本版是实现，基于整合方复审补全并锁定的验收测试（main d248f79）。

## 已实现

研究端和 W1a 用同一个判断决定一条目标能不能当关注的依据。两处：

1. `packages/core/src/research/watchDirections.ts`：新增 `isWatchableMemory`——当前、没标「不对」、没结束、不是没采纳的 AI 建议（`isUnadoptedAiAdvice`，contracts）。`collectWatchMemories` 改成查出候选后用它过滤，不再自己写一套 SQL 条件。
2. `packages/core/src/research/researchStore.ts`：`createTopic` 校验 `relatedGoalId` 时用同一个 `isWatchableMemory`，另加照旧的「必须是目标」（`type = 'goal'`）。

从资料里提炼的目标（`said_by` 为空）两边都收；没采纳的 AI 建议、标了「不对」的、被取代的、已结束的两边都拒。

```
packages/core/src/research/watchDirections.ts | +37 -13
packages/core/src/research/researchStore.ts   | +12 -1
```

和规格不一样的地方：无。

## 验收测试（v2 规格任务）

测试由整合方复审补全并锁定（main d248f79）：`packages/core/test/acceptance/g03-watch-goal-origin.test.ts`，本版未改动。本机 `node scripts/acceptance.mjs run G03`：3 条全过（含整合方补的「W1a 会拿的目标点关注一定成功，不会拿的一定被拒」，用一组目标形状逐个核对）。

## 自动化通过

- `node scripts/acceptance.mjs run G03`：3 条全过（2026-09-24 本机）。
- W1a 锁定验收（`w1a-suggest-topics.test.ts`、`w1a-suggest-topics-page.test.ts`）12 条全过。
- `node scripts/verify.mjs`：全过（提交前本机跑）。
- GitHub 上的 verify：绿，运行 [35944229136](https://github.com/plnoble/OMNIX-IXAEON/actions/runs/35944229136)。

## 真机通过

规格不要求真机检查（纯本地校验，自动化覆盖）。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无。

## 用户接受

未发生。
