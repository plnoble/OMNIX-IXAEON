# G03 关注方向接受你说的目标 交付（测试）

- 分支：grok/G03，基于 main `65db5ef`；档位 B
- 执行方：Grok
- 本版只交测试，等整合方看过并锁定后再写实现。

## 已实现

未实现。本版只有验收测试：

```
packages/core/test/acceptance/g03-watch-goal-origin.test.ts（新增）
```

和规格不一样的地方：无。「研究端和记忆端用同一个判断」落实为 `isUnadoptedAiAdvice`（`packages/contracts/src/entities.ts`）：said_by='ai' 或 origin='assistant_suggestion'、且 confirmation 不是 confirmed 的拒绝；origin='user'、said_by='user'、confirmation='confirmed' 的接受。

## 验收测试（v2 规格任务）

| 用例 | 规格条件 | 实现前 |
| --- | --- | --- |
| 人工建的 ✓、提炼出的你说的 ✓、采纳过的 AI 建议 ✓、没采纳的 AI 建议 ✗（VALIDATION_FAILED 且不建主题） | 条件 1 | 按预期失败（提炼出的你说的被「只能关联用户确认的目标」拒绝） |
| 只有一条提炼出的你说的目标：collectWatchMemories 选中它，mapWatchDirections 给出的 relatedGoalId 指向它，createTopic 成功且主题关联到它 | 条件 2 | 按预期失败（同样被拒） |

## 自动化通过

未跑全套：测试尚未锁定。实现提交时补 `acceptance.mjs run G03`、verify 与 GitHub 运行号。W1a 的锁定验收在实现后必须照过。

## 真机通过

规格不要求真机检查（纯本地校验，自动化覆盖）。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无（测试阶段）。

## 用户接受

未发生。
