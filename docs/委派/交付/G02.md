# G02 项目简报尊重授权撤销 交付（测试）

- 分支：grok/G02，基于 main `0fdf813`；档位 B
- 执行方：Grok
- 本版只交测试，等整合方看过并锁定后再写实现。

## 已实现

未实现。本版只有验收测试：

```
packages/core/test/acceptance/g02-brief-authorization.test.ts（新增）
```

和规格不一样的地方：无。测试直接调用 `buildProjectBrief`，git 用注入的替身计数（`opts.runGit`），不断言进程行为。

## 验收测试（v2 规格任务）

| 用例 | 规格条件 | 实现前 |
| --- | --- | --- |
| 授权都有效：跑 git，提交、两个会话、任务都在，counts 为 {1, 2, 1} | 条件 4 | 通过（现有行为） |
| 撤销一个编码代理会话来源的授权：简报没有它的标题与最后一句，另一个照常，counts.sessions 为 1 | 条件 1 | 按预期失败（撤销后仍带上） |
| 撤销 project_snapshot 授权：runGit 零调用，没有提交块，会话与任务照常 | 条件 2 | 按预期失败（仍跑了 git） |
| 项目根目录改到快照授权范围之外：runGit 零调用 | 条件 3 | 按预期失败（仍跑了 git） |

## 自动化通过

未跑全套：测试尚未锁定。实现提交时补 `acceptance.mjs run G02`、verify 与 GitHub 运行号。P3 的锁定验收在实现后必须照过（条件 4 的约束）。

## 真机通过

没跑。规格要求跑一次 `scripts/real/p3-hermes-brief.ts` 确认正常路径不退化，属于实现阶段，实现时跑并把输出原样贴上。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无（测试阶段）。

## 用户接受

未发生。
