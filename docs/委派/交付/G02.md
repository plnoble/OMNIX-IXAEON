# G02 项目简报尊重授权撤销 交付

- 分支：grok/G02，基于 main `df9e8fb`；档位 B
- 执行方：Grok
- 本版是实现，基于整合方锁定并补全的验收测试（main d248f79）。

## 已实现

项目简报不再取用已撤销授权的资料。`packages/core/src/runtime/projectBrief.ts` 两处：

1. **最近提交**：查询改为取「授权仍有效（`permissions.status='active'`）的最近一条 `project_snapshot`」的授权路径，且用 `isPathInside(授权路径, 项目根目录)` 判断覆盖（不是两者相等——授权了上级文件夹、根目录是其子目录时照样跑）。授权撤销或根目录移出范围 → 不跑 git。
2. **编码代理会话**：查询连 `permissions` 取 `status='active'`，撤销授权的会话不再进简报。

编码任务照旧（IXAEON 自己的本地数据）。

```
packages/core/src/runtime/projectBrief.ts | +22 -8
```

和规格不一样的地方：无。

## 验收测试（v2 规格任务）

测试由整合方复审补全并锁定（main d248f79）：`packages/core/test/acceptance/g02-brief-authorization.test.ts`，本版未改动。本机 `node scripts/acceptance.mjs run G02`：5 条全过（含整合方补的「根目录是授权文件夹的子目录照样跑 git」）。

## 自动化通过

- `node scripts/acceptance.mjs run G02`：5 条全过（2026-09-24 本机）。
- P3 锁定验收（`p3-project-brief.test.ts`）5 条全过（条件 4 的要求：授权都有效时不退化）。
- `node scripts/verify.mjs`：全过（提交前本机跑）。
- GitHub 上的 verify：绿，运行 [35960577059](https://github.com/plnoble/OMNIX-IXAEON/actions/runs/35960577059)。

## 真机通过

跑了 `node_modules/.bin/jiti scripts/real/p3-hermes-brief.ts`（真 Hermes、临时库、合成 git 仓库与合成会话任务）。输出原样：

```
=== 问：这个项目做到哪了？｜引擎 hermes｜14 秒
带上近况： {"commits":3,"sessions":1,"tasks":2}
--- 回答：
当前「P3 合成项目」的进展情况如下：

1. **已完成的核心工作**：
   - 基础架构与骨架搭建（09-17）
   - 导入乱码问题修复（09-18）
   - 重试机制补齐（09-19）
   - 乱码修复后的测试验证（09-24）

2. **当前进行与待推进任务**：
   - **验收测试**：已列入排队，待补充完善。
   - **交付说明**：处于草稿阶段，待最终定稿。

=== 问：接下来该做什么？｜引擎 hermes｜4 秒
带上近况： {"commits":3,"sessions":1,"tasks":2}
--- 回答：
根据当前项目状态，基础功能与问题修复（重试、乱码）已就绪，接下来应重点围绕**测试闭环**和**交付准备**推进。

建议待办：
- 运行现有测试套件，确认导入乱码修复及重试机制通过
- 编写并补全验收测试用例，覆盖核心链路
- 完善「交付说明」草稿并定稿
```

正常路径没有退化：近况照常带上（提交 3、会话 1、任务 2），模型回答基于这些近况。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无。

## 用户接受

未发生。
