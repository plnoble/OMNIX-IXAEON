# G07 搜索预算按天恢复 交付（测试）

- 分支：grok/G07，基于 main `f36485c`；档位 B
- 执行方：Grok
- 本版只交测试，等整合方看过并锁定后再写实现。不加迁移（`daily_request_cap`、`request_budget_day` 已由迁移 35 提供）。

## 已实现

未实现。本版只有验收测试：

```
packages/core/test/acceptance/g07-daily-budget.test.ts（新增）
apps/desktop/test/acceptance/g07-follow-budget.test.ts（新增）
apps/desktop/test/acceptance/g07-budget-page.test.ts（新增）
```

和规格不一样的地方：无。「今天」按本地时区，测试用本地中午构造日期。

## 验收测试（v2 规格任务）

| 用例 | 规格条件 | 实现前 |
| --- | --- | --- |
| 按天的主题四个不同日期各检查一次，每天都搜 | 条件 1 | 按预期失败（[true, true, true, false]，第四天额度耗尽不恢复） |
| 按天的主题同一天第 4 次不搜 | 条件 2 | 通过（现有额度行为） |
| 累计的主题额度用完后换一天也不恢复 | 条件 3 | 通过（现有行为，不许退化） |
| 搜索已配置时「关注」建出的主题 daily_request_cap = 3、request_cap = 3 | 条件 4 | 按预期失败（daily_request_cap 为 null） |
| 搜索没配置时仍是 paid_budget_mode = 'none' | 条件 4 | 通过（W1a 现有行为） |
| 研究页：按天「今天还能搜 N 次（每天 3 次）」/「今天的搜索额度用完了，明天恢复」；累计「还能搜 N 次（总额度）」/「搜索额度用完了，要继续请调高上限」 | 条件 5 | 按预期失败（页面仍写「剩余 N 次」） |

## 自动化通过

未跑全套：测试尚未锁定。实现提交时补 `acceptance.mjs run G07`、verify 与 GitHub 运行号。W1a 的锁定验收在实现后必须照过。

## 真机通过

规格不要求真机检查（受控搜索替身覆盖，不真搜索）。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无（测试阶段）。

## 用户接受

未发生。
