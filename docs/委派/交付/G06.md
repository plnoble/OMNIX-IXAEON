# G06 对话的项目固定 交付（测试）

- 分支：grok/G06，基于 main `51de87c`；档位 B
- 执行方：Grok
- 本版只交测试，等整合方看过并锁定后再写实现。

## 已实现

未实现。本版只有验收测试：

```
apps/desktop/test/acceptance/g06-conversation-project.test.ts（后端，新增）
apps/desktop/test/acceptance/g06-conversation-project-page.test.ts（界面，新增）
```

和规格不一样的地方：无。测试锁定的界面细节（规格没写死的）：

- 锁定提示用 `data-testid="ask-project-locked"`，文案含「换项目请开新对话」。
- 对话的项目取 `getConversation` 返回的 `conversation.projectId`（列表项同字段）；项目名从 `AskPage` 的 `projects` 属性按 id 取，没有项目的显示「全部项目」。

## 验收测试（v2 规格任务）

| 用例 | 规格条件 | 实现前 |
| --- | --- | --- |
| 在 A 的对话里提问，派给引擎的是 A 的上下文（替身截 run 入参 projectId） | 条件 2 | 通过（现有行为即如此） |
| 传来与对话不一致的 projectId → VALIDATION_FAILED「这个对话属于另一个项目，换项目请开新对话」，替身零调用、不新增消息 | 条件 3 | 按预期失败（当前不校验） |
| 当前选 B 时打开 A 的旧对话，下拉框回到 A 且不可选，有锁定提示 | 条件 1 | 按预期失败（下拉框停在 B） |
| 新对话可选 B；发出第一句后下拉框锁定为 B，且发出的 projectId 是 B | 条件 4 | 按预期失败（发完仍可选） |
| 列表每项显示所属项目名，无项目的显示「全部项目」 | 条件 5 | 按预期失败（列表只有标题） |

## 自动化通过

未跑全套：测试尚未锁定。实现提交时补 `acceptance.mjs run G06`、verify 与 GitHub 运行号。

## 真机通过

没跑。规格要求的临时数据目录复现（建 A、B，切到 B 再打开 A 的对话）属于实现阶段，实现时补并贴截图或描述。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无（测试阶段）。

## 用户接受

未发生。
