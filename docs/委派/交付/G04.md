# G04 编码任务按运行归属 交付（测试）

- 分支：grok/G04，基于 main `397de41`；档位 B
- 执行方：Grok
- 本版只交测试，等整合方看过并锁定后再写实现。不加迁移（`coding_tasks.origin_run_id` 已由迁移 35 提供）。

## 已实现

未实现。本版只有验收测试：

```
apps/desktop/test/acceptance/g04-task-origin.test.ts（新增）
```

和规格不一样的地方：无。归属的断言点是回答消息的 `meta.proposedTodos`（T2b 待办卡的同一条路径）。「另一个运行建的任务」由替身在本轮等待期间直接插入（`origin_run_id` 指向别的运行），这样时间窗口分不出、只有归属分得出。

## 验收测试（v2 规格任务）

| 用例 | 规格条件 | 实现前 |
| --- | --- | --- |
| propose_task 建任务时 origin_run_id 等于这次调用的 runId | 条件 3（契约 2） | 按预期失败（当前为 null） |
| 等待期间别人运行建的任务、没有归属的旧任务都不进 A 的回答，A 自己的照进 | 条件 1、条件 4、条件 3 的「照常出现」 | 按预期失败（时间窗口把三条都算进来） |
| 那个任务出现在它自己的运行（对话 B）的回答里 | 条件 1 后半 | 通过（现有行为） |
| 同一项目两轮并发，各自的任务各归各的回答 | 条件 2 | 按预期失败（两边都看到两条） |

## 自动化通过

未跑全套：测试尚未锁定。实现提交时补 `acceptance.mjs run G04`、verify 与 GitHub 运行号。T2b 的锁定验收在实现后必须照过。

## 真机通过

规格不要求真机检查（纯本地归属，受控替身覆盖）。

## Codex 审查（A 档）

B 档，不跑 Codex。

## 已知缺口

- 无（测试阶段）。

## 用户接受

未发生。
