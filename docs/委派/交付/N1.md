# N1 要求清单只在对上时提醒 交付

- 分支：grok/N1，基于 main `69124a8`；档位 B
- 执行方：Grok
- 本版：**先只交测试**，实现等整合方看过并锁定后再写。不加迁移。

## 已实现

尚未实现。本单只把规格 8 条验收条件写成测试。

## 验收测试（v2 规格，先只推测试，不锁定）

`packages/core/test/acceptance/n1-requirement-matches.test.ts`：

1. 一个方向两条要求、三条发现：A 全 `meets`、B 一 `meets` 一 `fails`、C 一 `meets` 一 `unknown` → `listMatchedFindings` 只返回 A。
2. 没有要求的方向：`judgeFindings` 不调模型、不写库；它的发现不进「符合你要求的」。
3. 判过不重判；新加一条要求后再调，只对这条新要求判（调用次数 = 发现数）。
4. 发给模型的只有标题、摘录、URL、要求原话；库里另有一条个人记忆，请求里看不到。
5. 模型不通：`judgeFindings` 返回 `failed > 0`、不抛错；这次检查仍成功，发现仍入库。
6. `isNew`：从没看过全是新；`markMatchedFindingsSeen` 之后旧的不是新；后来的对上发现又是新。
7. 核心：能加、能删要求；超过 200 字报错不写库。

`apps/desktop/test/acceptance/n1-requirements-page.test.ts`：

7. 概览页有对上发现时显示 `overview-matched-findings`，每条列出对上的要求与理由；没有时整块不出现；点「都看过了」调 `markMatchedFindingsSeen` 并刷新；侧栏 `nav-research-badge` 显示未看条数，0 不显示。W1b「最近的新发现」仍在它下面。
8. 研究页能加、能删要求；没有要求时提示「没有要求就不会提醒你」；超 200 字 IPC 报错，列表不变。

新符号尚未导出：核心测试用运行时取 `listRequirements` / `addRequirement` / `removeRequirement` / `judgeFindings` / `listMatchedFindings` / `markMatchedFindingsSeen`，不静态导入，免得先交的测试把门禁 typecheck 挡掉。

## 自动化通过

- 现实现尚未提供这些符号，验收测试预期失败（先交测试）。
- `node scripts/acceptance.mjs run N1`：未锁，不跑。
- `node scripts/verify.mjs`：验收测试不进日常 unit/integration。推送后看 GitHub verify。

## 真机通过

规格写明：判定用的模型 Key 在 Electron 加密存储里，执行方跑不了模型那一段——**待用户在应用里验证**。

能跑的：`scripts/real/n1-matched-findings.ts` 用临时数据目录启动应用，往库里塞合成的方向、要求与发现（判定结果直接写表），确认概览页那块、理由、「都看过了」、侧栏角标。本版先只交测试，真机等实现后再跑。

## Codex 审查（A 档）

本单 B 档，不跑 Codex。

## 已知缺口

- 未锁定（B 档锁定是整合方的步骤）。
- 检查流程何时调用 `judgeFindings`（定时轮次与立即检查产生新发现之后）等实现时接上；条件 5 已要求检查失败不影响发现入库。
- 规格契约 3 的 IPC 名称按原文写进测试；实现时从 `packages/core/src/index.ts` 导出存取函数。
