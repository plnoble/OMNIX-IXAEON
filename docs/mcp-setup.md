# Codex / MCP 接入说明

> 状态：M3 里程碑交付后生效。当前为占位结构。

## 前提

1. IXAEON 桌面应用已运行（本地接口监听 `127.0.0.1:43191`）。
2. 已在 IXAEON 中创建项目并导入资料。
3. MCP 访问令牌已生成（首次设置自动生成，可在设置页重新生成）。

## 配置

（M3 交付时填写：Codex 桌面端 / CLI / IDE 扩展的具体配置位置与一键生成片段。）

## 工具

| 工具 | 用途 |
|---|---|
| `prepare_task` | 开工前取得项目简报（目的/状态/决定/否决/未完成/最近工作，全部带引用） |
| `search_context` | 背景不足时检索原文与结论 |
| `get_source_excerpt` | 需要核对时展开引用片段 |
| `record_work_result` | 完工/部分完成/失败后写回结果与遗留问题 |
